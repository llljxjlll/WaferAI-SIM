# P5.1-R5b：固定 TP6 模型真实 CLI 形状门禁修复

日期：2026-09-15。R5 的直接 `_six_die_fixed_model_case(10,10)` 源测试全部通过，但第一次真正调用 `--fixed-global-tp6 --mesh-size 10x10` 时，旧 `_parse_args()` 的 `fixed_shapes` 白名单忽略新模式，进程在生产编译前 exit2：`this mesh size requires --scaled-all-dies`。原始 stdout 保留在 `/tmp/m1-fixed-tp6-10x10-fresh0/cli.stdout.txt`，没有 linked/NPUP/ProgramIO/NpuSim；该目录绝不可作为10×10成功收据。

修复 CLI：固定 TP6 显式模式允许旧发布 envelope 中物理 Die 数至少六的所有矩形，包括10×10；未选新模式时保留旧白名单；同时选择 scaled/fixed 和固定 TP6 在少于六 Die 上都以明确错误拒绝。`run()` 已有同模式冲突拒绝，生产逻辑与模型映射不变。对实际 `_parse_args()` 的10×10接受、2×2非法、双模式非法及源映射回归 **5/5 PASS，20.310s**。测试覆盖的是曾经失败的真实 CLI 入口，不把修补前的直接helper成功换算为后端成功。

下一步将精确提交修复，在新的clean detached源码工作树和新的空 fresh 目录重新运行10×10 fixed-global TP6全三段；两次独立 native 成功后方可在M3固定模型补充矩阵计数。当前M3 fixed10×10 runtime计数仍为0。
