# P3.1-R1：1×1 两层 Dense 推理双独立真实运行

> **后续校正（2026-09-15）**：本1×1运行的唯一计算core仍在Die0，原KV/时序结果保留；但native每Die4×4核与编译Fabric2×2不一致，严格M1硬件配置一致性尚需正确几何双跑。详见[P1.1-R1 核几何审计](P1.1-R1-前端原生核几何失配与矩形物理路由修正.md)。

日期：2026-09-15。作为M1八个代表形状中的1×1 Dense inference 案例，本记录冻结真实完整模型步骤的 **两次独立** compile→finalizer→ProgramIO resolver→NpuSim，不把它外推为其他family或形状。

模型 `workload_case_3d90fe5c6f8eb189`：Llama Dense、FP16、两层、V128/H32/I64/NH4/KVH2/DH8、TP1/DP1、一Die一个rank、一request，Prefill4→Decode1→Decode1。语义图定义96个操作，包括embedding、各层norm/QKV/RoPE/attention/O/residual/MLP、final norm、LM head/logits和跨步KV；生产三段编译及对应核心流均完成，原runner验收跨步KV与最后一段完成。该执行是 teacher-forced/static-trace timing workload；GEMM计算值/输出logits正确性未由functional oracle核验。

冻结源码 `75aeda068a9511bc948cfe3a155b85011fb232e7`，clean detached `/tmp/p5-phase-cost-source-freeze`，工具finalizer/resolver/NpuSim/SIM四SHA绑定，两个事先不存在且互不复用的目录 `/tmp/p5-phase-cost-1x1-fresh0` 与 `/tmp/p5-phase-cost-1x1-fresh1` 各完整物化。两轮 runner exit0、`compiled_receipt.runtime_status=verified`、源request/sequence/linked/NPUP和加载Python及工具SHA逐项相等；三段ProgramIO和原生NpuSim逐轮独立调用。三段KV字节边界均为512→640→768、one-shot drain、唯一 `[SIM_RESULT] makespan_cycles=46799`，没有把三段wall相加充当仿真makespan。全流程wall12.284/11.786s、编译3.949/3.817s、native2.896/2.666s。

本单案例满足M1 Dense inference 1×1代表形状的双独立真实运行要求；整个M1还要求四family×八形状=64次真实执行及训练两完整step，M2/M3仍须另行验收。当前 M1总完成状态 **false**。
