# P3 四类完整模型逻辑图与 Coverage Oracle 开发记录

日期：2026-09-14  
状态：前部记录各阶段当时的边界；P3.3-R1 的完整 MoE 推理声明已撤回，P3.3-R2 修复后受限 1×2/EP2 两层推理的真实 timing runtime 通过；四类 M1、MoE 训练及 M2 低 HBM offload 仍未完成。

## 1. 本切片目标与范围

本切片为 P3.1–P3.6 建立可审查的逻辑覆盖基线，先回答“一个完整负载必须包含什么”和“跨步状态如何连续”。它复用 P0 的 `WorkloadRunRequest`，但没有修改 P0 的 workload materialization 集成模块，也没有把逻辑清单冒充成可运行程序。

当前基线强制：

- 模型至少两层。
- 推理包含一次 Prefill 和至少两步 Decode。
- 训练包含至少两个完整 step，优化器为 SGD。
- Dense inference、Dense training、MoE inference、MoE training 使用同一版本化 typed graph 合同。
- graph 的 `lowering_status` 和 `runtime_status` 固定为 `NOT_MATERIALIZED`；coverage report 的 `lowering_verified` 和 `runtime_verified` 固定为 `false`。

因此，本记录表示逻辑图和独立 oracle 已完成，不表示 M1、真实 E2E 执行或模拟器支持已经完成。

## 2. 开发内容

### 2.1 Typed 逻辑图与状态版本

新增 `llm/frontend/wafer_frontend/schema/e2e_workload_graph.py`：

| 类型 | 作用 |
|---|---|
| `E2EOperationKind` | 枚举 forward、KV、routing、backward、gradient、sync、optimizer、store 和 step commit 操作 |
| `E2EWorkloadOperation` | 记录稳定语义 ID、顺序、family phase、step、layer、expert、parameter、读写 state 和依赖 |
| `E2EStateVersion` | 记录 parameter、KV、gradient 的逻辑名称、版本、producer 和全部 consumers |
| `E2EWorkloadGraph` | 绑定原始 workload request、完整 operation 序列、state lineage 及未物化状态 |

schema 校验会拒绝非连续序号、向后或未知依赖、未知 state、同一 state 多 producer、producer/consumer lineage 不闭合、跨 case 引用、陈旧稳定 ID，以及任何伪造的 lowering/runtime 完成状态。

### 2.2 四类图构建器

新增 `llm/frontend/wafer_frontend/passes/build_e2e_workload_graph.py`，入口为 `build_e2e_workload_graph()`。

四类 family 的覆盖如下：

| Family | 生成的逻辑链 |
|---|---|
| Dense inference | embedding → 每层 input norm/QKV/RoPE/KV load/attention/KV append/O/residual/MLP/residual → final norm/LM head/logits；Prefill 后连续两步 Decode |
| Dense training | 每步 parameter load → 完整 Dense forward → loss → 各层 backward → 每个可训练参数 WGRAD → gradient sync → SGD update → parameter store → step commit；连续两步 |
| MoE inference | Attention 主干加 router、冻结路由、dispatch、逐 expert forward、combine、residual 和 KV 跨步版本 |
| MoE training | 完整 MoE forward/loss，加反向 grad dispatch、逐 expert backward、dX combine、shared backward；shared/router/expert 参数分别生成 WGRAD/router gradient/expert gradient，再执行 sync/update/store |

状态连续性采用显式版本表达：

```text
Inference: KV[v] -> KV_LOAD(step=v) -> KV_APPEND(step=v) -> KV[v+1]

Training:  parameter[v] -> PARAMETER_LOAD(step=v)
           parameter[v] -> gradient producer -> GRADIENT_SYNC
                        -> SGD_UPDATE -> parameter[v+1]
                        -> PARAMETER_STORE -> next step load
```

训练中的 `STEP_COMMIT` 显式依赖该 step 的全部 parameter stores，避免只完成首个 store 就宣告 step 完成。

### 2.3 独立 Coverage Oracle

新增 `llm/frontend/wafer_frontend/passes/validate_e2e_workload_graph.py`，入口为 `validate_e2e_workload_coverage()`。

oracle 不调用 graph producer。它根据 request 独立重建：

- 每个 family、layer、step、expert 和 parameter 应存在的 operation multiset。
- 推理各层 `KV[0..decode_steps+1]` 的 producer/consumer 关系。
- 训练每个参数 `parameter[v] -> raw gradient -> synced gradient -> SGD -> parameter[v+1] -> store/load` 的 lineage。

实际清单缺操作或多操作时，先按 operation kind 给出稳定缺口，再执行 graph 自身结构校验。这样 producer 不能通过自报完成掩盖语义缺失。

## 3. 测试内容与结果

新增 `llm/test/frontend/unit/test_e2e_workload_graph.py`，共 6 个测试方法：

1. 四个 family 均生成两层、多步图，并分别通过独立 oracle；报告保持 lowering/runtime 未物化。
2. Dense inference 精确覆盖三次 embedding/head、两层三阶段 attention/MLP，以及每层 `KV[0..3]`。
3. Dense training 精确覆盖两次 loss、完整层 backward、每个参数两次 WGRAD/sync/SGD/store 和两个 step commit。
4. MoE inference 检查 router/dispatch/expert/combine；MoE training 检查反向 routing、shared backward、shared/router/expert gradient 和更新链。
5. 删除最后一层 attention 或任意 KV append，oracle 必须失败。
6. 删除 loss、WGRAD、SGD optimizer update 或 expert gradient，oracle 必须失败。

聚焦及 P0 request schema 相邻回归：

```text
PYTHONPATH=/workspace:/workspace/llm/test/frontend/unit \
python3 -B -m unittest \
  test_e2e_workload_graph \
  test_workload_run_schema -v

Ran 15 tests in 0.329s
OK
```

另执行新模块和测试的 `py_compile`、公共包导入检查及行宽检查，均通过。

## 4. 本切片没有完成的内容

以下内容仍需后续 P3 集成工作完成，当前不得据此发布 runtime/full-model 成功：

- 将 typed operations lowering 到真实 IR0/IR1/IR2、global action、schedule、manifest 和程序 records。
- 将 P1 placement/group/route 与 P2 memory allocation/residency 接到每个逻辑 operation 和 state。
- 生成真实 SEND/RECV/WAIT、KV HBM 访问、gradient collective、optimizer compute 和 parameter writeback。
- 由 finalizer、resolver 和 NpuSim 执行 Prefill→Decode→Decode 与两个训练 step。
- 真实 runtime markers、工作量/通信量/访存量/FLOPs oracle、资源排空和双独立运行证据。
- MoE 动态 token trace、零 token expert、local/remote route、router/gate score 数学梯度合同及共享/expert sync group 的真实 lowering。
- Dense mixed request、多请求不同上下文、saved activation 和 loss reduction/DP normalization 的可执行合同。

后续接入必须从 `E2EWorkloadGraph.operations` 和 `state_versions` 产生真实下游工件，并用运行证据反向核对本 oracle；不能根据 family 名称或逻辑 graph 存在就设置 `runtime_verified=true`。

## 5. P3 → materialization 集成与 happens-before 加固

`E2EWorkloadGraph` 已接入 `WorkloadMaterializationManifest` 并成为逻辑语义的单一来源。manifest 必需保存 graph 及其 canonical digest，且 graph 内嵌 request 必须与 manifest request 完全相等。P0 的粗粒度 `WorkloadLogicalOperation` 类型和 producer 已删除。materialization 每次先构建 graph，再强制运行本模块的独立 coverage oracle；oracle 拒绝时不会继续发布 preflight manifest。

独立 oracle 在 operation multiset 与 state lineage 之外新增传递依赖检查：

- Prefill logits → 第一步 Decode embedding，后续 Decode 也保持连续；
- 每层上一阶段 KV append → 下一阶段 KV load；
- 每个训练 step 的 loss → 全部 backward 操作；
- backward 按最高层到最低层逆序发生；
- 每个参数的 raw gradient → sync → SGD update → store；
- 当前 step store → 下一 step 对新参数版本的 load。

故障注入测试通过重建稳定 operation ID 和 state producer/consumer 引用，构造结构合法但缺少指定传递依赖的 graph。断开 Prefill/Decode、loss/backward、反向层序、gradient/sync 或 store/next-step-load 中任一关系，独立 oracle 都会失败。

聚焦验证：

```bash
PYTHONPATH=/workspace:/workspace/llm/test/frontend/unit python3 -B -m unittest \
  test_e2e_workload_graph test_workload_materialization test_workload_runner -v
```

结果：27 项通过。当前 graph 已实际进入 materialization，但仍未 lowering 为真实 simulator program；`lowering_status`、`runtime_status`、coverage report 和 runner 发布状态继续保持未物化/partial，不能据此声明端到端运行成功。

## 6. Typed tensor/value、KV 增长、MoE 路由与梯度同步合同

### 目标与实现

为逻辑 operation/state 补齐后续 lowering 所需的显式数据合同，同时保持 P3 仍是逻辑图，不生成 transport task、schedule 或 runtime 证据。

`E2EWorkloadGraph` 现在绑定完整 P1 `ParallelPlacement`，materialization 会把同一个 placement 实例传给 graph builder，并校验 manifest placement 与 graph placement 完全一致。新增 `E2ETensorValue` 记录 rank-local shape、复用的 `DType`、精确 bytes、state ref、P1 owner domain、logical rank、TP shard，以及 producer/consumer。operation 的 state reads/writes 会自动展开为 value input/output edges；graph 和独立 oracle 都检查引用及 lineage 闭合。

状态种类增加 activation、logits、loss、route token 和 dispatch payload。参数及 FP32 梯度值按 P1 ownership domain 覆盖全部 replica rank；普通 activation 覆盖 active ranks；每个 value 的 TP shard 必须与 rank placement 一致。KV value 按 request、layer 和 version 生成，形状使用 KV-head TP 分片，缓存长度明确为 `0 → prefill_tokens → prefill_tokens + decode_tokens`，bytes 随 Decode 单调增长。

MoE 每个 forward step/layer 生成冻结的 top-1 `token → expert` trace。dispatch 和 expert output 都有逐 expert payload state/value，payload shape 与 trace 的 expert token count 一致；零 token expert 保留零 extent、零 byte typed payload，combine value 同样进入 producer/consumer 闭合。

训练 `GRADIENT_SYNC` 复用 IR0 `ReduceOp.SUM`，直接绑定参数 owner 对应的 P1 synchronization group。`normalization_denominator` 必须等于实际 group rank 数，从而明确表达 SUM 后归一化得到 MEAN；MoE shared 参数覆盖 EP×DP replica group，expert 参数覆盖各自 expert-gradient group。

独立 oracle 不调用 producer，重新推导参数/梯度/KV/activation/logits/loss/route/payload 的 shape、dtype、rank/owner 覆盖，核对 state/value edges、冻结 trace、零 token expert、payload bytes 和 P1 group SUM-to-MEAN 归一化。故障注入改变合法 route trace 时必须被 oracle 拒绝。

### 测试

```bash
PYTHONPATH=/workspace:/workspace/llm/test/frontend/unit python3 -B -m unittest \
  test_e2e_workload_graph test_workload_materialization test_workload_runner -v
```

结果：29 项通过。新增断言覆盖 KV 各 version 的零值与单调 byte 增长、activation/logits/loss state、FP32 gradient owner、同步 group/reduction/normalization、Decode 零 token experts、零/非零 dispatch payload 和冻结 route trace 故障。

另执行新增/修改模块 `py_compile` 与 `git diff --check`，均通过。真实 lowering、transport/runtime task、数值执行和 simulator canary 仍未接入，因此 capability 与运行状态没有提升。

## 7. Logical graph 到 P1 transport 的精确绑定

P3 communication operation 现在是 materialization transport request 的唯一语义来源。QKV 对应每层、每 Prefill/Decode step 的 TP AllGather；GRADIENT_SYNC 对应每个训练 step 和参数 owner group 的 AllReduce；MoE DISPATCH/COMBINE 对应逐 expert payload 的 AllToAll。每项 request 保存 operation/value 引用和 typed value 的精确 bytes，零 token expert 保留为 no-op，因此 coverage 不会因优化掉零流量而失真。

新增独立 `validate_parallel_transport_workload_bindings` oracle，不复用 producer 的 request 集合：它从 graph 重新枚举预期通信点，验证 P1 group/owner、value rank、bytes、case/digest 和 phase/step/layer，并在存在 plan 时调用严格 send/recv 配对校验。伪造 payload bytes 的故障注入会失败。

四 family 测试覆盖 Prefill 与两个 Decode step、逐层 TP、两个训练 step 的 gradient sync、MoE dispatch/combine 和 inference 零 token no-op。联合聚焦回归 40/40 通过。该切片仍只到 transport plan，未生成 simulator program 或 runtime 成功证据。

## 与 P0 legacy Dense backend 的集成审计

统一 P3 graph 当前要求 inference 至少覆盖两层、Prefill 与 2 个 Decode step。对 production `compile_rect_mesh(AUTO)` 的实测表明，其 `naive_fixed/v1` 下游 `build_ir0` 拒绝 mixed Prefill/Decode static profile，错误为无法唯一确定 attention work。因此 P0 compatibility gate 将 P3 完整请求明确标为 `unsupported`，不会把 legacy 静态单 phase 编译结果标为 P3 graph lowering。

独立 legacy 纯 Prefill compiler canary 可以真实编译并生成 linked manifest，但 capability report 仍为 `standard_chain_complete=false`、`dense_workload_complete=false`。该结果只记录为 `legacy_static_profile/motif`，`full_model_ready=false`。P3 后续接 backend 的前置条件是 production lowerer 直接消费并覆盖 `E2EWorkloadGraph` 的全部 operation/value/route，而不是按相近 shape 复用 legacy profile。

## P3.1：Dense E2E compile-only 多 profile sequence

### 目标

解除 P3 Dense inference 完整图无法交给 legacy mixed-profile compiler 的第一个阻断。把统一 graph 的 `Prefill → Decode-1 → Decode-2` 拆为三个纯静态 profile，分别复用真实 production `compile_rect_mesh`，并用一个版本化 sequence artifact 证明 segment、graph operation 与 KV 状态连续性。此阶段不拼接三个 linked program，也不发布 runtime 或完整 E2E 支持。

### 实现

新增 `schema/dense_compile_sequence.py`，定义 `wafer_frontend.dense_compile_sequence/v1alpha1`：

- `DenseCompileSequence` 嵌入原始 `WorkloadMaterializationManifest`，绑定 materialization 和 logical graph digest；
- 固定三个 `DenseCompileSegment`，timeline 必须为 `(prefill, step=0) → (decode, step=1) → (decode, step=2)`；
- 每个 segment 嵌入派生后的 legacy `ExperimentSpec`、当前 step 的全部 P3 operation refs、全部 layer binding、production compiler capability report、非空 `LinkedProgramManifest` 及其内容 digest；
- 每个 layer/rank 建立 `DenseKvSegmentBinding`，显式记录 input/output KV version、P3 state ref、HBM address 和 reserved bytes；
- KV 地址从 materialization memory plan 中每个 rank 的 resident KV allocation 按 layer 稳定切片。相邻 segment 必须复用同一 layer/rank 地址与容量，且前一段 output state/version 必须精确等于后一段 input；
- `one_shot_workload_end` 只允许最后一个 Decode segment 为 true；
- compile 状态固定为 `independent_linked_programs`，runtime 状态固定为 `runtime_not_materialized`。

新增 `passes/dense_compile_sequence.py`。production pass 只接受 Dense inference、resident HBM、baseline timing-only、至少两层、恰好两个 decode step、DP/EP/PP=1、TP 覆盖完整 row-major RectMesh、单 legacy instance、model 精确一致且每 Die 有 HBM address space 的请求。它派生：

1. Prefill profile：统一请求的全部 prefill token；
2. Decode-1 profile：每个 request 一个 query token，context 为 prefill+1；
3. Decode-2 profile：每个 request 一个 query token，context 为 prefill+2。

三个 profile 分别调用真实 `compile_rect_mesh(AUTO)`。每次编译必须得到一个非空 linked program，并保持 `naive_fixed/v1`、`executable_baseline=true`、`standard_chain_complete=false`、`dense_workload_complete=false`。任何 capability 漂移都会拒绝 sequence。

### 真实 compiler canary

当前 production profile MVP 还有两个组合约束：Prefill 必须 `num_seqs=1`；TP>1 又要求 sequence parallel，而纯 Decode token 数必须能被 TP 整除。因此 TP2、单 request 的统一 P3 case 在编译前明确拒绝，理由为 `request.decode_tokens_must_be_divisible_by_tp`；增加 request count 又会与 Prefill 单序列约束冲突。

首个合法 canary 因此使用 1×1 RectMesh、TP1、两层、Prefill=4、Decode steps=2、request count=1。三段 production 编译全部通过：

- Prefill linked manifest：44 fragments；
- Decode-1 linked manifest：48 fragments；
- Decode-2 linked manifest：48 fragments；
- 三个 linked manifest digest 各不相同；
- sequence END 标记为 `false / false / true`；
- 最终状态仍为 `runtime_not_materialized`。

### 测试

新增 `test_dense_compile_sequence.py`，`setUpClass` 只执行一次三段真实 production 编译，其他测试复用该 sequence，避免重复成功段。覆盖：

- 至少两层、三 segment、每段非空 linked program 与 exact operation/layer binding；
- 全 layer/rank KV version `0→1→2→3`、state ref 和 HBM address continuity；
- sequence 严格 JSON 序列化往返和稳定 digest；
- 旧 schema version 拒绝；
- Decode segment 重置到旧 KV state ref 拒绝；
- 中间 segment 设置 one-shot END 拒绝；
- linked manifest digest 漂移拒绝；
- TP2 单 request 非法组合在调用 compiler 前拒绝。

命令：

```bash
PYTHONPATH=. python3 -m unittest llm.test.frontend.unit.test_dense_compile_sequence -v
```

结果：7 项通过，用时 35.045 秒。新增 schema/pass/test 的 `py_compile` 与 `git diff --check` 另行执行。

### 限制与下一步

每个 legacy linked manifest 都是独立 program，拥有自己的本地 control envelope。sequence artifact 只定义统一 workload timeline 和最终 one-shot END 语义，尚未重写或链接中间 program 的 control envelope，也没有把上一段 KV 实际 simulator memory 映射为下一段 ProgramIO 输入。因此三个 compile 结果不能直接作为连续 runtime 执行，且不能成为 full-model runtime evidence。

下一步需要 production sequence linker：删除或重写中间 local END，统一 runtime symbol/address namespace，把 `DenseKvSegmentBinding` 转为跨段 persistent ProgramIO/state ABI，并对最终组合 artifact 运行 finalizer、resolver 与两次独立 simulator canary。在这些检查完成前必须保持 `runtime_not_materialized`。

## P3.2：Dense training 两步 compile/lower 基础

### 复用路径审计

现有通用 `train_n6` 只覆盖 forward train；`lite_train` 的真实 backward/SGD 链只覆盖 LM-head motif，不能代表 P3 的全部 17 个两层 Dense 参数。可复用的完整参数路径是：

```text
flexible_dense_train plan
  → production train forward lower/link
  → flexible_dense_backward IR/projection/schedule/global DAG
  → flexible_dense_backward[_multi] lower/link
  → LinkedProgramManifest
```

该 backward linker 对每个 legacy parameter 已生成真实非空 records：HBM parameter load、WGRAD MATMUL、DP>1 tree DTE sync/local reduce、SGD_UPDATE 和 HBM store。其 carrier 的 `runtime_verified` 固定为 false；旧 `FlexibleDenseTrainPlan.backward_lower_link_status=out_of_scope` 仍是早期 plan 能力字段，新的 linked carrier 在旁路中补齐了实际 lower/link，但尚未形成 P3 原生端到端 runtime。

### Versioned sequence 与参数绑定

新增：

- `schema/dense_training_compile_sequence.py`
- `passes/dense_training_compile_sequence.py`
- `test/frontend/unit/test_dense_training_compile_sequence.py`

`DenseTrainingCompileSequence` 嵌入 P3 materialization 和两个 training segment。production pass 只接受两层及以上、恰好两个 step、SGD、resident HBM、baseline timing-only、Dense、PP/EP=1、全 row-major active mesh，并要求 P3 TP=mesh columns、DP=mesh rows；legacy model、batch/sequence、parallel、fabric 和 HBM spaces 必须精确匹配。

pass 先通过现有 production 路径生成一个真实的一步 `FlexibleDenseBackwardLinkedProgram`，再把这个稳定 compiled template 绑定到 P3 step 0/1。每个 step 的 17 个 P3 trainable parameter 都显式记录：

- PARAMETER_LOAD、WGRAD、GRADIENT_SYNC、SGD_UPDATE、PARAMETER_STORE operation refs；
- input/raw-gradient/synced-gradient/output state refs；
- 参数版本 `0→1`、`1→2`，以及 step 0 output 等于 step 1 input；
- 每个 TP shard 的 P3 owner ranks、精确 logical bytes；
- legacy parameter/state、load/WGRAD/sync/SGD/store action refs；
- linked manifest StateABI、HBM address 和 byte slice。

P3 的 MLP gate/up 是两个参数，而 legacy 使用融合 `w_gate_up`。绑定将二者限制为同一 legacy buffer 的两个等长、不重叠半区。TP2 下 legacy 对 embedding/norm/LM-head 使用完整 carrier，P3 使用 TP shard；绑定显式记录 `tp_shard × logical_bytes` 的精确 slice。其他参数必须完整覆盖一个 legacy shard。DP1 的逻辑 sync 明确降为 singleton no-op；DP2 绑定真实 reduce/broadcast tree action 与 DTE records。

sequence 状态为 `reused_real_one_step_linked_program`，runtime 固定为 `runtime_not_materialized`。只有最后一个 P3 step 具有 one-shot workload end 语义。

### 真实 linked canary 与测试

1×1/TP1/DP1：两个 step 各 17 个参数绑定；真实 linked manifest 含 1 fragment、202 records。

2×2/TP2/DP2：两个 step 各 17 个参数、每参数 2 个 TP shard；真实 linked manifest 含 1 fragment、1082 records，P3 sync 绑定 136 个 legacy DP tree action refs。

聚焦命令：

```bash
PYTHONPATH=/workspace:/workspace/llm/test/frontend/unit \
python3 -B -m unittest test_dense_training_compile_sequence -v
```

结果：7/7 通过，用时 88.772 秒。测试覆盖 1×1 非空 linked program、两步完整参数与版本连续性、gate/up 融合切片、2×2 TP/DP tree，以及漏 WGRAD、漏 SGD update、旧参数版本和 runtime 状态误提升失败。

相邻 P3 graph 与 flexible Dense backward 代表回归 11/11 通过，用时 35.260 秒。

### 当前限制

两个 P3 step 当前复用同一个真实的一步 linked program 模板；尚未把两个 step 合并为一个 control envelope，也未证明第一个 program 的 HBM store 会在同一 simulator 时间线成为第二步 load 的实际输入。legacy fused/replicated carrier 的 byte slice 是严格 compile binding，但现有 records 仍按 legacy 完整 carrier 执行。因此本切片证明真实非空 lower/link 与 P3 全参数/版本合同已闭合，不构成两步 runtime 或数值训练成功证据。

下一步需要 P3-native sequence linker/ProgramIO：将 step version 映射为跨段 persistent StateABI，处理中间 END、runtime symbol/session epoch，并运行 finalizer、resolver、两次连续 NpuSim canary。在此之前 capability 不得提升。

## P3.1-R：Dense Prefill→Decode→Decode 同实例 runtime sequence

### 目标与能力边界

把 P3.1 的三个真实 production linked program 放到同一个 NpuSim/SystemC 实例、同一套 WorkerCore、HBM backing 和时间线上连续执行。段边界只暂停供 host 校验和装载下一段，前两段不得停止 workload；第三段才执行一次最终 drain。本切片严格限定为 1×1 Mesh、TP1、两层 Dense inference、resident-only、baseline timing ProgramIO，不改变 `DenseCompileSequence.runtime_status=runtime_not_materialized`，也不把该 canary 提升为任意 Mesh、数值功能或 full-model readiness。

### Persistent KV StateABI

新增版本化 `PersistentStateReservationPolicy`。placement 对 KV binding 仍记录该段真实 tensor bytes，但分配游标按最终 Decode 的保留槽前进；默认无 policy 时保持原有地址与 digest 行为。三段 compiler 透传同一策略，因此每层 KV 在 Prefill、Decode-1、Decode-2 中地址稳定，真实 extent 分别增长，且不会与下一层状态重叠。sequence validation 还要求每段恰好覆盖全部物理 KV StateABI，逐 layer/rank 的 kind、die、address 不变，size 与本段 P3 KV version 精确一致。

### 同实例 sequence runtime

新增 `config_helper_program_sequence`，顺序加载三个 finalized Program artifact。它要求每段 ACK/DONE core 集一致；每个中间完整 DONE 集只生成 segment marker、加载下一段并 `sc_pause()`，最终完整 DONE 集才生成 one-shot drain marker并 `sc_stop()`。`MemInterface` 在 sequence 的非最终 DONE 边界把 DATAFLOW phase 从上段结束的 `PRO_START` 精确恢复为 `PRO_CONF`，确保下一段严格经过 CONFIG→WEIGHT DATA→START；该分支不作用于普通单 Program、不完整 DONE 集或最终 DONE。

ProgramIO 新增 `ApplyBeforeSequenceSegment`：首段正常初始化；后续段在 `SC_PAUSED` 边界刷新 SRAM，但保留已有 HBM byte，只对尚未出现的 KV tail subrange 做归一化、事务式补零。SRAM/HBM debug boundary API 允许 elaboration 或 paused，仍拒绝 active simulation。每段恢复运行前由同一 `Bindings` 复用同一个 `HbmRuntime`，每段结束后先执行 typed output probe，再读取该段全部真实 KV StateABI byte 并输出聚合 SHA-256。

`npusim` 增加成对的三段参数：`--program-sequence`、`--linked-manifest-sequence`、`--program-io-sequence`。preflight 对每段执行 artifact decode、finalizer 重建闭合、ProgramIO resolve，并验证两层四个物理 KV ABI 的地址连续和 extent 严格增长。运行结束后的统一 Program memory 报告从 sequence helper 读取最终 decoded artifact；同一进程只输出一个 `SIM_RESULT`。

### 真实 production canary

新增 `run_dense_sequence_runtime_canary.py`，调用 P3.1 production compiler 生成三段 profile，逐段调用真实 `npusim_program_finalizer`，基于 retained `LinkedProgramProfile` 生成并 resolve typed ProgramIO sidecar，最后在一个 `npusim` 进程中连续运行。产物保存在 `build-debug-final/dense-sequence-runtime-canary/`，包括三份 linked manifest、finalized program、finalizer report、ProgramIO sidecar 和完整 simulator stdout。

完整 canary 退出码为 0，稳定闭合结果为：

- sequence digest：`5674f80c87d75a96e1869da261a684f092d1bc6ea003c5612c68497be6ecff81`；
- 三段 artifact SHA-256：`6e7943a0edc8c0b87ea5f1e9eb804a232caadfa95e3c80d95733cdb6278d1d94`、`098425166f47e700170033e6fd622dcb2de1dfcdfffb02474b1cd4ba97dd9188`、`199b155f131db2f89e85aaeddc8e55db8b54819e20b323f5743df915fd09ac7f`；
- segment markers：`(0, final=0) → (1, final=0) → (2, final=1)`；
- KV bytes：`512 → 640 → 768`，对应 digest 为 `076a27c79e5ace2a3d47f9dd2e83e4ff6ea8872b3c2218f66c92b89b55f36560`、`9e132485d5107211de325a45e7917cbe3e4b5b9cde3e4ee91d7d2102317759ee`、`ef115a0e0c15cdc41958ca46b5b14b456115f4baec5e3ca68599d2a8f435e3b8`；
- START_DATA 三段均完成：enqueued/injected/accepted/delivered/consumed/completed 全为 3；
- 最终只出现一个 `DENSE_SEQUENCE_DRAIN segments=3 one_shot=1` 和一个 `SIM_RESULT makespan_cycles=46787`；
- Program memory：LSU issued/completed=`65/65`、HBM write bytes=`768`、LSU/DTE residual=`0/0`；router、D2D、collective、credit 和 start-data drain 均闭合。

### 测试

```bash
python3 -m llm.test.frontend.integration.run_dense_sequence_runtime_canary --timeout 600
```

结果：PASS，完成三次真实 compile、finalize、resolve 和同实例 simulator sequence。

```bash
PYTHONPATH=. python3 -m unittest \
  llm.test.frontend.unit.test_dense_compile_sequence -v
```

结果：7/7 PASS，用时 34.615 秒。覆盖三段 compiler canary、KV version/address continuity、serde、旧 schema、中间 END、旧 KV 和 digest 漂移失败。

```bash
./build-debug-final/npusim_program_io_selftest
```

结果：8/8 PASS，包含 paused boundary 下已有 HBM 保留、缺失 tail 初始化和 SRAM 刷新。

```bash
PYTHONPATH=/workspace:/workspace/llm/test/frontend/unit \
python3 -m unittest test_placement_context_schema -v
```

结果：6/6 PASS，包含 reservation policy 严格校验、稳定 ID 和 JSON 往返。`npusim` target 重建通过。

### 限制与下一步

当前证据证明的是 production compiler/finalizer/resolver 产物在同一 timing simulator 实例上的两层 Dense、1×1/TP1 三段连续执行，以及 KV 物理地址和 byte presence 连续。ProgramIO probe 仍是 timing-only，KV 内容由 behavioral execution/缺失 tail 初始化形成，尚无参考框架的逐值数值比对；只运行一次 canary，也未达到 capability builder 要求的两次独立执行证据。因此 capability 必须继续 fail closed。下一步应生成独立、版本化 runtime evidence carrier，执行两次隔离 canary并核对 artifact/marker/KV digest，然后扩到 TP>1/多 Die 和真实 functional oracle。

## P3.2-R：Dense training 两步同实例 timing runtime

### 生产接口适配

`build_timing_program_io` 现在接受 P3.2 复用的
`FlexibleDenseBackwardLinkedProgram`。它不引入新的训练 IR，而是从 linked
manifest 的 `address_operand_bindings` 恢复 SRAM buffer 的精确读写方向，从
`state_operand_bindings` 恢复 trainable StateABI 的 HBM load/store 方向。1×1
carrier 生成 15 个非零 deterministic parameter seeds、45 个 initialization 和
15 个 HBM output probe。

`npusim --program-sequence` 从固定三段改为至少两段，并要求 program、manifest、
ProgramIO 三个路径列表数量严格相等。preflight 自动区分 KV sequence 与 trainable
parameter sequence：KV 集允许任意非空 die/ABI 数量，保持 kind/die/address 并要求
extent 逐段增长；training 限定两个 segment，要求 StateABI 的 kind/die/address/size
完全连续。该修改同时解除 Dense inference 2×2 的 16 个 KV ABI 硬编码阻断。

训练 sequence 在首段前读取同一 HBM backing 的 version 0，在每个完整 DONE 边界读取
version 1/2，并输出聚合 SHA-256。每段还输出 manifest-bound MATMUL、SGD_UPDATE、
LSU_STORE record 数；完整 DONE 证明这些顺序 records 已执行。第一步仍走
`sc_pause()`，第二步才输出一次 `DENSE_SEQUENCE_DRAIN` 并 `sc_stop()`。

### 1×1 真实 canary

新增 `run_dense_training_sequence_runtime_canary.py`，直接使用 P3.2 的两个真实 linked
segment，逐段运行 production finalizer 和 ProgramIO resolver，然后在同一个
NpuSim/SystemC 实例、同一套 WorkerCore 和 HBM backing 中执行。

结果：PASS。

- sequence digest：`11cc62f59a1f86471589b27395a3f9f0e3b76332e0b390c4e7d6c4640570b881`；
- 两段 artifact SHA-256 均为
  `aa040002c0de9a17742c80cd922cc0f584f9f6bb437985e237db3eaad41c1c79`；
- segment marker：`(0, final=0) → (1, final=1)`，最终 drain 恰好一次；
- parameter StateABI：15 个 legacy carrier，共 808B，version `0→1→2` 的地址和
  byte presence 连续；
- 两步各有 15 个 P3 WGRAD action 到 MATMUL record 的精确静态绑定；runtime 每步
  完成 41 个 MATMUL records、15 个 `TRAIN_SGD` invocation 和 15 个 HBM store；
- ProgramIO 每步 15/15 HBM probes 通过；
- LSU issued/completed=`60/60`，HBM read/write=`1616/1616B`，LSU/DTE
  residual=`0/0`；
- `SIM_RESULT makespan_cycles=6715`。

三个 HBM 内容 digest 均为
`3fd87ddbb5afc16fe55cef4b340fc0952f4b2431a614a90428858c78eb58cd85`。
这是预期的 timing-only 行为：当前 MATMUL/SGD primitive 计算工作量并发出 runtime
marker，但不实现数值 buffer mutation；store 因此把同一 seed 写回。证据只证明物理
HBM backing、load/WGRAD/SGD/store 执行顺序和版本边界连续，明确携带
`functional=0`，不声称参数数值发生更新。

### 测试

```bash
PYTHONPATH=/workspace:/workspace/llm/test/frontend/unit \
python3 -B -m unittest test_dense_training_compile_sequence -v
```

结果：8/8 PASS，用时 93.536 秒；新增 Flexible backward ProgramIO 的 15-state、
45-initialization、15-probe 闭合。

```bash
PYTHONPATH=/workspace python3 -B -m unittest \
  llm.test.frontend.integration.test_run_dense_training_sequence_runtime_canary -v
```

结果：4/4 PASS；覆盖完整两步 marker、缺 version 1、第二步旧 output version 和
functional 误提升失败。

相邻 `test_program_io_state_pass` 为 6/6 PASS；`npusim` target 重建通过。真实 canary
退出码为 0，产物仅写入 `/tmp/dense-training-sequence-runtime-canary`，未纳入源码提交。

### 后续边界

本切片尚未提供 SGD 的数值执行器或参考框架逐值 oracle，因此 compile sequence 的
`runtime_status` 继续保持 `runtime_not_materialized`，workload functional capability
不得提升。2×2/TP2/DP2 的 compile 与 ProgramIO carrier 已存在，但真实 collective
sequence canary 留待独立运行；若 collective session 或跨段 reset 阻断，应保留精确
runtime 失败证据，不得以 1×1 结果外推。

## P3.3/P3.4-C：MoE 多层、多步 production compile sequence

### 复用路径与范围

新增 `schema/moe_compile_sequence.py` 与
`passes/moe_compile_sequence.py`，复用现有
`compile_flexible_moe_baseline → lower_link_flexible_moe_production`
production 路径。没有复制 lite-MoE 的冻结 2/4 Die 图，也没有用占位 stage 代替
linked program。

首个 fail-closed 子集限定为：

- FP16、静态 top-1、resident HBM、baseline timing-only；
- TP=DP=PP=1，EP=expert 数=全部 row-major Mesh Die；
- 至少两层；
- inference 为 Prefill + 两个 Decode step；
- training 为两个 SGD step；
- 每个 `(step, layer)` 生成一个独立真实 Flexible-MoE
  `spec → executable plan → LinkedProgramManifest`。

sequence 明确标记 `coverage=moe_blocks_only`、
`compile_status=independent_production_linked_blocks` 和
`runtime_status=runtime_not_materialized`。这些单元是 production
MoE block 的 compile 结果，不表示 Attention、共享 backbone、head 已一起 lower，
也不是可以按单元顺序直接连续执行的完整模型 runtime。

### 路由和 operation 闭合

每个 compile unit 绑定唯一 P3 `E2ERouteTrace` 和 digest，并按显式
`token_index_mod_ep` source-rank policy 构造 Flexible-MoE trace。validator
重新核对：

- token、expert、slot、expert histogram 与冻结 P3 trace 精确一致；
- router → route-freeze → dispatch → 每个 expert forward → combine 的
  operation kind、step、layer、expert 与顺序；
- route、dispatch payload、expert output、combine value refs 的 producer/consumer
  闭合；
- production spec 的 Mesh、hidden/intermediate、expert/EP/top-k 与 P3 request
  一致；
- plan/spec/linked manifest lineage，以及 manifest 中 plan digest 闭合；
- linked manifest 非空、lower/link 已验证，而 runtime flag 必须为 false。

当前 P3 冻结路由使用 `expert=token_index % expert_count`；与上述来源策略组合后，
代表 inference trace 是全本地 expert route。此 compile canary 因而不声称验证了
inference remote dispatch。已有 Flexible-MoE production 回归继续独立覆盖多矩形
remote DTE，training 单元中的 gate gradient AllReduce 也生成真实 DTE records。
后续若要让端到端 inference trace 覆盖 local/remote/偏斜路由，需要先把 token
source rank 加入 P3 route trace，而不是在 lowering 中暗自改变 frozen assignment。

### 两步训练状态闭合

每个 training unit 绑定一份 replicated router parameter 和每 expert 的
gate/up/down 三参数组合。production 的 gate parameter replica 精确对应 P3 router
rank-local tensor；production expert fused state 的 bytes 精确等于 P3
gate+up+down 三 tensor bytes 之和。

对每个参数都绑定 P3 raw-gradient、gradient-sync、SGD-update、parameter-store
operation 和 input/output state。validator 要求：

- step 0 为版本 `0→1`，step 1 为 `1→2`；
- 同一 layer/parameter 的 step 0 output state 等于 step 1 input state；
- router sync denominator 等于 EP，expert sync denominator 为 singleton 1；
- router 映射 production gate WGRAD、tree AllReduce、SGD 和 store；
- expert 映射 production fused expert WGRAD、SGD 和 store，P3 singleton sync
  保持 no-op，不伪造 production collective。

### 测试

```bash
PYTHONPATH=. python3 -B -m unittest \
  llm.test.frontend.unit.test_moe_compile_sequence \
  llm.test.frontend.integration.test_run_moe_compile_sequence_canary -v
```

结果：7/7 PASS，用时 6.740 秒。1×2/EP2 tiny model 生成：

- MoE inference：6 个 unit，204 个 linked records；
- MoE training：4 个 unit，304 个 linked records；
- 合计 10 个 unit、508 个 linked records；独立重复 compile 完全一致。

失败测试覆盖 route trace 替换、旧参数版本、TP 路径误接入和 runtime 状态误提升。

相邻回归：

```bash
PYTHONPATH=. python3 -B -m unittest \
  llm.test.frontend.unit.test_flexible_moe_production \
  llm.test.frontend.unit.test_e2e_workload_graph -v
```

结果：23/23 PASS，用时 9.528 秒，覆盖 production Flexible-MoE
1×1/多矩形 lower-link、remote DTE/event closure 与四 family P3 graph。

### 后续边界

当前 production Flexible-MoE carrier 仍是一 expert/Die、TP1/DP1 的独立 MoE
block。尚未支持多个 experts/rank、P3 active subset、TP/DP、完整 Attention/shared
参数 lowering，也未把多 block 合成同一 simulator control envelope。训练只证明
compile-time 状态和 action 映射，尚无同 HBM backing 的两步 runtime 或数值更新
证据。因此本节不能提升任一 MoE full-model runtime capability。

## P3.1-R2：2×2 全参与 Dense inference 编译与 runtime 阻断审计

### 目标与能力边界

本切片把 P3.1-R 的 1×1/TP1 两层 Prefill→Decode→Decode 推进到物理
2×2 Mesh、四个 active Die、TP4 和四个 request。production legacy lowerer 仍要求
`TP == mesh rank_count`、DP/EP/PP 均为 1、active Die 为完整 row-major Mesh；2×2
上的 TP2 active subset 继续明确返回
`full_row_major_mesh_required`/`tp_must_cover_mesh`，没有被误报为可执行能力。

### 等长多 request Prefill

IR0 构建、logical expand、IR0 schema 与独立 validator oracle 现在接受等长纯
Prefill，严格要求：

```text
context_sum == prefill_tokens == num_seqs * context_max
query_key_pairs == num_seqs * context_max * (context_max + 1) / 2
```

ragged Prefill、混合 profile 和不满足该恒等式的输入继续 fail closed。该语义同步进入
Python artifact validator 与 C++ `ATTENTION_EXACT` record codec；wire ABI 不新增字段，
request 数从 `query_tokens / context_max` 唯一派生，并要求整除。TRAIN_FORWARD 同步使用
相同的等长 batch 恒等式，避免继续把 aggregate token 数误当成单序列长度。

multi-die linked program 会把 ISA region 包装为 `RegionManifest`。Dense sequence 与
runtime canary 的 StateABI 收集现在按公共 artifact schema 的既有方式解包 leaf
`CommandFragment`，仍逐项校验全部 KV kind/die/address/extent。runtime hardware 的
HBM home range 直接绑定 compiler 使用的 `HbmAddressSpace`，避免 2×2 时把每 Die
1 GiB 全局地址误配到 1 MiB fixture range。

### 2×2 production compile/finalize 证据

四 request、两层、TP4/SP、四个 row-major rank 的三段 production compile、真实
finalizer 与 typed ProgramIO 均完成。三段结果为：

| segment | linked records | cores | artifact SHA-256 | linked manifest digest |
|---|---:|---:|---|---|
| Prefill | 1452 | 4 | `76bfae3bdf5731478608a745ea15ff18b9b32213748733b4660ef184c96ccce7` | `f367300d8e4684f8c08472fb71e57b900d702aaa0537643b2f5e5ee8730f532d` |
| Decode-1 | 1468 | 4 | `aa93d129f01031c8e3d95fda8ebacf527f6f082cc6d4eacbd0a1df60e280a565` | `d7b218e246798d5293785466d94ce8f1320c9703a1cb50ef7b18d9561b121079` |
| Decode-2 | 1468 | 4 | `d0943db56ec0f25971a9f05db0e61d4c676ae00e30feee2c0eeda005f518fd8f` | `17e259920092a895c0f572122c0cc8091d9ef78bfe1422fbf917c0a2fbd3ccae` |

ProgramIO 的 initialization/probe/blob 数分别为 `432/4/9`、`448/4/11`、
`448/4/11`。这些产物只写入 `/tmp/dense-sequence-runtime-canary-2x2`，没有纳入源码。

### 真实 runtime 结果与当前阻断

`npusim --program-sequence` 已用上述三个 finalized artifact、四个 HBM stack 和同一
SystemC 实例启动。修正 home range 后，首段真实执行到 29427 ns，随后 core 12
报告：

```text
P2P endpoint receive session capacity exhausted
```

production endpoint 每 core 固定 `MAX_BUFFER_PACKET_SIZE=3` 个 session。TP4 linked
stream 每个通信组已经产生三个 peer receive；后续通信组开始时，旧 session 尚未全部
退休或没有经过安全 admission，第四个 receive 因此被 runtime 正确拒绝。本切片没有
提高全局常量来掩盖调度/生命周期错误，也没有把 runtime 启动证据记为三段成功。
后续必须在 production lowering/scheduler 中加入 endpoint session capacity-aware
admission，或修复同步 P2P session 的退休点，并以同一 2×2 canary 闭合三段 marker、
KV `4096→5120→6144` bytes 和 one-shot drain。在此之前 2×2 Dense runtime 与 active
subset capability 均不得提升。

### 测试与检查

- multi-request IR0/logical expand 聚焦回归：18/18 PASS；
- Python ATTENTION_EXACT 多 request 正向、ragged/non-integral 失败 case：PASS；
- `npusim --isa-v1-selftest`：PASS，其中 record codec 1211 checks；
- 2×2 active subset 独立 fail-closed case：PASS；
- `npusim` target 增量 build、Python `py_compile`、`git diff --check`：PASS；
- 2×2 完整 canary：compile/finalize/ProgramIO 与 runtime 启动 PASS，runtime 在上述
  P2P session capacity gate 处 fail closed。

## P3.3-C2：MoE 完整推理模型的类型化组合编译

### 目标与实现

新增 `schema/moe_full_model_compile_sequence.py` 与
`passes/moe_full_model_compile_sequence.py`，在 1×2/EP2 上构造两层
Prefill→Decode→Decode 的完整 MoE 推理模型编译证据。每个 profile 真实编译 Dense
shared-spine linked 模板，将每层 Dense MLP 的 gate/up、SwiGLU 和 down region 精确
排除，再插入对应层的 production Flexible-MoE linked unit。

组合 schema 对每一步和全序列执行有序 operation 闭包校验，要求 embedding、各层
norm/QKV/RoPE/KV load/attention/KV append/attention out/residual、MoE block、final
norm、LM head 与 logits 全部存在。每个 operation 必须绑定到真实 IR1 node、KV
StateAccess、logits value、MoE action或冻结 route trace；未知 lineage、错误绑定和操作
遗漏均 fail closed。

当前 linker 没有 region replacement，也不能把 shared spine 和 MoE block 组成单一
可运行 manifest。因此产物明确标记为 full-model composite、compile-only，runtime
状态保持 `runtime_not_materialized`，没有把多个独立 linked witness 误报为端到端执行。

### 测试结果

新增测试 6/6 PASS，用时 56.948 秒，覆盖 canonical round-trip、完整 operation 闭包、
Dense MLP 精确替换、语义错误的 node/action 绑定、未知 lineage、operation 遗漏和
runtime overclaim。原 MoE sequence 与新 full-model 测试组合回归 10/10 PASS，用时
41.581 秒。

对应源码提交为 `12f6e9bb feat(frontend): compile full MoE inference sequences`。

### 能力边界

本切片只证明 1×2/EP2、两层、Prefill 加两个 Decode step 的完整 MoE 推理模型能够
形成可重建、可严格校验的 production compile composite。MoE runtime 仍未物化；
MoE 训练仍缺 shared 参数 load、loss/backward、梯度同步、SGD/store 与 EP2 expert
版本闭包。后续需要 linker region replacement 或等价的组合执行合同，才能将该编译
证据升级为同一 simulator 实例中的完整模型 runtime。

## P3.1-R2：2×2/TP4 Dense runtime session admission 闭合

日期：2026-09-15

P3.3-C1 记录的 capacity 阻断已关闭。后续逐 linked token 与 runtime 生命周期审计
修正了当时对根因的初步判断：fused async receive 均紧跟同 token WAIT，峰值只有 1；
占满三槽的是已完成同步 TX 指令、仍等待 completion ACK 的 transport session。旧
Worker 把这种暂态满容量当成结构错误立即拒绝下一个 receive。

runtime 现通过 `READY / WAIT_FOR_TRANSPORT / REQUIRES_LOCAL_RETIRE` 三态区分可由
独立 ACK 进展释放的背压与必须执行未来 WAIT 才能释放的结构性超额。前者等待
`ev_p2p_progress` 后重试，后者仍 fail fast；`MAX_BUFFER_PACKET_SIZE` 保持 3。

复用 P3.3-C1 完全相同的 finalized 产物，2×2/TP4、四 request、两层
Prefill→Decode→Decode 已在同一 NpuSim 实例完整通过：三段 marker 顺序闭合，KV
bytes 为 `4096→5120→6144`，one-shot drain 为 1，core 0/4/8/12 P2P residual 与
shared timing residual 均为 0，只输出一个 `SIM_RESULT makespan_cycles=88111`。
1×1/TP1 对照仍为 46,787 cycles，KV `512→640→768`，同样只输出一个
`SIM_RESULT`。该证据只提升这两个已实测 Dense case，不外推任意 mesh。

## P3.3-R1：1×2/EP2 两层 MoE 推理单时间线执行（计算量验收暂撤回）

日期：2026-09-15。此前 P3.3-C2 的 `compile-only` 是当时真实边界；本切片
新增可执行合成 manifest 与独立 canary，不把原 compile-only 记录改写成当时
已运行。`MoeFullModelCompileSequence` 每个 Prefill/Decode 段现在绑定真实
executable manifest/digest，compile 状态是 `full_model_runtime_linked`，
runtime 状态是 `executable_manifest_materialized`；编译结果自身不宣称
runtime 已通过，运行证据独立于编译状态。

### 数据流、生命周期与物理空间

新增 `moe_full_model_linker.py`：在一条 linked timeline 中保留 Dense 的
embedding、attention/KV、norm、residual、final head/logits，精确移除每层
原 Dense gate/up、SwiGLU、down MLP records，按层克隆 production MoE
state/compute fragments、runtime symbols、relocations、BufferABI 和 HBM
StateABI，并插入 `norm2_out → MoE.activation` 与
`MoE.output → down_out` 的真实 `DTE_ISSUE` SRAM copies（两层四条）。
复用原 Dense 的 `norm2_out` FREE、`down_out` ALLOC 生命周期，而非只补一条
数据桥。Dense 根区 `[0,4096)`，MoE input `[4096,40960)`、comm
`[40960,77824)` 均受 hardware block allocator 与 131072 B SRAM
capacity 约束；层间采用 8192 B 有界地址隔离，全部物理 ALLOC literal、
address relocation 和 BufferABI 随层移动。逻辑路由/work 与模型层数未减小；
每层各 rank 的 gate/expert weight 和 retention 等 P2 StateABI 在 HBM
继续真实 load/store，层间命名与 HBM backing 分离。

生产 multi-die MoE lowerer 的严格模型数据流由显式
`full_model_dataflow` 开关开启，仅用于 `rank0_shared_spine` 完整模型：
DISPATCH RECV 写 expert rank activation，EXPERT_FORWARD 的 MATMUL
读取同一 activation 并写 expert output，COMBINE SEND 读取同一 output，
COMBINE RECV 写 origin output，WEIGHTED_COMBINE 从 origin output 读取，
最终 bridge 将 origin output 返回 Dense residual。默认旧 MoE production
lowering 无此开关，2×3 和 1×2 原 canonical manifests/digests 均未变化。
独立结构 oracle 从 P2 flow/action 和 linked physical address bindings 重新
检查每一 flow 的 source/target buffer、`length_bytes == logical_bytes`、
expert MATMUL 输入/输出和各层四个 Dense↔MoE 端点；删去任一 bridge、
正 work GATE 或最后一层动作会失败。

模型声明 gate FP16 weight 是 `H=4 × E=2 × 2B = 16B`；旧固定
`(1,32,1,16)` 需读 32B，超出已合法分配的 weight 16B。严格路径按
每 source `m=len(assignment_refs)` 和 P2 gate FLOPs
`2 × m × H × E` 发 `(1,m,H,E)`，`m=0` 且 P2 flops/bytes 为 0
时仅保留 typed gate/pack/combine action 与依赖，不物化虚构 compute
records；非空 expert MATMUL 的模型计算未缩小。EXU/SFU/VEC 成本模型
对正工作至少计一次硬件 cycle，零工作仍为 0，selftest 覆盖 tiny、zero
和既有 large 案例。

### 真实 canary 与验收边界

命令：

```bash
python3 -m llm.test.frontend.integration.run_moe_full_model_sequence_runtime_canary --timeout 300
python3 -m unittest llm.test.frontend.unit.test_moe_full_model_compile_sequence \
  llm.test.frontend.unit.test_moe_compile_sequence \
  llm.test.frontend.unit.test_flexible_moe_production
./build-debug-final/npusim --isa-v1-selftest
```

生产 finalizer、typed ProgramIO、`npusim --program-sequence` 的
1×2/EP2 两层 Prefill→Decode→Decode 同实例的原有 transport/部分
expert 计算 trace 运行通过；三个段各一个
`P0.logits` probe，标记依次 `(0,not-final),(1,not-final),(2,final)`，
KV bytes `128→192→256`，只产生一次 one-shot drain、一次
`SIM_RESULT makespan_cycles=8852`。独立 P2 flow oracle 期望跨 die
12 包，实测 D2D in/out 各 12、die0→die1 和 die1→die0 各 6；
expert core16 执行 6 次 MATMUL，core0 bridge `DTE_STATS`
issued/completed 各 12，pending/active/inflight 为 0。core0/core16
`PROGRAM_MEMORY` LSU/DTE residual、P2P sessions、P2P timing、router
和 D2D link residual 全为 0。聚焦 28/28 单元测试通过，含三类
物理断链负测；两项旧 canonical golden 通过，ISA v1 selftest 与
`git diff --check` 通过。产物及 stdout 留在本地 `build-debug-final/`
（未入源码提交）。

**2026-09-15 计算量审计更正：本节先前称完整 MoE 推理 runtime 已通过，
现撤回该声明。** P2 每个 expert rank 的两条 token assignment 要求
`m × 6 × H × I = 2 × 6 × 4 × 8 = 384` FLOPs，expert tensor
`3 × H × I × 2B = 192B`，分别为 gate/up/down 三个矩阵。
R1 physical lowerer 只发一次 `(1,1,H=4,I=8)` MATMUL，实际仅
`2 × 1 × 4 × 8 = 64` FLOPs、读取首 64B expert weight；原先
`expert MATMUL6` 计数不能证明 P2 384 FLOPs/动作或完整权重消费。
因此 R1 的 PASS 只证明 shared spine、MoE 通信与**部分**expert
MATMUL 的真实运行、KV/ProgramIO/资源排空；完整 expert work
coverage、full-model inference runtime、M1 仍为 **blocked**。
修复须物化 gate/up/down 独立 MATMUL、SwiGLU 与有界 SRAM
中间值，逐矩阵核对物理 FLOPs 和完整 192B weight extent，且
删去任一投影的物理负测必须失败，再运行同一 1×2 canary。

此 canary 为 **timing-only/static-route**：ProgramIO 明确采用
`TIMING_PARTIAL`，对 MoE owned output workspace 注入零初值只是使
DTE 对实际 SRAM payload 进行合法读取；仿真 MATMUL 只计算周期，
不生成 expert 的数值输出。logits probe 证明物理探针与内存读有效，
不证明 router score、expert output 或 logits 数值正确。因此这里证明
原有已发出的物理动作、数据通路、通信/状态版本与成本执行，
`compute_functional_verified=false`；不能将零注入当作有用 expert 数值
的证据。MoE training 仍停在 compile-only：shared trainable 参数
load、真实 loss/backward、router/expert 梯度与归一化同步、SGD/store
及跨两个 step 的参数版本闭包尚未 materialize/runtime。M1 四类与
任意 mesh、M2 offload 均不得由本 1×2 样例宣称完成。

## P3.3-R2：专家三投影和 SwiGLU 完整物理工作量

日期：2026-09-15。本节解决 R1 因只执行一个 64-FLOP GEMM 而撤回完整
MoE 推理验收的阻断，保留上文原始失败 witness。独立 P2 模型合同：
每 rank/layer/segment 的 expert GEMM 为 `m×6×H×I` FLOPs，
FP16 expert weight 三块 gate/up/down 各 `H×I×2B`，SwiGLU
逐 `m×I` 元素计算。冻结模型 H4/I8，Prefill 每 expert
`m=2`，三个 GEMM 每个128、合计384 FLOPs，SwiGLU `N=16`
（SFU=16，vector=64）；两次 Decode 每 expert `m=1`，每个
GEMM64、合计192 FLOPs，SwiGLU `N=8`。三段、两层、两个
expert rank 的 GEMM 有用工作合计 `3072` FLOPs。每 expert/layer
的192B HBM weight保留真实StateABI/LSU LOAD，三个MATMUL data
operand的 tensor view 分别严格覆盖 `[0,64),[64,128),[128,192)`；
没有改逻辑 action FLOPs、层数、token routing 或模型权重形状。

严格 `full_model_dataflow` 路径将一个 P2 EXPERT_FORWARD 扩展为
四个连续的 canonical 两record动作：gate 沿用P2 action ID，up、
SwiGLU、down 根据 `plan/expert/stage` 派生稳定physical action ID。
这样满足生产 finalizer 一动作只能有一个 `SRAM_BIND+compute`
body 的严格规则（最初同一P2 action串连四个compute时finalizer
真实报 `action has a non-canonical mixed record body`），未放宽
全局validator。gate/up `(1,m,H,I)` 写各自有界concat FP16
SRAM半区，SwiGLU读同一concat的 `2×m×I` 元素并写activated
SRAM，down `(1,m,I,H)` 读activated并写expert output；
COMBINE SEND直接读这个output。四动作的per-layer BufferABI、
ALLOC、relocation/tensor slice、FREE与所有ProgramIO部分初始化
均绑定到同一物理core。实际linked manifest逐die named-region的
最大地址为Dense `[0,3680)` /4096B、MoE input `[4096,4096+8512)`
/36864B、MoE comm `[40960,40960+9408)` /36864B；hardware
SRAM总容量131072B，三region总大小77824B。零token expert
测试证明专家weight仍有完整HBM state，但P2 `m=0/FLOPs=0`
时不发虚构expert GEMM/SwiGLU。

独立结构oracle逐层、逐segment从P2assignment与physical
record/relocation/binding重推三次projection的shape、FLOPs、
192B互不重叠weight view、SwiGLU N、concat/activated/output
handoff和COMBINE DTE endpoint。分别删除gate/up/down或
SwiGLU的任一物理record，4项故障均失败；原drop bridge、
router GATE、最后一层故障继续失败。默认原2×3与1×2
production manifests冻结canonical digest不变。

生产finalizer的三段artifact SHA-256：

| phase | SHA-256 |
|---|---|
| Prefill | `1570a88161c3eb21fccc73ef67d3184b14710f17a44c9ae7fb8b32b906e6d446` |
| Decode-1 | `d70831200cc1114424a71cd6517d0de3af4e918d543fec83dfbda47bcf3c0380` |
| Decode-2 | `ebee3471dd52c4d88965b2fc41646ca220488a5b945355e56120b1712086c709` |

真实1×2/EP2两层Prefill→Decode→Decode在同一NpuSim/SystemC
实例执行：远端expert core16 `18 MATMUL + 6 SwiGLU` starts，
SwiGLU每次start/end相差2ns；三次P0.logits probe PASS，
D2D in/out packets各12、die0→die1和die1→die0各6，KV bytes
`128→192→256` 与R1的digest完全相同，MoE bridge core0
issued/completed12、pending/active/inflight0。ProgramMemory
LSU/DTE、P2P session/timing、router和D2D link最终residual
全部0；one-shot drain1，`SIM_RESULT makespan_cycles=9338`一次。
命令：

```bash
python3 -m llm.test.frontend.integration.run_moe_full_model_sequence_runtime_canary --timeout 300
python3 -m unittest llm.test.frontend.unit.test_moe_full_model_compile_sequence \
  llm.test.frontend.unit.test_moe_compile_sequence \
  llm.test.frontend.unit.test_flexible_moe_production
./build-debug-final/npusim --isa-v1-selftest
```

本切片只验证该固定1×2/EP2模型的完整 physical MoE inference
timing工作、单次真实runtime和资源排空；timing primitives并不产生
专家数值输出，ProgramIO concat/activated/output 的
`TIMING_PARTIAL`零值仅证明 SRAM payload 可读，functional
expert/logits正确性仍为false。MoE training的shared loss/backward、
expert/router/共享梯度的全参数sync/update/store及跨两个step
state版本闭包仍未materialize/runtime；四类M1、任意mesh、
两次fresh repeatability均不能由该单case声明完成。

M2 MoE低HBM外存容量阻断：当前P2 resident每die peak `1440B`，
但已运行程序中MoE expert物理StateABI从die home base+16MiB
起，第二层再增加1MiB地址空洞（die0 state end `17,829,904B`；
die1为1GiB home base之上相同偏移）。这些空洞非模型参数字节。
P4 external DMA目前只支持计算前全部bring-in或最终writeback，
尚无访问时逐层/段DMA与有限HBM window的state remap。
低于1440B的真实HBM容量不可能用startup全量换入替代
按层流转；`offload_runtime_verified=false`，后续需真实
地址重映射、搬运窗口峰值和work-consumes-DMA happens-before。

## P3.4-A：MoE 训练反向 transport 的局部物理交接

日期：2026-09-15。范围只覆盖 1×2/EP2、两层、两个训练 step 的
四个 MoE block **compile-only**；不是已链接的 shared-backbone
端到端训练，也没有在 NpuSim 运行。保留 P2 的全部训练 action/flow
及零 token expert：empty DGRAD/WGRAD/GATE_WGRAD/COMBINE_BACKWARD
要求逻辑 FLOPs/bytes 为零且不伪造 MATMUL，非空动作仍保持原模型量。

严格 `full_model_dataflow + TRAIN` 路径新增逐 rank 有界 FP16
`backward_gradient` SRAM BufferABI；物理 `BACKWARD_GRADIENT` 的
DTE SEND/RECV 分别读/写该 ABI，专家 `EXPERT_DGRAD` MATMUL
实际读收到的 gradient，输出到有界 expert output ABI；
`BACKWARD_DX` 的 DTE SEND 读该 output，RECV 写源 rank output，
`COMBINE_BACKWARD` 从收到的 output 读。全部地址绑定与物理 record
保持同一个 P2 flow/action、die/rank 和真实 buffer endpoint；
未改变已经冻结的默认生产 MoE inference manifest。

新增独立结构 oracle `validate_moe_training_backward_handoff`，
从 P2 的 flow/assignment 反查 linked program record、relocation
对应的 exact BufferABI，拒绝传输 payload 比 P2 bytes 短、
错 rank/地址、零 work 虚构 GEMM。对同一实际编译出的 MoE block
分别删去 `BACKWARD_GRADIENT.RECV`、专家 `EXPERT_DGRAD` 和
`BACKWARD_DX.SEND` 物理 record，三项故障均必须失败。

本切片明确没有证实梯度的源头：shared loss/backward 尚未产出
发送的梯度，现有 EXPERT_WGRAD/GATE_WGRAD 也没有将物理 MATMUL
的 FP16 workspace 输出生产到 SGD 消费的 FP32 gradient StateABI；
逐 projection expert WGRAD、gate gradient FP32 同步、与整个
shared Dense spine 对齐的 backward→SGD→parameter store/load
两个 step 仍缺真实 ProgramIO 和 NpuSim witness。
`moe_training_runtime_verified=false`，四类 M1 和 M2 仍未完成。

聚焦验证：`python3 -m unittest`
`llm.test.frontend.unit.test_moe_compile_sequence`
`llm.test.frontend.unit.test_flexible_moe_production`
`llm.test.frontend.unit.test_moe_full_model_compile_sequence`
共 31/31 PASS（79.616 秒），`git diff --check` PASS。

## P3.1-R3：1×4/TP4 Dense 连续推理的双独立 NpuSim 执行

日期：2026-09-15。本切片冻结两层 Llama Dense `H=32/I=64/V=128`、
`NH=KVH=4`、FP16、四 request 各 Prefill 一 token、两个 Decode、
1×4 四个 Die 全参与、TP4/DP1 的固定模型。真实 workload case ID 为
`workload_case_943fe13c8e8973d8`，request digest
`545aac1550970ff0ec6f096ce5b04e1beaf16c41fd47f32275665591c0c3cb96`；
P3 graph digest `f44c344c17c3571ca613f0113c16872605a96f5977ca85c35bc5e03d4d87bd04`。
新增旧 Dense runtime canary 的 `--mesh-size 1x4|4x1` 入口。
四 Die manifest 保留原 2×2 fixture 的模型/steps/TP4，重新 materialize
真实对应矩形 placement、P1 transport 与物理 fabric；硬件 Die
`x=columns,y=rows`，横向 1×4 的第一条 0→1 link 从 east 端口发出。
横纵结构测试 1/1 PASS，并曾查出、修正一次 `die.x/y` 反转。

真实第一跑：

```text
python3 -m llm.test.frontend.integration.run_dense_sequence_runtime_canary \
  --mesh-size 1x4 --output /tmp/m1-dense-infer-1x4-exec0 --timeout 900
```

三段 production linked→真实 C++ finalizer→typed ProgramIO→同一
NpuSim `--program-sequence` 时间线 EXIT0，三段 NPUP SHA-256：
`b7878f1d11bc640a7c5825e3bec96423d5eb6ccd30be6b3b024f07d38aa60121`、
`d61ebe6f266f4f512537df6a5ffab73a6e886d886b286691831900b2820fab7c`、
`27ff159561f61cc8e89ee4b7d91f889bc4c00ee6fa17e5cee8f67dac03ec1a32`。
硬件 SHA `05d75d2e063b5fc3af01946c933724e262e3509759d97f49d94ab0ebd5864285`，
simulation SHA `337f5bc3f195489e8c6ef8830d83ce709a3e79a434401bd545504bf543420afb`；
finalizer SHA `9aa386ef8c62e0218919ece0892651644ba36a15704d28c24c47571c8b4cda4d`，
NpuSim SHA `0a891d326d4a2b9245fde461f6a01569fbeea65cb73ba98c194b9ff531ab26b5`，
执行后均再次核对相同。前端 runner SHA
`5511d6560e16edbdbf959054a548bf65c7bf3b8c355cbf5d2c215c3ea4291a33`。

第二跑在 `/tmp/m1-dense-infer-1x4-exec1` 将前述冻结 linked/NPUP/
ProgramIO/finalizer 报告/hardware/mapping 共 14 个静态输入按原 SHA
逐文件核验一致，随后以新的 NpuSim 进程和新的工作目录运行完全相同的
三段命令（`--trace-window 1000000`）；EXIT0。两次原始 stdout
SHA 分别为 `cc7ad1fd752d834219d2e694a64eab9b7ae242d96e573f90e090b9f3fe44a74d`
和 `f8f1ff715788140eed2a65522bff4b73f28638a61f89eb72684d6a7ce154665c`，
原始日志中非验收文本不同，不能冒充相同原始文件；按逐条精确解析的
15 个 **验收 marker 全部一致**。两次均有三段 `(0,0),(1,0),(2,1)`、
ProgramIO 每段 `probes=4 pass=1`、KV 物理 bytes
`4096→5120→6144` 和一致 digest、`one_shot=1`、恰好一次
`[SIM_RESULT] makespan_cycles=80344`。四个物理 core 0/4/8/12 各
`LSU issued=completed=65`、HBM read/write `83648/1536B`、
LSU/DTE residual=0；各 core P2P source/destination 真 payload
`9216B`、72 次本地 completion，router/link/timing residual=0。

首次长编译的完整 wall/peak RSS 未由旧 runner 自动计量（最后一次运行中
`ps` 观察 elapsed 17:24、RSS≤115272 KiB），故正式发布预算项仍记
`not_measured`，不从文件时间反推峰值。这个 1×4 的双 NpuSim 执行只
证明 scoped Dense resident timing 推理；完整 M1 仍需四类家族×八形状
和补充项双独立运行。MoE 训练和低 HBM offload 不随此结果升级。

### 同模型纵向 4×1 的生产链和拓扑变形

同一 `H32/I64/V128/L2/NH4/KVH4`、Prefill4/four requests、
Decode×2、FP16/TP4/DP1 模型，只有物理 mesh 与 X-first
路径从 1×4 横向变为 4×1 纵向。新增 case ID
`workload_case_b0c4e6aa787ce656`，request digest
`a8c842d2f29dbc4c2118c841ab44d6b1567a8675f2a0ac9ad15916be38358eef`，
P3 graph digest `0ff9faca3a1f56bc7b4997f71cbded94f08de370f6b97ed32c38a7724fdf6ea4`。

```text
python3 -m llm.test.frontend.integration.run_dense_sequence_runtime_canary \
  --mesh-size 4x1 --output /tmp/m1-dense-infer-4x1-exec0 --timeout 900
```

第一次经过完整三段 production linked→finalizer→ProgramIO→单次
NpuSim time-line，EXIT0；真实 hardware SHA
`396172a735e226f690c35e63a6df271d66363fe74b97554a5659262e09efa2c1`，
NPUP SHA 为 `97d6fce1fefea8377480742a170517b7329203b62f34ec202fa9899cd0f1f016`、
`72ac0ca4468af08c9457536dda22d6cf0a3f348f70ee5db75b160721be9cb814`、
`94e2b8883f1f25badba8e68c1810c2ee408e9f7e1b739390cf972973764f35ea`。
第二次 fresh NpuSim 位于 `/tmp/m1-dense-infer-4x1-exec1`，与第一次
14 个静态 linked/NPUP/ProgramIO/finalizer/hardware/mapping 文件
SHA 逐个相同，执行进程独立、相同 tool SHA，EXIT0。两次 stdout
SHA 分别为 `20b539a230e0453bc2b54aa1396a804be20aa7e63516c08494d099a8ea03e7ae`
和 `05e06ba403a6cb7177c1f58a7ae33040dbb0a5508b132cf198da145859947eae`；
与 1×4 相同，raw 文本不完全相同，15 条实际验收 marker 在
4×1 两次运行中逐条精确一致：KV `4096→5120→6144B`
且三个 digest 与横向相同，ProgramIO 各段四份 probe，
`one_shot=1`、每次恰好一个 `SIM_RESULT makespan=90522cycles`，
四个 core 0/4/8/12 各 LSU issued/completed65、读/写HBM
`83648/1536B`、LSU/DTE residual0，P2P 各 core payload9216B、
本地 completion72，router/link最终 residual0。横向 1×4
同逻辑 work 的 makespan 为80344cycles；路径差异可以改变时序，
不要求两个物理形状 makespan 相同。4×1仍仅是受限 M1 Dense
resident timing 双跑，不是完整 M1、任意 die 数或 offload 发布。
