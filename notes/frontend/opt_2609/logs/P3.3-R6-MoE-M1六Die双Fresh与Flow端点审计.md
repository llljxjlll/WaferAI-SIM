# P3.3-R6：MoE M1 六 Die 双 Fresh 与 Flow 端点审计

日期：2026-09-16

## 冻结与修正

六 Die shard 使用 detached freeze `/tmp/moe-m1-87d868c5-freeze`。runner SHA `00efaf3c…`，driver SHA `727b5ec5…`；finalizer/resolver/NpuSim/simulation SHA 仍为 `76a7e61d… / 29e8fb38… / 1090f93f… / 337f5bc3…`。

本 shard 暴露并修正旧 P2P 审计把所有 executable ranks 当作 transport endpoints 的错误。runner 现在从已签 remote flow 的 source/destination 推导 endpoint runtime cores，并在 binding 记录 flow 多重集及排序后的 core IDs。reopen 从 flow rows 重新计算 endpoint 与 X-first 链路后，分别精确比较 P2P drain 和 D2D counters。zero-flow ranks 仍必须有 PROGRAM_MEMORY 行、参与 active/compiled Die 覆盖且 residual 为 0。

逻辑 flow ID 可在层和 sequence segment 之间复用，因此 reopen 保留并逐行累计完整多重集；不会用去重掩盖 request/packet 数量。

最终输出根：

```
/tmp/p3-3-moe-m1-freeze-shard-b-v3-2x3-3x2
```

matrix receipt `status=verified`，binding SHA256 `a85a5ba5665a10b754602c38475b23e6cf3670786bf98e23fc0c318172887d41`。

## 2×3 双 Fresh

- sequence `c72c1c58…`
- 全 Die/core：`0→0, 1→4, 2→8, 3→12, 4→16, 5→20`
- PROGRAM_MEMORY：core0 `71/71`；其余五 core 均 `18/18`；所有 LSU/DTE residual 为 0
- flow endpoint/P2P cores：`0,4,8,12`；zero-flow cores `16,20` 没有 phantom P2P drain
- D2D：`0→1 E=8`、`0→3 N=2`、`1→0 W=8`、`1→2 E=2`、`2→1 W=2`、`3→0 S=2`
- resolver initializations/probes：`120/1, 124/1, 124/1`
- 两次 makespan 均 `8579` cycles；17-artifact map 和 source/tool binding `5c28ce5d…` 一致

## 3×2 双 Fresh

- sequence `10ba1915…`
- 全 Die/core 与 PROGRAM_MEMORY 数量同 2×3，六 Die 全覆盖并闭合
- flow endpoint/P2P cores：`0,4,8,12`；zero-flow cores `16,20` 无 phantom drain
- D2D：`0→1 E=8`、`0→2 N=2`、`1→0 W=6`、`1→3 N=2`、`2→0 S=4`、`3→2 W=2`
- resolver initializations/probes：`120/1, 124/1, 124/1`
- 两次 makespan 均 `8643` cycles；17-artifact map 和 source/tool binding `a71d94fc…` 一致

## 严格续跑与失败目录

相同 frozen command 加 `--resume`：

```
MoE full-model matrix RESUME 2x3 verified
MoE full-model matrix RESUME 3x2 verified
```

测试：matrix audit 14/14 PASS；full-model compile sequence 11/11 PASS。

两个前置失败根均保留且不计发布证据：`/tmp/p3-3-moe-m1-freeze-shard-b-2x3-3x2` 记录旧 all-executable P2P 门禁；`/tmp/p3-3-moe-m1-freeze-shard-b-v2-2x3-3x2` 记录 reopen 错设 flow ID 全局唯一。最终 v3 从更新后的 clean freeze 全部重新双 Fresh，未复用失败产物。
