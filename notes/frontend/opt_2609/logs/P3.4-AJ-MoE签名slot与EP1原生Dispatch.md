# P3.4-AJ — MoE 签名 slot 与 EP1 原生 Dispatch

将 P2 `MoeCompileSequence.unit.spec.trace.assignments` 的每 token `slot_index` 纳入正式 `MoeFullTrainingBlockWorkload.frozen_slot_by_token`。源 IR0 构造器按 token 序填入，schema 要求每位专家的 slots 恰好是 `0..count-1` 的排列；EP2 等非恒等排列仍是合法源，不能错误降为 EP1 复制。

EP1 两层真实训练源的 `expert_count=1`、token→expert 全 0、token→slot 恰为 `0,1,2,3`。仅在这三个条件同时成立时，新的 `MOE_DISPATCH` leaf 将 4×4 FP16 activation 用原生 `DTE_ISSUE + DTE_WAIT` 复制到同序 expert input。路由表为已签 4×5 INT32 输入，保留在 BufferABI 与源路由证明中；IR2 ScheduleSet 为 dispatch 签专用 DTE token，N5 action、CommandFragment、linker 和 linked manifest 的 DTE operand role 均窄支持该动作。每层原始 2 条、生命周期装饰后 4 条，DAG 相对验证通过。

验证：源 workload 与 dispatch 测试合计 24/24 PASS；route-state-native、expert、manifest/linker 回归 41/41 PASS；相关 Python `py_compile` 和 exact-path `git diff --check` PASS。负测拒绝短 DTE payload；非恒等合法 slot 排列不满足当前原生复制的 guard。官方 `_lower_fragments` 已越过 router 与 dispatch，下一阻断为 `MOE_COMBINE` 尚无 public opcode 映射。Combine 当前物理 action 缺动态 route score operand，不能以普通 copy 冒充 weighted combine；完整两步 SGD native 门禁保持关闭。
