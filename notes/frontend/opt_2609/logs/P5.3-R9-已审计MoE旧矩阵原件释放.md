# P5.3-R9：已审计 MoE 旧矩阵原件释放

日期：2026-09-17。P5.1-R32 已严格审计 MoE resident 100 形状 / 200 Fresh。七个 `p5-m3-moe-resident-remaining65-shard{0..6}-87d868c5` 和 `p5-m3-moe-resident-unified3-87d868c5` 共 68 个 case 的 `case_evidence.json` SHA-256 逐案等于仓库 P5.1-R32 的机器收据，分片 `matrix_receipt.json` 均为 `verified`，没有活跃进程引用。审阅后只释放这八个精确 `/tmp` 根，总占用约 1.6 GiB；overlay 已用空间由约 80 GiB 降至 79 GiB。其余 MoE 冻结源码、工具、初始矩阵与正在运行的 Dense `all_dies_scaled` 分片均未触及。

仓库保留 P5.1-R32 发布收据和开发日志。已释放的 68 个原始 case 若需逐字节复审，必须从冻结源码及工具重新生成；不能再对原路径运行发布审计。
