# P3.4-AL MoE 双层前向公开 ProgramIO 与解析

状态：仅 EP1、单步双层前向的 ProgramIO 物理输入收口；完整两步训练门禁保持关闭。

- `MoeFullTrainForwardLinkedSource` 只接收同一 `LoweringContext` 中两个真实 expert 和两个 combine 层，并重新校验完整 linked manifest。`build_timing_program_io` 由此可走公开校验入口，而非私有 prevalidated 捷径。
- 专家双 scratch 根从已签 `ScheduleSet` 与 manifest `BufferABI` 交叉核对后进入 ProgramIO 生命周期语义；缺失或不一致直接拒绝。
- 真实两层前向：51 个 lowerer leaves、207 条 record，`NaiveManifestLinker` 全量反查通过。`npusim_program_finalizer` 产生 28,562B `.npup`；公开 ProgramIO 生成 76 个 initializations、1 个 output probe。生产 `npusim_program_io_selftest --resolve` 对这三个落盘产物返回 `ProgramIo resolved ... initializations=76 probes=1`。
- 单测 `python3 -m unittest llm.test.frontend.unit.test_moe_full_train_forward_program_io.MoeFullTrainForwardProgramIoTest -v`：2/2 PASS（真实链接正证与缺失 state seed 负证）。原始单测输出 `/tmp/moe_ep1_forward_stage_a/program_io_unittest.log`；诊断产物同一 `/tmp/moe_ep1_forward_stage_a/`。
- HBM route seed 使用冻结 P2 赋值构造的两份 80B `MOE_STATIC_ROUTE` 状态；其余 load-before-store FP16 state 以非零 0.0625 seed 输入。此处的 state seed 只证明 ProgramIO 完整/可解析，不证明数值训练梯度。

尚缺：该 step0 前向的完整 NpuSim 物理运行；两步反向、共享 dCombined、expert/backbone SGD 状态链，以及两个独立 fresh 的全模型训练执行。本阶段不计 M1 完整训练。
