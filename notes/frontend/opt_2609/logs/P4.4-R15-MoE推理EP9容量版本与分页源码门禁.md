# P4.4-R15：MoE 推理 EP9 3x3 容量版本与分页源码门禁

日期：2026-09-16。按 R14 的 M2 下一形状，固定 3x3 九专家、两层 Prefill2→Decode2、请求数2 的真实模型 SHA-256 `b3135f10e464b4938ed581e4d936cfd98317c8e5269b860b859dc8a94e3eb7be`。每 Die 1024B HBM 的同模型 resident 在 `memory_capacity_exceeded` 拒绝。R14 的 8192B 外存若原样用于 EP9，offload 也在 `requests[14]` 因 704B 新请求超过剩余容量而拒绝，不能声称旧固定外存硬件支持 EP9。本阶段明确引入 **EP9-v4 容量 profile：单外存 12288B**，保留每 Die 1024B HBM、入口 Die0 的一条 256B/cycle、latency2、queue depth2、max outstanding2 外链。该新 profile 的 P3 offload 预检通过，真实外存参数分配末端 9792B；这是显式更大外存容量，而非原 8192B profile。

从冻结生产源完整编译三段 3x3 程序；每段保留九个 core `0,4,8,12,16,20,24,28,32`、51 个 StateABI。Prefill 有 39 fragments、69 StateOperandBindings，Decode 各43/73；EP9 专属 typed relinker 保留原命令、DTE flow、SRAM 和动作，仅将真实参数/KV HBM StateABI 迁到 1024B 内的分页槽。每段 source 与 paged manifest 均经原生 finalizer 得到 NPUP/报告，分页 ProgramIO 各有109初始化+1probe。生产 canary 收据 `/tmp/moe-m2-ep9-3x3-compile-canary-v1/compile_canary_receipt.json` SHA-256 `4ff29845e7a8ef2ae7d6a1af455c9933ebed3f4ac181a32a001b3d2b73486b1b`，状态 `compile_finalizer_program_io_only`，逐段绑定 manifest/NPUP/report/ProgramIO SHA。

独立 source-derived EP9 sidecar 干跑通过，ID `moe_inference_paged_runtime_7ee067e4532de21dd3ff`：47 个真实物理参数页共5304B、四个 rank0 KV 页、215 个计划 LSU 分页事件，预期外存读取16360B/写入10944B；生产 flow 的 X-first 预期有六条有向物理链路、24 packet hops。该干跑没有运行 NpuSim，以上事件和链路均只是源签名期望，**不计 M2 原生完成或双 Fresh**。relinker SHA-256 `ea4162247a0887051e31662b0abf4d2235dd0579737d12941e57546ce69abd51`，sidecar builder SHA-256 `5a37d2966301714f67ee8e04b48dd2d155ef167bb6f9a8a7b3038081009eaf02`，canary SHA-256 `a3a28d1f42225f262d70e453d7f1ad1b2bb40e07402add21bec45b5f84256298`。冻结源 `/tmp/p4-moe-core4-source-21680aa6` 对应提交 `21680aa6b9c7aff559627a3f69d351e4225cf60c`；finalizer SHA-256 `b25cb94048b9f93fef1da02a879fb4763b8efbd5a207e1aac8a68b47f72814dd`。

可复现生产产物：

```bash
PYTHONPATH=/tmp/p4-moe-core4-source-21680aa6 python3 \
  llm/test/frontend/integration/run_moe_inference_paged_ep9_compile_canary.py \
  --source-root /tmp/p4-moe-core4-source-21680aa6 \
  --finalizer /tmp/p4-moe-core4-tools-fullfresh-21680aa6/bin/npusim_program_finalizer \
  --mesh 3x3 --output /tmp/moe-m2-ep9-3x3-compile-canary-replay
```

下一门禁是冻结 EP9-v4 C++ pager/NpuSim 与 DRAMSys 配置，以空根各自独立 production compile→finalizer→ProgramIO→NpuSim 两次，退出后只读重审，核对实际九 Die、215 DMA、共享外链等待、KV 和 X-first 有向链路。10x10 固定百专家在原 8192B 外存同样容量拒绝，其新 profile 与计算/内存预算须独立审计，不把 EP9 结果推及 EP100。
