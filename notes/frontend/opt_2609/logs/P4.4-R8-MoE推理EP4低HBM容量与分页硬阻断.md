# P4.4-R8：MoE 推理 EP4 低 HBM 容量与冻结分页链阻断

日期：2026-09-16。沿用已验的 1x2 MoE offload 基线 `/tmp/p4-moe-core4-double-fullfresh-21680aa6/full_fresh_evidence.json`，本次只读尝试三个未验 EP4 矩形 `1x4`、`4x1`、`2x2`。专属复现探针为 `llm/test/frontend/integration/probe_moe_inference_ep4_offload_gap.py`，固定源码 `/tmp/p4-moe-core4-source-21680aa6` commit `21680aa6b9c7aff559627a3f69d351e4225cf60c`；探针 SHA-256 `ff2c81d97d19385eebb971751e9d6f36775230949809b54941a819a02d2d7f2f`。冻结分页 relinker/runtime 源 SHA-256 分别 `ce44aa841f2c5d490c57d2d130ffbc36300ddd0b419a854263cc414da385fad6`、`b6ec214ef51d9fce63edef2b749f1a0e40a5dd14bf8ec8a97f0c888f997a4118`；只读 NpuSim/finalizer SHA-256 分别 `c95c92b8470f3808c888b1610a1800a3e0dff6fad380f1835a98cd81bc50ddf8`、`b25cb94048b9f93fef1da02a879fb4763b8efbd5a207e1aac8a68b47f72814dd`。

```bash
PYTHONPATH=/tmp/p4-moe-core4-source-21680aa6 \
  python3 llm/test/frontend/integration/probe_moe_inference_ep4_offload_gap.py \
  > /tmp/moe-m2-ep4-frozen-gap-repro.log 2>&1
```

原始诊断日志 SHA-256 `ec932d9884e20dce3bc7a20b10cd8ee1510381407d202c81b7e41e231913037e`。探针对每个形状从同一 MoE request 派生 resident 与 external-offload policy；每 Die HBM 容量保持基线的 **1024B**，四个真实 home 为 `die:i`、base=`i<<30`；本次只读规划探针外存容量 8192B，尚未宣称最终最小预算。三个形状 resident 均在 `requests[2]` 报 `memory_capacity_exceeded`（Die0，所需 384B，当前 home 总容量 1024B）；相同模型和三步的 offload preflight 均通过，manifest ID 依次 `workload_materialization_78462ef000ad3e7b`、`workload_materialization_704987eedf382333`、`workload_materialization_ffddaf179c568e2b`。三形状的三段 full-model 生产编译也都通过，首段四核 `0,4,8,12` 的记录数为 `207,52,52,52`；Decode 两段为 `199,52,30,30`。每段 31 个真实 StateABI、首段 39 个 fragment、Decode 43 个 fragment。真实 expert/门权 ABI 分布在 Die0..3，KV 四页在 Die0。

**阻断位置**：原 `relink_moe_inference_paged_segment` 在三个形状全部九段都以 `SchemaError at source: two-layer EP2 full-model linked source shape changed` fail-closed，未生成 EP4 paged linked/NPUP/ProgramIO，因而绝无 EP4 native 或双 Fresh 成功可计。`moe_inference_paged_compile_sequence.py` 只认 core0/4、23 StateABI 和两 Die home；`moe_inference_paged_runtime.py` 只认两 HBM、19 权重页、89 个实际 LSU/DMA gate。原生 `llm/src/memory/moe_inference_mid_program_pager.cpp` 的 `ParseTwoDieFabric`、`backends.size()==2`、`die_id<=1`、`core_streams.size()==2`、`89` gate/`1384B` 权重均进一步锁定 EP2；只放松 Python 校验会被真实原生工具拒绝。

下一实现点是独立版本化 EP4 typed relinker 与 sidecar：从四核 production manifest 的实际 StateABI/LSU 与外存计划导出页、逐核 gate、DMA 字节与路径签名，保持 1024B/Die 和 resident 拒绝；同步扩展原生 MoE inference pager 的相同 schema/四 Die 路由校验，重新构建冻结工具后先做 `1x4` 两次独立 production compile→finalizer→ProgramIO→NpuSim 与退出重审。扩展前此报告只证明容量负例、offload 规划和 resident 生产源码可编译，**不证明 M2 offload EP4 验收**；R7 的 1x2 双完整物化仍是唯一已验 MoE offload 形状。
