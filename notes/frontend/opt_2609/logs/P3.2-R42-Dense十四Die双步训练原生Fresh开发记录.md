# P3.2-R42：Dense 2×7 十四 Die 两步训练原生 Fresh

沿用已验证源码提交 `9b8bce3a` 与同一原生工具，在固定硬件容量下把完整两层 Dense、TP7×DP2 部署至十四个物理 Die。先在 `scripts/frontend_tmp.py --max-gib 4 --failure-hours 0 run --kind scratch` 内完成独立编译预检：源图 772 节点，物理 DP 梯度任务 1680 条，两个副本各下沉 2194 片段；210 个物理状态 ABI、420 条两步参数梯度路径、物理门禁 13776 个 action / 287 条状态边 / 77 个 fence 全部通过。预检成功后 scratch 连同生成的 JSON 自动删除。

接着从独立空托管 evidence 目录执行完整编译、finalizer、ProgramIO resolver 及**一次真实 NpuSim 单次调用**，没有复用编译预检的任何 NPUP/链接/状态文件。14 个 Die 上的完整两步训练通过 CE 前后向、FP32 梯度同步、物理 SGD 420 次、210 个 HBM 状态初始化、1749 次 ProgramIO 初始化、28 个终端探针、跨核心状态事件与 D2D 排空；`makespan_cycles=191039`。原生可执行文件 SHA256 `61b753a956471f74e7a18320d1aa217a7b08995d771eaf078339b67186b3bc16`，精简 35 字段收据见 [R42 收据](P3.2-R42-Dense十四Die双步训练Fresh收据.json)。这是一轮 Fresh 实跑，未声称双 Fresh 复现。

运行前确认 4 GiB 托管限额，原生执行后把收据复制至 notes，随即 `release` 删除约 149 MiB 的原始 NPUP、JSON、stdout、VCD；托管 root 回落至只剩标记文件。编译继续复用已有 `build-debug-final`，未复制或新建整套 CMake 构建目录；后续临时 CMake 构建使用 `{tmp}/build` 并由 scratch 成功自动清理。

这是 M1 Dense TRAIN 中的 2×7 单一实例；其他并行映射、四类完整负载的覆盖、M2 offload 及 M3 全形状验收仍待各自独立完成。
