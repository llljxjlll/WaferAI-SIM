# P3.4-X — MoE 单 Die 原生专家 0x25 梯度 leaf

状态：1×1 两层两步的四个独立 P2 leaf 均真实 lower/link 三条 `GEMM_WEIGHT_WGRAD_TIMING` (0x25) 专家 FP32 梯度记录；完整两层两步单次 native SGD invocation 门禁保持关闭。

阻断点：先前 1×1 full-model TRAIN 落在 legacy 单 rank lowerer，虽有 E2E 专家 gradient/SGD 源状态，却只输出 activation/state/output BufferABI，没有 SwiGLU backward 上游 buffer 或专家 0x25。现在只将 `full_model_dataflow && TRAIN && rank_count=1` 导向现有 multi lowerer 的零链路分支，并把 child action 精确覆盖审核允许此 singleton。默认 legacy 单 rank profile 与推理分支不变；EP2 multi 分支不变。

真实编译的 1×1 四个 step/layer leaf 每个均有 3 条专家 0x25。独立物理 oracle 逐条核实 forward activation、SwiGLU backward 的 gate/up `(k,I)` 两段与 down `(k,H)` 上游、FP32 gradient 三个互不重叠的 128B StateABI 切片，以及对应的 operand relocation、BufferABI 和 m/n/k/dtype。一个 0x25 被改为 MATMUL 或删除时拒绝。P2 FP32 梯度生产校验现在分别严格审核 native 0x25 路径和原有 legacy MATMUL+cast 路径；EP1 四 leaf 与 EP2 正向通过，删除真实 child record 被拒绝。

验证：`python3 -m unittest llm.test.frontend.unit.test_flexible_moe_production llm.test.frontend.unit.test_moe_compile_sequence llm.test.frontend.unit.test_moe_full_train_named_wgrad_operands`，41 项 PASS；输出 `/tmp/moe_ep1_native_wgrad_tests.log`。

尚未完成 full model source 的 shared dCombined、backbone 反向/梯度、所有参数 state 更新的合并及一次物理 NpuSim invocation。此记录仅是四个独立 leaf 的真实原生梯度生产，不以 leaf 代替完整训练。
