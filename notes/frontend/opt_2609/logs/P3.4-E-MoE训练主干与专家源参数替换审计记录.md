# P3.4-E：MoE两层两步训练源模型替换与状态审计

生产MoE推理逐层删去Dense `gate_up→swiglu→down` 并接 `router→dispatch→expert→combine→residual2`。MoE训练亦须这一源模型替换；P3.4-D完整Dense15参数物理门禁只适合Dense，若连同MoE专家一起运行会重复MLP工作。新增独立 `FullMoeSharedTrainRequirements` 从真实L2/EP2/SGD两步的MoE E2E source、四个MoE production linked block及原两层Dense训练模板独立重算源参数和动作。源图仍为 partial、`source_ir0_replacement_materialized=false`，不能冒充训练执行。

对两层H4/I8/V16、seq4、EP2，每层精确排除gate_up/SwiGLU/down三个Dense前向节点及gate_up/down两个物理参数，共删六节点、四StateDecl；留下11个共享backbone StateDecl和两步22条源FP32梯度路径。11个参数依固定源tensor语义一对一关联MoE E2E `state_version0/1/2`三个真实ID，共33个唯一状态版本，owner是共享die0，不按字符串子串猜参数。两层每step独立router/route_freeze/dispatch/两expert_forward/combine和grad_dispatch/两expert_backward/dx_combine源operation refs必须存在。

MoE router Gate物理有Die0/Die1两个replicated StateABI；两个expert的逻辑gate/up/down三参数分别融合成该expert die上的一个真实HBM StateABI。每step/layer源梯度要求为2个router replica + 2expert×3投影=8条 `source参数+die owner+梯度producer→sync→SGD→STORE+0→1→2`路径，四unit合计32条。物理fused参数不是三份独立HBM状态；三投影要各自证明真实FP32 producer。零token expert需源图和物理程序显式zero-work/unchanged-state规则，不能伪造正梯度计算。

生产`require_moe_shared_train_no_dense_mlp`在真实Manifest中查被替换4个Dense StateABI全无且保留11共享Source StateDecl全有；旧真实Dense backward15状态立即拒。full-MoE源门禁继续要求物理DAG未执行6个被删Dense MLP forward/其反向，四MoE source unit及全部每step/layer源MoE operator实际有物理动作，并须真实新的训练IR0→IR1/projection/schedule将MoE combine输出接residual2。common timeline linker检测 `flexible_moe_production_lowering` MoE片段，缺独立源替换约束立即拒；即使提供当前source partial，仍按未物化full-model IR0严格拒，不制造SUCCESS状态或运行receipt。

`python3 -m py_compile`新MoE schema/源gate/common merger和自测通过；`python3 -m unittest llm.test.frontend.unit.test_full_moe_shared_train_requirements` 18/18 PASS，涵盖11共享状态33版本、Router双die状态、expert各投影owner、旧Dense15负例、被篡改替换及source_ir0 false边界。没有MoE full TRAIN生产finalizer/ProgramIO/NpuSim成功结果。下一步开发实际两层MoE shared主干+CE→LM head→各norm/attention反向和具名专家投影FP32梯度，在正式源GlobalActionDAG单时间线串两SGD步骤并验数据流/状态/drain；目前full-model MoE TRAIN runtime **blocked**。
