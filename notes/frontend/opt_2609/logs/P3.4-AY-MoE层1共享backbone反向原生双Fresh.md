# P3.4-AY：MoE EP1 层 1 共享 backbone 反向原生双 Fresh

状态：**partial native pass；M1/M2/M3 full-training gate closed**。层 1 的 expert/router dX 合流后，本阶段继续以该真实 output 作为 norm2 的上游，构建 norm2 gamma FP32 WGRAD、`RMSNORM_BACKWARD` 输入 dX，再和 residual2 的真实 skip 梯度通过 FP16 `RESIDUAL` 加法形成 residual1 的上游。源码 builder 严证 norm2↔residual1↔residual2 的同层前向引用、残差反向 skip、参数 StateDecl、shape/dtype/sharding；没有伪造 tape。

源码提交 `7a3d1dc2`，新 source 测试 5/5 PASS。共享树诊断 `/tmp/moe_layer1_backbone_probe1` 完成 N4/N5/N6、finalizer、ProgramIO、NpuSim 两 step 后因并发导入 Python 源哈希漂移而 fail closed，未计 Fresh。正式 clean detached freeze `/tmp/moe_layer1_backbone_freeze` 为 `7a3d1dc2fac20f92698e63d563ceced6ac0da2b2`。

两次独立全新执行根 `/tmp/moe_layer1_backbone_fresh0`、`/tmp/moe_layer1_backbone_fresh1`，各含 step0/step1 materialize→finalizer→resolver→NpuSim，共四次 one-shot native。每 step 67 leaves、316 native records、2 个 norm gamma WGRAD、2 个 RMSNorm backward、5 个 GEMM dX、6 个 RESIDUAL、104 个初始化、9 个 output probes。层 1 norm2 gamma 梯度与 residual1 上游梯度各有且仅有一个 terminal probe；HBM 读 1248 B/写 0 B，ProgramIO verify、credit 平衡和 D2D drain 均 PASS；0 SGD。

只读审计 `/tmp/moe_layer1_backbone_double_fresh_audit.json` 为 `pass_partial_only`：两根各重开 17 个产物，重算 32 个显式源文件、342 个入口/352 个出口 Python module、finalizer/resolver/NpuSim SHA、真实 NpuSim cwd `/tmp/moe_router_dx_tools`、每类 records、NPUP、ProgramIO 与日志；两 Fresh 同步骤 linked ID 精确一致。工具 SHA 仍为 finalizer `6cc731011230c674e243279a7e7a65490e476c645a261177c71fa21d3fbd09c1`、resolver `f39ca054372cdd1c4618b1db304f3a78294a76880fc89d5f66c36eef1f3e4856`、NpuSim `b9db246e81ea353d931a894e59cb330c22810120bbc5b1f613dbb2e70cbb98d5`。

这仍是 timing/结构证据：上游 0x25/0x26 没有数值梯度字节产出，所有相关 probe 期望零；尚未反传 attention/层 0，未给 router/expert/backbone 所有参数更新，也未在同一 HBMRuntime 内执行两个 SGD step。下一阶段先接 router 与三个专家参数的源绑定写回，随后继续 attention→layer0；全训练数值门禁保持关闭。
