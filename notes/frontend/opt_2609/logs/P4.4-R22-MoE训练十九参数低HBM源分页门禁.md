# P4.4-R22：MoE 训练 19 参数低 HBM 源分页门禁

日期：2026-09-17。源码提交 `04299b23`，范围为 EP1/1×1、两层 tiny MoE、两步 SGD。此阶段**仅是低 HBM 生产源、ProgramIO 与 DMA 事件合同**；还没有让 NpuSim 执行外链，不计 M2 runtime 成功。

固定同一训练请求在每 Die 2560B HBM 下，P3 `resident_hbm` 明确 `memory_capacity_exceeded`；改为 `EXTERNAL_OFFLOAD`、host:0 2048B 后，P3 materialization 通过，真实 HBM workspace 最高 2016B，两个参数分配是 shared 568B@0、expert 384B@576，共 952B。物理训练源的 19 个 StateABI 恰好按 shared+router 568B、六个 expert 投影 384B 与这两个 P3 分配对应；另两个每步 80B 静态 route trace 留在 HBM，不冒充模型参数。

源 relinker 对两个已经通过 P3.4-BB 双 Fresh 的实际 160 片段/621 记录/67 StateABI LSU linked 程序，只重定位 19 个可训练参数到 HBM 2048..2176 的 128B 复用槽，两个 route trace 留在 2304/2432，最高地址 2512 < 2560；源 action、opcode、record、DAG 与 SRAM buffer 不改。两个重定位 linked manifest 均通过严格 schema 和冻结原生 finalizer，NPUP SHA-256 分别 `2d5664f72108256c7585848319d096ccf179c0e51018772b9a2cadb4498ee7df`、`4aae885ae1937707328a61d43059c508f9a6e91e369f3fc526912e3a9b2a69f8`。

ProgramIO 由原生产侧重绑到重定位 NPUP：每步保留 150 个 SRAM 初始值、2 个真实 HBM route seed 和 1 个 loss probe；19 个权重 HBM seed 改由外存 authority 负责。两步新 sidecar 分别有 152 initializations、1 probe，冻结 `npusim_program_io_selftest --resolve` 两次均通过。独立源签名 DMA sidecar `/tmp/moe_all_sgd_c3_paged_runtime.json` SHA-256 `d0cabb08b80d58e226c07cd2e38f176cd4cbbdb62d6425ceb2312bff217d2819`，绑定 P3 request/plan、两个真实 linked digest、单共享外链 queue/max outstanding=2、19 个非零 ProgramIO 参数 seed、两步各 46 次 LOAD 前 restore 与 19 次 STORE 后 writeback，共 130 个真实 LSU gate，预期外存读 4864B、写 1904B。两个 route load/step 保持 HBM，不计外链事件。

下一步必须接 NpuSim 原生 pager：按这些实际 LSU 事件同步执行真实 external↔HBM DMA、在 128B 槽复用前确认前次消费完成、用外存值探针核对版本 0→1→2、检查单次仿真两步、共享链路服务/排队及最终 drain；随后从干净冻结源与工具做两次独立完整物化/执行。当前只有 source/finalizer/ProgramIO/sidecar，通过这些步骤不能宣称 M2 MoE training 通过，也不证明 SGD 数值更新。
