# P3.4-V — MoE 单 Die 物理前向 HBM 与官方 IR1

状态：EP1 两层 step0 前向源的真实物理候选通过；完整两层两步 native SGD 门禁保持关闭。

从真实 1×1 MoE 训练 preflight/四个编译 unit 和完整 Dense 共同主干前向开始，构造 EP1 物理组、无虚构 D2D route 的单 Die embedding、11 个 shared 与 8 个专家/路由参数的互不重叠 HBM home，以及 19 个实际 StateDecl 绑定。源 IR0 的 30 个前向节点与物理 IR1 候选逐节点一一对应；8 个 EP1 owner proof 均绑定 Die0 的 source E2E state、参数视图、生产 StateABI、切片地址。官方 `IR1.validate` 接受此候选。EP2 原有 32 节点、16 owner、两 Die 路由和 HBM 布局保持验证通过。

负测篡改单 Die expert owner proof 的物理 Die 为 1，source-bound 校验拒绝。EP1/EP2 HBM 地址空间数量与实际 rank/EP 数精确相等；router ABI extent 读取实际声明而非固定 EP2 的 16B。

验证：`python3 -m unittest llm.test.frontend.unit.test_moe_full_train_ep_placement llm.test.frontend.unit.test_moe_full_train_ep_ir1_source`，输出 `/tmp/moe_ep1_physical_stage_tests.log`；最终测试摘要以退出码为准。

后续仍需将真实 dCombined、backbone 和 expert 反向、所有 FP32 梯度生产、两步参数 v0→v1→v2 更新与一次物理 native invocation 连通，并完成双 Fresh/reopen。当前物理 IR1 只证明 step0 前向，不代表完整 TRAIN 可执行程序。
