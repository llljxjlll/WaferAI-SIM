# P3.2-R9：Dense Embedding 离散输入与真实表梯度覆盖

日期：2026-09-15。范围：M1/P3.2 源训练梯度 oracle 纠错；不增加完整训练 runtime 状态。

在完整 Dense TRAIN 前向源图，`EMBEDDING` 的 index input `token_ids` 为离散 INT32，参数 table 为 FP16，hidden output 为 FP16。对于 token ID 并无可执行的 dIndex/dX 数值梯度；上游 hidden FP16 dHidden 进入 index trace 驱动的 FP32 embedding table WGRAD。若只为了“每个 forward node 都有 backward 标签”新造 FP16 Embedding index `dX` primitive、两个重复 scatter 或以 MATMUL 凑齐表梯度，会重复/错误计算并让 full-model coverage 假阳性。

修正仅新 `schema/full_dense_gradient_requirements.py`：`required_forward_refs` 仍覆盖每个原前向 op（包括 embedding），全部源参数 StateDecl/每step/rank FP32 gradient path 仍存在，`required_backbone_backward_refs` 在原 CE_FORWARD 外再排除 EMBEDDING 的不存在的 token-index derivative；embedding 表参数的 `backward_producer_refs`/`named_wgrad_op_ref` 仍是必须由真实 source+indexed native FP32 scatter 装配来证明的要求。TP1 的 26 forward 节点目前24个需独立 backbone dX，加CE dLoss与embedding table WGRAD；TP2/DP2 源 34 forward 节点对应32个 backbone dX 需求。此前 P3.2-R6/R8 日志所写原 33 个“非 CE” reverse 是历史未纠正计数，不能当现在的物理覆盖目标。

`python3 -m unittest llm.test.frontend.unit.test_full_dense_gradient_requirements llm.test.frontend.unit.test_full_dense_gradient_physical_gate llm.test.frontend.unit.test_full_dense_train_request_binding`：25/25 PASS，检验各形状仍拥有完整 embedding forward StateDecl/参数 WGRAD/FP32 extent，同时拒绝不存在的 `backward::<embedding>` index 梯度要求，保持真实 legacy/read-only 参数与 motif 冒充 full-model 的门禁。表 FP32 indexed scatter native、严格 index view、SRAM/HBM大表分块、真实片段 ABI/ProgramIO NpuSim 及多步 state continuity 尚未生产实跑；这阶段属于源码批次。
