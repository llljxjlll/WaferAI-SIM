# P5.3-R10：Dense 训练链接重复重算消除

日期：2026-09-18。源码提交 `cd106431`。在 2×10、TP10×DP2 双步 Dense 训练双 Fresh 中，生产编译约需 1590 秒/轮；继续针对 P5.3 编译规模成本检查 TP×DP2 的 `link_train`。以同一 TP3×DP2 两层双步生产编译作 cProfile 样本，确认 `TrainLinkedProgram.create().validate()` 在 `NaiveManifestLinker().link_train(source)` 已生成规范 manifest 后再次对同一 source 完整链接。旧实现的链接区段执行 2 次规范链接，采样累计 15.725 秒。

生产 `link_train` 现在只在当前 builder validation session 内记录由规范 linker 刚生成的 source/manifest **对象身份配对**。`TrainLinkedProgram.validate()` 仅对这一精确配对复用证明；从文件重新读取、离开会话、换源或换 manifest 对象时仍调用规范 linker 重算，并保留后续完整内容 ID 与结构校验。没有更改 manifest 字段、record 语义、发布 schema 版本或 native 工具。

相同 TP3 生产编译探针在修改后再次通过 64 KiB wire SRAM 调度、1908 个 linked fragments、90 个物理状态、180 条梯度路径和 3792 个物理动作的物理门禁。cProfile 下链接区段从 **15.725 秒降至 8.935 秒**，规范 linker 调用从 2 次降为 1 次，约减少 43% 的采样耗时。该数字只代表同机 TP3 编译的带 profiler 样本，不外推为 TP10 或端到端原生仿真加速。

`test_train_forward_link` 7 项通过，其中新增回归逐次计数：builder 链接一次，退出会话后 `validate()` 再严格链接一次；现有严格 serde、ID、非法 source、篡改校验继续通过。`test_full_dense_training_dp2_source` 4 项通过。两次 TP3 探针均放在 4 GiB 上限的受管 scratch，成功后自动删除；临时 profiler 脚本亦已删除，专用 `/tmp` 根只余 23 B 标记。现用 `build-debug-final` 保留。

此前 2×10 原生双 Fresh 收据绑定旧源码 `0a8deffe`，不能据此宣称新提交完成相同形状的双 Fresh。此项减少重复编译工作，不解决 Dense 全 Die 推理 Embedding 分段、MoE 训练生产链或 M1–M3 剩余矩阵。
