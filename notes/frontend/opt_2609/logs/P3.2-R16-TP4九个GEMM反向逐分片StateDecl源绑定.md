# P3.2-R16：TP4 九个 GEMM dX 的逐分片 StateDecl 源绑定

日期：2026-09-16。本阶段承接 R15 owner-rank 投影。TP4 生产前向图有 9 个 GEMM 和每个 GEMM 对应的 4 个不同参数 StateDecl；现有 `GemmInputDxWorkload.source_parameter_state_ref` 仅表示一个 state，不可以把单 shard 假作 4 个 rank 的权重。新增 `dense_tp_gemm_dx_state_refs(plan,forward_ref)` 从同一 `FlexibleDenseTrainPlan` 逐 TP rank 返回真实前向 GEMM 权重参数状态 ID，并核对 forward 的 StateAccess READ、StateDecl 分片 index、参数 tensor identity、rank-local shape/dtype/bytes 和唯一 owner rank。对各 GEMM 总计 36 个唯一 StateDecl；缺 shard READ 与伪造 GEMM ref 明确失败。

聚焦验证：

```text
python3 -m unittest llm.test.frontend.unit.test_full_dense_tp_gemm_dx_state_bridge llm.test.frontend.unit.test_full_dense_tp_owner_projection
```

结果 **4/4 PASS**。保留既有公共 GEMM dX workload schema 完全不变，1×1 R14 native artifact 的源码 ABI 未受影响。该 source bridge 目前尚未接入完整 TP4 IR0→native GEMM dX，缺失 TP-partial dX→逆 ReduceScatter SUM、60 shard 完整 WGRAD/SGD、DP>1 DTE SUM 和双 Fresh；不能据此声称完成 1×4 或 M1。
