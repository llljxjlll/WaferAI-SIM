# P3.2 R18：TP4 两步真实反向源与 Tape 安全融合进展

2026-09-16。目标是从已验证的 TP1 两层两步完整训练出发，把四个 TP shard 的真实反向梯度和参数状态逐个送入原生执行链。**本记录是阶段性进展，不表示 TP4 已原生执行或 M1 完成。**

## 已实现

- DP1/TP4 两步 IR0 图有 382 个实际节点、432 个 StateAccess、60 个不同参数 StateDecl；每步有完整 CE seed/backward、逐层反向及每个权重 shard 的 FP32 WGRAD 与 SGD。前向 GEMM dX 按每个 source parameter 的四个真实 rank StateAccess 绑定，FP16 激活 dX 和 TP-partial sharding 保留，反向 AG/RS 跟随前向输出分片。norm、attention、RoPE、residual 和 embedding 均在同一真实两步 IR0 图内。
- 每个 shard 的 step0 SGD 到该 shard step1 forward 的控制版本边独立保留；缺任一 rank SGD 状态访问和跨步版本边，validator 必须拒绝。
- 带反向 tape 读者的前向中间值不可通过当前仅支持一个边界输出的 V1 融合消除；`discover_fusion` 只跳过该类被 tape 真实读取的候选。两步完成后重新发现 canonical fusion candidates，仍有 8 对安全的前向 GEMM/RS 可融合。TP4 graph 经 `DenseIR0Validator`、`FusionSemanticValidator`、真实 placement 和 fusion partition 成功。

## 测试和边界

`env PYTHONPATH=.:llm/test/frontend/unit python3 -m unittest llm.test.frontend.unit.test_full_dense_training_tp4_source llm.test.frontend.unit.test_fusion_semantic_validator llm.test.frontend.unit.test_full_dense_training_two_step_ir0.FullDenseTrainingTwoStepSourceTest.test_two_complete_steps_and_all_parameter_version_edges`：11/11 PASS；新增测试验证 60 个参数 shard 的逐步 WGRAD/SGD、60 条版本边、8 个 tape 安全融合候选，并负测缺失的 shard StateAccess、版本边和非法融合候选。全量 TP1 两步源含真实 native lower/link 回归 13/13 PASS（143.071s），保留已有 1×1 原生路径。

实际运行 `/tmp/dense_tp4_native_pipeline_probe.py` 依次输出 `plan`, `source 382`, `ir1 382`, `partition`；紧接 `inter_die_plan.py::_unfused_all_gathers` 拒绝独立反向 `REDUCE_SCATTER`：`inter_die_plan supports only AllGather as an unfused collective`。该阻断是严格 schema 的正确拒绝。后续需给 `StandaloneCollectivePlan`、直接策略和 IR2 投影实现真实 source-backed SEND/RECV/REDUCE 及分片验证，然后打通 schedule、native lower/link、physical gradient gate 和正式矩形双 Fresh。**不能跳过 ReduceScatter、用占位 record、把现有 source PASS 申报为 TP4 原生训练。**

追加：独立反向 RS 的真实 N4 直接规划现已 PASS（SEND/RECV/WAIT/REDUCE，FP16 输入 FP32 累加），TP4 pipeline 输出 `plan_collective`；IR2 投影发现参数计算操作数绑定问题，继续收敛，不据此宣称原生成功。
