# P5.1-R13 Dense 全形状运行器源码绑定与部分 Fresh 续跑

## 目标及修改

对 1..10 全形状的长时间、双独立原生运行增加严格断点续跑：矩阵 binding v2 额外固定真实 Dense 完整推理 sequence runner 的 SHA256；若 case 仅完成 Fresh0，恢复时重新打开其 receipt、原生 stdout、KV 和原始 linked/NPUP/ProgramIO/hardware 字节及物理 Die/D2D 证据后再运行 Fresh1。若两个 Fresh 已完成但尚未写汇总证据，先重新审计并比对两者，再补写 case evidence。坏文件、单独 Fresh1 或源码/工具漂移按失败处理，不从残缺证据推断完成。

## 测试与范围

`python3 -m unittest llm.test.frontend.integration.test_run_dense_native_mesh_matrix`：12 项通过；包括部分 Fresh 恢复、字节污染与断档拒绝、源码绑定。`git diff --check` 通过。

这只完善 P5.1 的运行与复验底座；不代表 100 形状已执行，亦不改变 M1/M2/M3 完成状态。已有 binding v1 的运行目录不能直接与 v2 混合续跑；需要保持原冻结驱动复核旧记录，或为新矩阵使用新目录。所有模型负载仍按各自真实物理运行收据单独计数。
