# P4.4-R17：MoE 推理 EP100 10x10 容量版本与百 Die 分页源码门禁

日期：2026-09-16。固定正式 10x10 百专家、两层 Prefill2→Decode2、请求数2 模型 SHA-256 `2f62cabab913000929d6f1566e66a61549cb4a9c02a359f616817bb3f0e0a9e8`。每 Die 1024B HBM 的 resident 在 `memory_capacity_exceeded` 拒绝；EP9 的 12288B 外存对百专家也不足，原 8192B 外存同样真实拒绝。正式 M1 源 manifest 的每 Die 两个 router gate 各800B，P3 workspace 末端至多464B：原 EP9 的1024B HBM下，完整800B源 LSU页无法与workspace同驻，192B权重槽更不可能接纳；单纯放大外存不解决真实本地页阻断。

本阶段显式引入 **EP100-v5 新硬件 profile**：一百个物理 Die 各2048B HBM，单个262144B外存；保留入口Die0唯一外链256B/cycle、latency2、queue depth2、max outstanding2和生产10x10 X-first路由。同一固定模型在2048B/Die仍 resident 拒绝；offload preflight通过，真实P3参数分配末端254392B、HBM workspace末端最大464B。v5的800B权重槽为每 Die `512..1312`，四个rank0 KV页为 `1344..1600`，均低于2048B。**这是与R14/R16明确分离的更大HBM和外存硬件profile，不是旧1024B/8192B硬件扩形成功。**

从相同冻结生产源独立完整编译三个10x10 segment，EP100专属 typed relinker保留全部一百个runtime core、原生产命令/DTE/SRAM/动作，只迁移真实StateABI到v5低HBM分页槽。每段source与paged manifest分别经原生finalizer生成NPUP/报告，分页ProgramIO各837初始化+1probe；三段生产canary收据 `/tmp/moe-m2-ep100-10x10-compile-canary-v1/compile_canary_receipt.json` SHA-256 `200ed625cfd7c7bbb1cf60457b15839b0888b2f511125a8d0ce81894dbc1f158`，状态明确为 `compile_finalizer_program_io_only`。百Die源每段有415个真实StateABI，Prefill 615、Decode各619个StateOperandBindings。source-derived sidecar对新canary只读干跑通过，ID `moe_inference_paged_runtime_a176a93f9f2ec210686d`，严格绑定真实P3 workspace百Die向量、411个物理参数页共198952B、四页KV、1853个计划LSU分页事件、预期外存读取597304B/写入115776B；生产flow的X-first物理期望为六条有向链路、32 packet hops。以上都不是NpuSim实际执行计数，尚**不计M2 EP100双Fresh完成**。

冻结源 `/tmp/p4-moe-core4-source-21680aa6` 对应提交 `21680aa6b9c7aff559627a3f69d351e4225cf60c`，原生finalizer SHA-256 `b25cb94048b9f93fef1da02a879fb4763b8efbd5a207e1aac8a68b47f72814dd`；本阶段EP100 relinker SHA-256 `a33a8ccdceb07f1f078dd12ad509f85cefb0c9228e19a00d78afec3bd29506ff`，sidecar builder SHA-256 `21074954fa4dd41dab859184a50a47e88ad84f343f0739050d550be4f84241ee`，canary SHA-256 `b08275139ba656d8d5ff90e1995418e97a49a314e69e1b807002b48339cd3237`。

```bash
PYTHONPATH=/tmp/p4-moe-core4-source-21680aa6 python3 \
  llm/test/frontend/integration/run_moe_inference_paged_ep100_compile_canary.py \
  --source-root /tmp/p4-moe-core4-source-21680aa6 \
  --finalizer /tmp/p4-moe-core4-tools-fullfresh-21680aa6/bin/npusim_program_finalizer \
  --mesh 10x10 --output /tmp/moe-m2-ep100-10x10-compile-canary-replay
```

下一门禁需冻结支持v5的原生pager/NpuSim及DRAMSys配置，然后对10x10新空根进行两次独立完整production compile→finalizer→ProgramIO→NpuSim和退出只读重审，核实际100Die HBM、1853DMA、共享外链真实等待、KV与有向D2D链路。若资源或源码校验失败，仅报告准确失败根，不把本阶段规划/干跑充作原生结果。
