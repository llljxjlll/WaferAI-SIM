# P5.1-R14 MoE 全形状部分 Fresh 严格续跑

## 目标及修改

MoE 全模型 1..10 形状矩阵以前仅能续跑已经写入完整 `case_evidence.json` 的 case；某个 case 完成 Fresh0 但在 Fresh1 开始前中断，会被当成缺失的完整 case 而拒绝恢复。本切片增加按顺序检查 Fresh0/Fresh1：已有 Fresh 的原生 stdout、三段 manifest/NPUP/ProgramIO、resolver、物理 Die/D2D 流量、KV 和源码及原生工具闭包，都重开并与相应运行器中的独立 oracle 对比。两个 Fresh 均完成但汇总文件未生成时，先比较相同语义再补写汇总。残缺 Fresh、缺 Fresh0、篡改产物继续拒绝。

## 验证及证据边界

`python3 -m unittest llm.test.frontend.integration.test_run_moe_full_model_native_mesh_matrix`：16 项通过；新增中断、篡改与 Fresh 断档负测；`git diff --check` 通过。变更后的 driver SHA 与已有冻结 shard 的 binding 不同，旧 shard 使用旧冻结 driver 严格复验；新 shard 应启用新空根并各自重新物化两次。此前 100-shape MoE resident shard00 10 case/20 Fresh 的冻结证据仍独立有效；其余形状尚未因此完成。M1/M2/M3 整体完成状态不变。
