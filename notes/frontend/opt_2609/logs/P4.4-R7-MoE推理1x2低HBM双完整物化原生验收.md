# P4.4-R7 MoE 推理 1×2 低 HBM 双完整物化原生验收

日期：2026-09-16

冻结源码 `/tmp/p4-moe-core4-source-21680aa6`、只读 NpuSim 和 finalizer 位于 `/tmp/p4-moe-core4-tools-fullfresh-21680aa6/bin`。正式证据为 `/tmp/p4-moe-core4-double-fullfresh-21680aa6/full_fresh_evidence.json`，状态 `verified`：两次在不同目录独立完成 production compile、finalizer、ProgramIO 和原生执行，每次包括两次独立 NpuSim 执行，总计两次完整物化、四次原生执行。验收器重开每次 15 个产物并核对 SHA、源和工具绑定以及物理标记，跨物化收据一致。

| 项目 | 实测 |
|---|---|
| 形状和执行 | 1×2，两层 Prefill → Decode → Decode；两次完整物化，各两次原生回放 |
| 同模型容量配对 | resident 在每 Die 1024B HBM 容量约束下报 `memory_capacity_exceeded`；offload 运行成功，每 Die 物理状态峰值 960B |
| 外存与 KV | 89 笔实际 DMA；读 4600B、写 2880B；KV 版本 0→1→2→3，容量 0→128→192→256B |
| 物理资源 | Die 0/1，Core 0/4 的 LSU issued 分别 71/18；跨 Die 双向各 6 包，D2D 共 12 in/12 out |
| 时间与排空 | makespan 9545 cycles；状态、ProgramIO、外部 KV probe 与 drain 审计通过 |
| 工具绑定 | NpuSim `c95c92b8…`，finalizer `b25cb940…`；`functional=false`，仅声明时序与状态证据 |

验收命令：

```bash
python3 -m llm.test.frontend.integration.run_moe_inference_paged_offload_full_fresh --output-root /tmp/p4-moe-core4-double-fullfresh-21680aa6 --npusim /tmp/p4-moe-core4-tools-fullfresh-21680aa6/bin/npusim --finalizer /tmp/p4-moe-core4-tools-fullfresh-21680aa6/bin/npusim_program_finalizer --simulation /tmp/p4-moe-core4-source-21680aa6/llm/test/program/p5_behavioral_simulation.json --native-timeout 900 --process-timeout 2400
```

此证据仅完成 MoE 推理 offload 的一个 1×2 真实样本。M2 的四类负载、八个代表矩形、训练 AdamW、checkpoint/recompute 和完整 resident/offload 发布矩阵尚未完成。
