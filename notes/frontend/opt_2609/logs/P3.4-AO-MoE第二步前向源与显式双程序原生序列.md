# P3.4-AO MoE 第二步前向源与显式双程序原生序列

状态：EP1 两层 step0、step1 前向均实际 materialize→IR0→IR1→IR2→Schedule→51-leaf/207-record link→finalizer→source-bound route ProgramIO→resolver→NpuSim；双程序在**同一次 NpuSim invocation**执行通过。此阶段仍不是 full TRAIN：没有反向与 SGD record，也没有 step0 写回产生的物理 v1 参数内容。M1 完整训练门禁关闭。

`build_moe_full_train_forward_ir0(..., step=1)` 严格以 P2 step1 的两层 route trace 和操作为源，每个 router/expert 参数 StateDecl 都反查 source v1 input，并证明其恰为 step0 对应 SGD output、producer operation；EP owner、HBM leaf home 和实际 physical Die 保持闭合。route HBM state 的 generation/seed 改随 step；step0、step1 的两份 route source、IR0、linked、npup、ProgramIO SHA 均不同。旧 step1 必须拒绝的单测改成 v1 正证与 step2 拒绝；另有 v0 replay、step0 route replay 负证。

原始失败 `/tmp/moe_ep1_two_step_forward_a/npusim.log`：未显式选择 forward-only 模式时，既有 Dense training sequence witness 因缺 WGRAD/SGD/STORE 正确拒绝。新增 `--moe-forward-sequence` 仅接收两份精确 EP1 双层 MoE 前向（51 个片段、207 records、19 个 trainable StateABI、2 个不同 route StateABI、签名 route ProgramIO），不改 Dense full-train witness。该模式输出每段 `[MOE_FORWARD_SEQUENCE_STEP] ... backward=0 sgd=0 functional=0 pass=1`，不会输出 `DENSE_TRAINING_SEQUENCE_STEP`。

本轮两份独立全新目录 `/tmp/moe_ep1_two_step_forward_c/` 和 `/tmp/moe_ep1_two_step_forward_d/`：各自两次完整 materialization/finalizer/resolver/one-shot 及一次两段原生 sequence PASS；每段 ProgramIO probe PASS，单 invocation 总 LSU 42/42、HBM read 2224B、HBM write 0、DTE 8/8、最终 P2P/D2D drain 0 和 credit 平衡。三项 preflight 负证均 exit 2：缺显式 flag 仍触发 Dense training gate，未签 route sidecar 被拒，重放 step0 作为 step1 被拒。每个目录的 `receipt.json`、`sequence.npusim.log`、`reject_*.log` 与两个 step 的 linked/npup/ProgramIO 是原始证据。C/D 是开发期诊断。源码精确提交 `fcbee4e8` 后，又在两个全新目录 `/tmp/moe_ep1_two_step_forward_e/`、`/tmp/moe_ep1_two_step_forward_f/` 独立完成同一完整流水；各自状态 `two_step_forward_sequence_native_pass`，两步 one-shot 与单 invocation sequence 通过。两个 receipt 的 10 项 source SHA、finalizer/resolver/NpuSim、simulation/hardware SHA 精确相同；step0 artifact SHA 均为 `7d7f903485de12d0198267cb9378664778fcba01ba225c38102d941a8ef425d8`，step1 均为 `78e9063ec58caf22c36424033c1e07852e8349caa2dbd5cc9e91570486f9f117`。每根保留原始 receipt、两个 step 的所有产物、three reject logs 和原生 sequence log。74 项相关 MoE 回归全通过，原始输出 `/tmp/moe_step1_regression_final.log`。显式 NpuSim forward-only 模式源码已随共享文件提交 `3eefad43` 入库。

当前 `ProgramIO` 对 step1 的物理 HBM 输入仍由 host timing seed 初始化；它记录和绑定 P2 v1 *来源*，但 step0 没有真实 SGD 写回，故不能声称物理 v0→v1 参数更新。`Cross_entropy_forward_prim` 为 timing primitive，loss probe 全零也不是数值正确性证据。下一阶段必须接入 CE dLoss→shared backbone dCombined、动态 score/expert 反向和每个状态的真实 SGD store，然后重新走 full-train sequence gate。
