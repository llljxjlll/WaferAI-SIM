# P4.4-R6 MoE 推理低 HBM 双完整物化验收入口

冻结源码 `d83b9c09` + 只读NpuSim `c95c92b8…` / finalizer `b25cb940…` 下，现有单次生产编译的两次独立原生回放均成功：同模型resident HBM容量不足拒绝，external 1024B/Die、89笔实际DMA（read4600B/write2880B）、KV版本0→1→2→3、真实物理Core0/Core4的LSU issued71/18、D2D east/west各6 packet（总12/12）、makespan9545、全部ProgramIO/logits/external KV probes与drain0。原始单次物化根 `/tmp/p4-moe-core4-lowhbm-full-d83b9c09-fresh`，该回放尚不等于两次独立完整编译物化。

新增 `run_moe_inference_paged_offload_full_fresh.py`：在各自空目录启动两次完整独立的production编译→source/paged finalizer→ProgramIO→双原生回放，重开每次报告、15个产物哈希、物理HBM/Core/D2D、DMA/KV/ProgramIO markers，再跨两次比较；源码文件与三工具SHA进程前后绑定，失败时不发布 `verified`。审核器已重新打开上一成功真实产物并PASS `makespan=9545, D2D=12, 15 artifacts`。正式双完整物化需在本入口冻结后新根实际执行并单独登记；M2四类/八形状总矩阵仍未完成。
