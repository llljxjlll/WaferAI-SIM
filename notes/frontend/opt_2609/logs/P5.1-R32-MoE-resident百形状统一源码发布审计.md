# P5.1-R32：MoE resident 100 形状统一源码发布审计

日期：2026-09-17。完成 R24 冻结的 canonical `1×1..10×10` 100 个形状的 **MoE 推理 resident 子矩阵**审计：100 个不同 case、200 次独立 Fresh 原生执行、每次 Prefill→Decode→Decode 三段，600 份 finalizer 和 600 份显式 ProgramIO resolver 结果。审计程序 `llm/test/frontend/integration/audit_moe_resident_100shape_release.py` 在本次退出码 0，逐个重开 200 棵 Fresh 的源码/工具绑定、17 类生产文件 SHA、三段 linked/NPUP/ProgramIO 一致性、HBM home 与全部活跃 Die、KV、D2D request/双 ACK/data、LSU/DTE 排空及原生 SIM_RESULT。机器可读收据 `P5.1-R32-MoE-resident100形状统一审计收据.json` 为 `status=verified`、精确列出 100 个 case SHA-256，文件 SHA-256 `18d7dec6e99d0bafe90f8bdd61c7b315bba4fb19b80a2254cbaeb419668464fd`；审计 stdout `/tmp/p5-m3-moe-resident-100-audit.stdout` 最后一行为 `MoE resident release PASS 100 cases, 200 fresh executions`。

审计输入是 R24 的 35/65 冻结分区计划（SHA-256 `25b563d6371a3f50e28ac952cdfcbc8d3cf7d46ee53154d7b93562c7b4bb812f`）及七个互斥分片 `/tmp/p5-m3-moe-resident-remaining65-shard{0..6}-87d868c5`。七个分片矩阵收据均 `verified`，分别包含 `[10,10,9,9,9,9,9]` 个 case；R31 对末分片重新执行严格 `--resume`，9/9 全部重开通过。初始 35 个 case 中 `1x3`、`1x4`、`2x2` 的旧 Fresh 使用了 runner SHA `924d17ba…`，不能直接算作同一次源码冻结发布；因此从新的 `/tmp/p5-m3-moe-resident-unified3-87d868c5` 根重新生产了这三项的六次原生 Fresh，之后退出并用同一命令 `--resume` 三项均重新打开通过。该补跑 `matrix_receipt.json` SHA-256 `b5a356d1fd0ac8e62eadee52ef2d08bc77def26e70b6b0d1cb8a08eba0ec91bc`，`1x3/1x4/2x2` case SHA 分别为 `e127986114a1a818e568c5baf0875e3455a4ad2fe03dd53f1927043d3b735866`、`929aa26b540e2f28d7f3e365b29a65b97678191c95965537a0f441c5ae301104`、`7e7eb02f5d330e218a887310afa08249b466d4d0d316c972913646621afffb29`，两 Fresh cycles 分别为 `8479/8479`、`8713/8713`、`8619/8619`。

统一冻结 checkout `/tmp/moe-m1-87d868c5-freeze` 的 matrix driver SHA-256 为 `727b5ec53da4b76b0d22c9c61f4a7a91854c7ea7128276ea79ef648d533e04c4`，runner 为 `00efaf3c83a7e9c5974a8374afa593cd9805bb142219430498db9f34fe2eb0c4`；200 次 Fresh 的 302 个导入 Python 文件摘要集合及四种工具摘要集合完全相同，入口快照合并 SHA-256 `07e02e66a02080e4e6789c251bb3eb4856ac07221df90e919660972162031f31`。原生 finalizer/resolver/NpuSim/simulation SHA-256 依次是 `76a7e61d8b7fd4f49d225d5927d224b6198de9ddf9f1ba397dbe51d314dd25d8`、`29e8fb38cace40beca5efdfba53ae520f04e71d088ffdc4648a868f7bfb03763`、`1090f93f8eb787a117804e33622a9a53dc656b2098ccbb29e1ed612f90bab5cd`、`337f5bc3f195489e8c6ef8830d83ce709a3e79a434401bd545504bf543420afb`。审计重算这些文件字节摘要，并逐 Fresh 检查每 Die 1,073,741,824B HBM、131,072B SRAM；每形状全部 `rows×columns` Die 活跃。模型为两层 FP16 tiny MoE，`hidden=4`、`intermediate=8`、`heads=2`、`num_experts=EP=rows×columns`、2 个 request、Prefill 2 token、Decode 2 次。路由 trace 仅使部分 expert 有 token，其余零流 Die 仍被真实编译并运行，但不虚构其 D2D 流量。

实测 200 次的 makespan 为 7,199–9,933 cycles；生产编译 36.210–137.001 s，原生 NpuSim 0.314–14.143 s，前端峰值 RSS 536,688–626,864 KiB，原生子进程峰值 RSS 485,032–693,252 KiB。这些是当前冻结机器/工具的观测范围，不能外推吞吐。验证还运行了现有 MoE matrix 16 项单测（全通过），以及对全局计划删除、重复、插入形状的三项拒绝检查；将 finalizer 换成 `/bin/true` 的真实绑定负测在写发布收据前拒绝。

**范围边界：** 本收据只证明 MoE 推理的 shape-scaled、全活跃、resident、teacher-forced/static-trace timing 子矩阵，模型专家数随形状改变，HBM 容量足够。它不是同一固定模型跨 100 形状、低 HBM offload、MoE 训练或 Dense 三个 family 的 100-shape 子矩阵；也不证明数值 logits、动态 router 决策和 M3 整体完成。其他必需补充项及其失败/未测状态须单列，不能并入本收据。
