# P3.2-R13 SwiGLU 反向原生双输入尺寸修正

两步完整 Dense 训练 manifest 已由生产 finalizer 链接为一个 NPUP，ProgramIO 已成功解析253个初始值和2个loss probes；进入 NpuSim 后，真实 runtime 在 core0 `SRAM_BIND record 175 input_count=2` 与 `COMPUTE record 176 input_count=1` 间报错。根因是 `swiglu_backward_timing::initialize` 把源图中 gate/up 的2N输入和 upstream 的N输入错声明为单个2N输入。公共 primitive 的 `data_size_input` 修为 `{2*N, N}`，与原有头文件注释、源 ISA 和 finalizer 的两输入合同一致；不改变计算工作量或梯度的数值功能标志。

`cmake --build build-debug-dense-infer-pager --target npusim -j 4` 通过；本次二进制 SHA256 `555e13c25374dbe95ba67850f64877b33c0802797449d3da4d353f4589361239`。完整两步原生执行、物理梯度门禁和双 Fresh 正由训练入口用该二进制继续验收；此记录只证明编译及阻断点修正，不把尚未通过的完整训练计作 M1。

## 全局 ISA 自检补正

首次编译后全局 ISA 自检准确发现 `record_lowering_selftest.cpp` 仍按旧单输入 `{2N}` 检查新 primitive，458项中失败1项（记录 `/tmp/p3-swiglu-two-input-isa-selftest.log`）。现将该已有 SwiGLU 回归同步为 `{2N,N}`，重编成功；`build-debug-dense-infer-pager/npusim --isa-v1-selftest` 退出0，原生 record lowering 458/458 通过（完整 stdout `/tmp/p3-swiglu-two-input-isa-selftest-v2.log`）。更新后 NpuSim SHA256 为 `0d0dc88ddfa8f38e9a7a94977cadf9ef6fede0c2a42ca8c1e9a8e781c06e72a3`；之前两份训练 Fresh 均绑定修改自检前的同一个 `555e13c2…` 二进制，它们的物理执行成功不由新自检构造。
