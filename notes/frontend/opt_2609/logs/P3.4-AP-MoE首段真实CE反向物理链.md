# P3.4-AP MoE 首段真实 CE 反向物理链

状态：EP1 两层模型的 step0、step1 各从自身实际 CE 前向 logits/labels tape 增加独立 FP32 dLoss→FP16 dLogits 反向节点；其 dLoss 不是 forward loss alias。新 IR0 各有 31 节点/55 values，经过官方 IR1、partition、N4 projection/schedule、GlobalActionDAG、N5 lower/link，各 52 leaves/213 records，恰一条物理 CROSS_ENTROPY_BACKWARD。原生 finalizer、ProgramIO resolver 和 NpuSim 单程序实际通过，两步各有非零 1/M dLoss SRAM seed、签名 P2 route HBM seed；NpuSim 的 PROGRAM_MEMORY/PROGRAM_IO/CREDIT/DRAIN 审计通过。原始开发诊断根为 `/tmp/moe_ep1_ce_backward_a/`，双独立 Fresh 为 `/tmp/moe_ep1_ce_backward_b/`、`/tmp/moe_ep1_ce_backward_c/`；后两份的 linked/npup/ProgramIO SHA 精确一致，工具/simulation/hardware SHA 一致。B/C 生成后 runner 又补了 source hash receipt 字段，因此不将 B/C 当作最终冻结证据。提交 `35f6e874` 后从全新 `/tmp/moe_ep1_ce_backward_d/`、`/tmp/moe_ep1_ce_backward_e/` 独立重跑，两个 receipt 的 8 项 source SHA、finalizer/resolver/NpuSim、simulation/hardware、两步 linked/npup/ProgramIO SHA 精确一致；native log SHA 因日志时间项不同而异，两份的 PROGRAM_MEMORY、PROGRAM_IO verify、CREDIT/DRAIN 审核均通过。随后 head-backward 扩展是另一个阶段，不混计这里的 CE-only 结果。

正负单测 `test_moe_full_train_ce_backward_ir0.py`：step0/step1 各自 CE tape、不同 IR0 ID、独立 dLoss、实际 dLogits producer；错误 producer 与重复附加反向节点拒绝。13/13（含 fixture 依赖测试）PASS。当前 NpuSim primitive 仍是 timing，不把其数值 probe 作为数学梯度证据。

**全训练门禁维持关闭。** 这条 CE dLogits 尚未被 LM-head backward 消费；两个 MoE block 的 shared dCombined、动态 score 梯度、expert 0x25 WGRAD、backbone backward、gradient sync、SGD HBM writeback 均未进入这个 linked program。两个 step 仍各由 host 初始化参数，因此当前不能证明物理 v0→v1→v2。下一最小生产连接是 CE dLogits→LM-head dHidden/dWeight，再逆向到 layer1 MoE combine 的共享 dCombined；之后才可接 router/expert/backbone 与更新。
