# P3.4-AR MoE 真实共享 dCombined 物理源

状态：EP1 两层 MoE 的两个 step 都从独立 dLoss→CE dLogits→LM-head dHidden→final RMSNorm dX，再经 layer1 residual2 backward，产出对应真实 layer1 MoE combine 输出的 FP16 dCombined；同时生成 final norm 的 FP32 gamma WGRAD。IR0 各 36 节点；官方 IR1→N4→schedule→GlobalActionDAG→N5 各 58 leaves/238 records，其中 CE_BACKWARD、LM-head 0x25/0x26、norm gamma 0x24、norm dX 0x29、residual dX 0x2C 每项恰一条。原生 finalizer、ProgramIO resolver、NpuSim 单程序两步通过。开发期原始根 `/tmp/moe_ep1_shared_reverse_a/`；该 runner 以每步 signed route HBM seed 和非零独立 FP32 dLoss seed 构造 ProgramIO；日志检查 PROGRAM_IO verify、PROGRAM_MEMORY、CREDIT、D2D drain。正负单测验证两步真实 head→norm→residual→combine 依赖与重复附加/缺 head 拒绝，结果见 `/tmp/moe_shared_reverse_unit.log`。

**仍未满足 full TRAIN：** dCombined 目前是 terminal output，没有给 MoE combine backward/score/expert backward 消费；本阶段没有专家或动态 router 梯度、backbone 其余层反向、梯度同步或 SGD HBM 写回。两步各由 host 初始化 v0/v1 参数，不能证明物理跨步状态更新。NpuSim timing primitive 只验证物理执行和 transport，不证明梯度数值。完整训练门禁继续关闭。下一最小改动需要官方 MoE combine backward action/IR0→N4→lowerer，并把本阶段 residual 0x2C 的 dCombined BufferABI 精确接入；之后才可用现有 0x28 score backward 和 0x25 专家 WGRAD 接完整链。后续 post-commit 双 Fresh 的根目录和哈希单独补记。
