# P3.4-W — MoE 单 Die 专家梯度源与双步版本链

状态：source-bound expert WGRAD 与 SGD 版本 lineage 通过；完整 M1 MoE 两层两步 native SGD 门禁保持关闭。

1×1 真正的两层、两步编译 unit 经 EP1 前向 IR0/物理 HBM 源，产生六个具名专家 gate/up/down FP32 WGRAD tile。每个 tile 的 m/n/k、FP32 梯度字节、EP owner 与原始 P2 expert WGRAD action 一致。四个 token 全属 expert0，所以每层 expert WGRAD 为 768 FLOPs、router WGRAD 为 32 FLOPs；EP2 仍是每层每专家 384、router rank0 64/rank1 0。六条专家参数桥分别核对真实 IR0 前向读取、E2E 反向/gradient/sync/SGD/store 操作，以及两步 v0→v1→v2 版本接续。删除一条路径会被拒绝。

验证：`python3 -m unittest llm.test.frontend.unit.test_moe_full_train_ep_placement llm.test.frontend.unit.test_moe_full_train_named_wgrad_tiles llm.test.frontend.unit.test_moe_full_train_gradient_source_bridge`，45 项 PASS；输出 `/tmp/moe_ep1_gradient_stage_tests.log`。

此阶段仍只证实源与物理参数 home、tile 几何和版本 lineage；尚未证明 0x25 真正消费物理 SwiGLU-backward buffer、backbone dCombined、完整共享参数反向或 native 两次 SGD。后续应通过 linked record/BufferABI/relocation 的独立 oracle 与真实单次 native invocation 核验。
