# P3.4-AZ：MoE EP1 层 1 四参数 SGD 原生双 Fresh 与状态写回

状态：**partial native pass；M1/M2/M3 full-training gate closed，numeric_gradient_witness=false**。本阶段把层 1 router gate 的真实 0x25 FP32 WGRAD 与专家 gate/up/down 的三路 0x25 FP32 WGRAD，各自绑定到同 step P2 parameter group、同一 EP owner 的 FP16 `TRAINABLE_PARAMETER` StateDecl，生成四条官方 `SGD_UPDATE` 和对应 HBM `LSU_STORE`。IR0 builder 逐项核对 P2 输入版本、输出版本与 SGD op ref、参数/梯度形状、producer 和 READ_WRITE access；不接受 detached 梯度。源码提交 `08ce9969`，新源测试含依赖回归 16/16 PASS。

正式 clean detached freeze `/tmp/moe_layer1_sgd_freeze` 为 `08ce9969ce39c9705d2b4686356efbb3e8908622`。两次全新根 `/tmp/moe_layer1_sgd_fresh0`、`/tmp/moe_layer1_sgd_fresh1` 各自从零执行 step0/step1 materialize→finalizer→resolver→NpuSim，一共四次 **独立 one-shot**。每 step 79 leaves、340 native records、4 SGD、4 LSU_STORE、108 个 ProgramIO 初始化、5 个 terminal probes；四条 SGD 分别处理 4、32、32、32 个 FP16 参数。NpuSim 每步 HBM 读 1448 B、写 200 B；ProgramIO verify、credit 平衡、D2D drain 归零。

严格只读审计 `/tmp/moe_layer1_sgd_double_fresh_audit.json` 为 `pass_partial_only`：两根各重开 17 个产物，重算 33 个显式源码、344 个入口/354 个出口 Python module、三工具哈希、实际 NpuSim cwd、linked/NPUP/ProgramIO/日志。审计从 linked state operand bindings **重新**映射四个 `LSU_STORE` 到四个互异的 `READ_WRITE`、FP16、Die0 `TRAINABLE_PARAMETER` StateABI，大小严格 8+64+64+64=200 B，并与 receipt 的 state refs 精确比对。两 Fresh 的 step0/step1 linked IDs 相同。工具 `/tmp/moe_router_dx_tools` SHA256 仍为 finalizer `6cc731011230c674e243279a7e7a65490e476c645a261177c71fa21d3fbd09c1`、resolver `f39ca054372cdd1c4618b1db304f3a78294a76880fc89d5f66c36eef1f3e4856`、NpuSim `b9db246e81ea353d931a894e59cb330c22810120bbc5b1f613dbb2e70cbb98d5`。

四个参数确有物理写回，**但这两 step 此时仍各自 one-shot，尚未证明 step0 STORE→step1 LOAD 的同一 HBMRuntime 版本链**。0x25/0x26 timing primitives 跳过实际梯度输入/输出字节，ProgramIO 的零期望不能证明参数数值变化。层 1 attention、层 0、其他共享/backbone 参数更新也未覆盖；M2 AdamW/offload 未由此证据证明。下一阶段使用显式 partial 序列模式核对同运行时版本，再继续全图反向。
