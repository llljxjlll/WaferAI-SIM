# P3.4-AM MoE 单步双层原生前向首次物理通过

状态：EP1 step0 两层完整前向 **physical NpuSim PASS**；不是两步完整训练，也不是双 Fresh。M1 完整训练门禁仍关闭。

在 `a749e805` 源代码、`build-moe-training-grad/npusim` 重建后，从 `/workspace/build-moe-training-grad` 执行：

```sh
./npusim --program-one-shot \
  --program /tmp/moe_ep1_forward_stage_a/program.npup \
  --linked-manifest /tmp/moe_ep1_forward_stage_a/linked.json \
  --program-io /tmp/moe_ep1_forward_stage_a/program_io.json \
  --hardware-config /tmp/moe_ep1_forward_stage_a/hardware.json \
  --simulation-config /workspace/llm/test/program/p5_behavioral_simulation.json \
  --mapping-config /tmp/moe_ep1_forward_stage_a/mapping.spec \
  --trace-window 1000000
```

退出码 0；原始输出 `/tmp/moe_ep1_forward_stage_a/npusim.log`。程序每层实际执行 1 个 frozen-route router (`MOE_SIGNED_ROUTER ... pass=1` 共 2)、1 个 `moe_score_weighted_forward`（共 2）、1 个 `swiglu_forward`（共 2），以及共 13 个 `Matmul_f`、1 个 `Cross_entropy_forward_prim`。`PROGRAM_MEMORY` core0 LSU 21/21、HBM read 1112B、residual 0；DTE 4/4。ProgramIO resolved/applied/verify 均 pass，单个 output probe `valid=1 exact=1 pass=1`。最后 `P5 P2P TIMING DRAIN residual=0`、`CREDIT data_balanced=1 ctrl_balanced=1`、`DRAIN d2d_link_residual=0`。

输入摘要：linked `8285ea28354b6cc8f5fd10951cc0dcfe43654e7ff79e5164ede48757d77bb9cd`；npup `7d7f903485de12d0198267cb9378664778fcba01ba225c38102d941a8ef425d8`；ProgramIO `ad3be3c80ca6c44d8cdb678a97a3fa86bb2c7f7f2225ca2d89c7bdc5d2aa2200`；hardware `67377035beb9972633b892688cd9921382a7f81bb37675019954ad586dded30f`；NpuSim binary `49c5e76cf49552667589cccc4a67fd5aedad40a1baaaa40a0f38c107b2d26e54`（均 SHA-256）。

这一组是构造和执行各一次的诊断产物，不满足 source/tool 冻结双 Fresh。IR0 builder 当前显式只接受 step0；没有实际反向 dCombined、expert/backbone 梯度以及两步 SGD 状态链，故不能把成功的 CrossEntropy 前向当作训练通过。
