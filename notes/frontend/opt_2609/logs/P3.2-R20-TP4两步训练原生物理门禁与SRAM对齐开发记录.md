# P3.2-R20：TP4 两步训练物理门禁与原生 SRAM 对齐

## 范围与已验证事实

固定两层、两步 SGD Dense TRAIN，1×4 DieMesh，TP=4、DP=1。生产 IR0 共 382 个节点、320 次真实 StateAccess、60 个参数 shard；N4/N5 构造真实的前向 AG/RS 与反向逆通信，链接 1144 个 leaf fragments、四个物理 Die（runtime core 0/4/8/12）。这是固定形状 TP4 物化进展，**不代表** 2×2 DP2 或八形状已完成。

已保存 `/tmp/dense_train_tp4_20260916_l/full_tp4_two_step.linked.json` 与同目录 `full_tp4_two_step.physical_dag.json`：完整 `verify_full_dense_two_step_linked_native` 验证通过，共 2400 个物理 action、60 条逐参数同 shard 的 STORE0→LOAD1 版本边、384 条真实 SEND→RECV 传输边。门禁逐 step/rank 核对逆向 ReduceScatter 的 LOCAL_COPY、DTE_SEND/RECV/WAIT、FP16 LOCAL_REDUCE 与逆向 AllGather 的真实 DTE、EVENT_SET/WAIT；GEMM dX 上游逆 AG 的一个本地 DTE_ISSUE 和三个远端 DTE_RECV 必须按源身份写到同一个 FP16 BufferABI，随后真实 barrier 才准 dX 读取。FP32 WGRAD 同一 BufferABI 供 DP1 数学恒等同步并被本 shard SGD 消费，不能生成虚构的 HBM WGRAD 参数 READ。60 个状态版本与每 rank 15 参数的链均已严格验证。

另一次 `/tmp/dense_train_tp4_20260916_m` finalizer 与 ProgramIO 已实际成功：原生 manifest 有 CE_FORWARD=8、CE_BACKWARD=8、SGD_UPDATE=120、DTE_SEND/RECV 各 384、LOCAL_REDUCE=64，八个各自不同的物理 FP32 CE seed ABI 和 60 个真实 HBM 参数 shard 初始化。run Fresh 前 runner 误将两步逻辑 CE_BACKWARD 计为两条物理 record，已纠正成 2 step × 4 rank = 8。

## 首次仿真发现的真实硬件阻断及当前修复

`/tmp/dense_train_tp4_20260916_n` 第一次实际 NpuSim Fresh1 在四物理核参与后，于 64631ns 被固定 SRAM 分配器拒绝：`E549 ... label=frontend_label_program_symbol_595f30625ad9e753 offset=32 base=0 requested_alignment=32 effective_alignment=64`。源位置为前向 TP fused collective 的 `temp.fused_op_skeleton_fc4b425cfb6c7aa7.chunk.0.rank.0.recv.from.1`，对应 die0 `buffer_abi_30be7b9e93a83ce5`、32B FP16 payload、原调度 offset32/align2。当前 `hardware.json` 明确需要 64B 固定 SRAM allocation，后端拒绝是正确的；该轮 Fresh1 失败，Fresh2 未启动。

已把 Python N5 减法 staging 改为真实 hardware profile 对齐、IR2 校验 rank-major 物理 stride = `align_up(logical_chunk_bytes, allocation_alignment_bytes)`，使 native LOCAL_REDUCE 记录携带源 BufferABI 推导的真实 stride，并在 `artifact_manifest.py` 要求该 stride 与全部 rank-ordered BufferABI 的起点、逻辑 element 范围精确一致。`/tmp/dense_train_tp4_20260916_aligned_native/full_tp4_two_step.linked.json` 为重新完整构造并链接的 64B 物理布局；native LOCAL_REDUCE 真实逻辑元素跨度 32B、物理输入 stride 64B，linked manifest digest `ef61f4867f92c4043f0da2ec4d2c3d8eb305e277ab24bdd18501cb527822bee1`。其完整物理梯度门禁再次 PASS，384 条传输边与 60 条状态版本边仍闭合；从同一源程序移除一次真实 DTE_SEND 或 LOCAL_REDUCE 两项篡改均被正确精确拒绝。IR2/Naive scheduler 59/59、manifest/standalone lowering 25/25 回归 PASS。

runner 即使 NpuSim 原生失败也保留 Fresh 全量 stdout。隔离 C++ V3 stride codec/finalizer/primitive 的冻结二进制通过全局 ISA 自测及真实 4×32B/64B stride 原语测试 20/20 后，以 `/tmp/tp4-stride-tools-frozen/bin/{npusim,npusim_program_finalizer}` 从全新输出根 `/tmp/dense_train_tp4_20260916_o` 完成 **两次真正独立的 NpuSim Fresh，均 PASS**。固定 NpuSim SHA-256 为 `5611ab4fd3514e1402c58711e86dd2ec20d78b4b0b0f0c7a21596aaaa388b5ef`，finalizer npup 821000B/SHA-256 为 `e7cd3fb7751c22c35062fc8dbf758b441c6fbef957a58a6ffc2653ffc88b4610`，manifest SHA-256 为 `ef61f4867f92c4043f0da2ec4d2c3d8eb305e277ab24bdd18501cb527822bee1`。finalizer 报告四核、6456 records，未绕过 ProgramIO。

`evidence.json` 记录每次一个 program invocation、两次 makespan 同为 63268 cycles，60 个真实 HBM 参数 shard、60 条跨步状态版本边、384 条物理传输边、8 个独立 CE backward FP32 dLoss seed、1588 次 ProgramIO 初始化和 8 项 probe。两份完整 stdout 分别位于 `fresh_0/npusim.stdout.txt` 和 `fresh_1/npusim.stdout.txt`，SHA-256 前缀分别 `28517f15605c60a9082ec6` 与 `3b98763cc4ebaa5af598fd`（确为不同运行日志）。逐份原生 stdout 均有 8 CE forward、8 CE backward、120 SGD，每个 runtime core 0/4/8/12 各 30 次 SGD；ProgramIO 的 resolved/applied/verify 三阶段 pass=1、非零 D2D link_sites/link_units、一条 SIM_RESULT，以及 router_residual=0、d2d_link_residual=0。物理逆通信 opcode 计数 DTE_SEND/RECV 各384、LOCAL_REDUCE 64。TP4 两层两步原生训练已经形成可复查的单一时间线；仍不把 DP1 的本地 WGRAD 视作 DP>1 真SUM，也不声称2×2及其他矩形已完成。
