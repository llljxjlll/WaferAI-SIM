# P3.2-R24 Dense TRAIN DP2 全局 N4 计划集成

将真实 2×2 TP2×DP2 训练的60条 WGRAD→FP32 DP SUM→SGD 物理路由与双波 N4 `RankProgram` 纳入 `TrainInterDiePlannedIR1`。两个本地 TP 副本各自保留24个 TP standalone collective，分别精确标识60个跨 DP 同步源引用；只允许这60个已纳入全局跨副本计划的同步节点不生成本地 TP collective。包装层重新核对源 N3 放置、DP 物理组、60条真梯度路径、通信动作配对和每个SGD的源绑定；缺失跨副本计划或删减梯度均拒绝。

实测完整两层两步 DP2 源→N3→partition→N4：2个副本、60条跨副本真实路由、每副本24条独立TP collective及60条精确 DP 引用，`TrainInterDiePlannedIR1.validate()` PASS。`test_full_dense_training_dp2_n4` 正反2/2 PASS；原 TP4 DP1 N4 回归2/2 PASS，`git diff --check` PASS。

当前进度是严格源绑定的**N4物理计划**。尚未将这些跨副本程序投影到N5任务及全局DAG，因此没有DP2 native/finalizer/ProgramIO/NpuSim或两次Fresh证据；后续N5必须同时覆盖60条真实跨Die通信与两个副本每个SGD的FP32归约结果依赖。
