# P5.3-R8：已审计 Dense 旧矩阵原件释放

日期：2026-09-17。此前 Dense resident mixed 100 形状 / 200 Fresh 的四个原始 `/tmp/p5-dense-resident-100shape-shard{0,1,2,3}-b6f7a689` 分片已完成 P5.1-R42 严格统一审计，P5.2-R5 又逐案重审并提取真实阶段开销。清理前每片恰有 25 个 `case_evidence.json`，四份 `matrix_receipt.json` 的 SHA-256 逐一等于仓库内 P5.1-R42 机器收据，没有活跃进程使用这些路径。四片原始目录总占用约 6.6 GiB；审阅后按精确路径释放，overlay 已用空间由约 86 GiB 降至 80 GiB。未触及正在运行的 `all_dies_scaled` 四个受管分片，清理后它们已有 9 个 case 证据、合计约 692 MiB；`/workspace/build-managed` 仍为 8 KiB。

仓库保留 P5.1-R42 的发布审计 JSON、P5.2-R5 的 200 Fresh 阶段开销 JSON 和阶段日志；旧原件删除后不能直接重跑该 mixed profile 的逐字节审计，如将来需要原始程序必须从冻结源码及工具重新生成。当前全 Die 正式矩阵使用另外的冻结源码和受管任务根，不依赖被释放的旧分片。
