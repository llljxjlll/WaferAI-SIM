# P4.4-R13：MoE 推理 EP6 分页重链接与生产产物门禁

日期：2026-09-16。沿用 R12 的冻结生产源、固定六专家两层 Prefill→Decode→Decode 模型、2x3/3x2 各六个物理 Die 和每 Die 1024B HBM。本阶段新增独立 EP6 typed relinker 和从源 manifest 推导的分页 runtime sidecar，不改变 EP4 的四专家合同，也不把编译产物计为 M2 原生完成。

两形状各自完整编译三段生产源，对每段原始与分页 manifest 均调用冻结原生 finalizer，生成 NPUP 和报告，并从分页产物生成 typed ProgramIO。每段有六个 runtime core `0,4,8,12,16,20`；分页 ProgramIO 各有 **85 初始化和 1 probe**。R12 的原始源 ProgramIO 为 Prefill 120、Decode 124 初始化；分页后将 35 个物理参数页迁到外存，故这里的 85 是分页产物的实际数量，不能混用原始源的 120/124。2x3 收据 `/tmp/moe-m2-ep6-2x3-compile-canary-v1/compile_canary_receipt.json` SHA-256 `060ddc56395e620523adac017693ac2245f6357786214c341cd5e1b185e28460`；3x2 收据 `/tmp/moe-m2-ep6-3x2-compile-canary-v1/compile_canary_receipt.json` SHA-256 `666dc828636751fbe3fdebc20e96d05714833f7558c11e2ae1d948868ab37e2b`。两份收据状态均为 `compile_finalizer_program_io_only`，绑定 relinker SHA-256 `4be3841bf30167e4f72fa07e0fd7edc56079f1584b03bf379bcd9b683a544370`、canary SHA-256 `493a5cda9d4a22bf2ef446f0b9ae5e5a57dd604aaf3eb94af6bb25ce4b4e7f87`、finalizer SHA-256 `b25cb94048b9f93fef1da02a879fb4763b8efbd5a207e1aac8a68b47f72814dd`，并逐段绑定 manifest/NPUP/report/ProgramIO SHA。

EP6 sidecar 在两个形状的只读干跑均从真实六核 manifest 验出 35 个参数页共 3432B、四个 rank0 KV 页，以及 161 个源 LSU 对应的分页 DMA 事件；预期外存读取 10744B、写入 7488B。真实 P3 外存参数保留至 6240B，8192B 外存容量未被放大；物理 X-first 路由由 2x3、3x2 各自生产 flow 计算。sidecar SHA-256 `f7c5447817ad0581a77766999a35cf79dea16ec89974d79333bf8e08e631ba05`。这些是 typed 合同和计划事件；**尚无 NpuSim 物理 DMA/链路成功结果，也没有双 Fresh**。

复现生产产物：

```bash
for mesh in 2x3 3x2; do
  PYTHONPATH=/tmp/p4-moe-core4-source-21680aa6 python3 \
    llm/test/frontend/integration/run_moe_inference_paged_ep6_compile_canary.py \
    --source-root /tmp/p4-moe-core4-source-21680aa6 \
    --finalizer /tmp/p4-moe-core4-tools-fullfresh-21680aa6/bin/npusim_program_finalizer \
    --mesh "$mesh" --output "/tmp/moe-m2-ep6-${mesh}-compile-canary-replay"
done
```

下一门禁是冻结新版本原生 pager/NpuSim 和完整 DRAMSys 配置，分别以两个空根进行 2x3、3x2 的两次独立完整 production compile→finalizer→ProgramIO→NpuSim，再退出重开审计 161 个实际 DMA、固定两笔 outstanding 的共享外链等待、六 Die HBM/KV 与 X-first 有向链路。任何 0ns abort 或只读 replay 均不算 Fresh。
