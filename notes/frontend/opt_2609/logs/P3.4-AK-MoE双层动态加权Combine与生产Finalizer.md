# P3.4-AK — MoE 双层动态加权 Combine 与生产 Finalizer

修复训练前向 `MOE_COMBINE` 未消费动态 router score 的源图缺口：IR0 combine 输入现在是 expert output、5-field INT32 route、FP16 score，score TensorValue 的消费者同时包含 route-freeze 与 combine。IR0 形状/arity、full-forward coverage 和 N5 action 角色逐层验证该链，IR1/IR2 不丢失 score。EP1 专用物理 leaf 使用现有 public `MOE_SCORE_WEIGHTED_FORWARD` 0x27，3 个 SRAM_BIND 输入、一个 FP16 输出；native kernel 实际按 route home slot 读取 expert return，乘以每 token 动态 score 后写 combined 输出。片段和 linker 对四个独立 BufferABI 与八个符号地址做精确闭合。

完整 step0 双层前向通过官方 `_lower_fragments` 生成 51 个 leaf、207 条原生记录，并通过 `NaiveManifestLinker.link` 及 manifest 相对校验。真实生产 finalizer 最初 fail closed 于 `MOE_STATIC_ROUTE` 未知 StateKind；C++ 窄加 STEP/READ_ONLY/INT32[K,5] 支持后，又拒绝 expert 四组 compute body、第二 gate/up concat 输出 64 B addend。三个精确障碍均已窄修，`build-moe-training-grad/npusim_program_finalizer` 对 `/tmp/moe_ep1_forward_stage_a/linked.json`（562,318 B）成功生成 `/tmp/moe_ep1_forward_stage_a/program.npup`。错误修改签名 JSON 未重算 artifact ID 时亦被 finalizer 拒绝（仅作为 ID 完整性证据，不冒充 dtype 负测）。

`test_moe_full_train_combine_native`（含 fixture expert tests）10/10 PASS，涵盖两层真实 score BufferABI、0x27 片段、参数篡改和整图链接。当前仅 step0 前向，ProgramIO/NpuSim 仍在推进，完整两步 SGD native 门禁保持关闭。
