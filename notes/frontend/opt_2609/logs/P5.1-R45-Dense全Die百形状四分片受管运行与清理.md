# P5.1-R45：Dense 全 Die 百形状四分片受管运行与清理

日期：2026-09-17。正式 M3 Dense 推理 `all_dies_scaled` 100 形状矩阵使用 Git clean 的冻结源码 `85359f91`、同一套冻结 finalizer/resolver/NpuSim 与 DRAMSys 配置。每个形状应有两次独立 Prefill→Decode→Decode 原生运行；仅四个 25 形状分片全部完成、再由冻结发布审计器复核 100 case/200 Fresh 后，才能声明该 profile 通过。此前源码 SHA 不同的分片不得混用。

| 分片 | 受管根 | 任务目录 | 上限 |
|---|---|---|---:|
| 0 | `/tmp/waferai-frontend-m3-full` | `job-dense-all-die-shard0-yq153fl8` | 8 GiB |
| 1 | `/tmp/waferai-frontend-m3-shard1` | `job-dense-all-die-shard1-nt7de52h` | 6 GiB |
| 2 | `/tmp/waferai-frontend-m3-shard2` | `job-dense-all-die-shard2-3w7ui1f9` | 6 GiB |
| 3 | `/tmp/waferai-frontend-m3-shard3` | `job-dense-all-die-shard3-74of5sbb` | 6 GiB |

四个任务均由 `scripts/frontend_tmp.py run --kind evidence --failure-hours 48` 启动，受管根每 30 秒检查容量；失败保留 48 小时供诊断/原位续跑，成功证据待发布审计后 `release`。如意外中断，使用同一受管根的 `frontend_tmp.py resume JOB -- python3 -m llm.test.frontend.integration.run_dense_native_mesh_matrix ... --resume --output-root '{tmp}/matrix'`，保持分片、工具、配置和超时参数不变；runner 会重审已完成 case。活跃任务与未标记目录不允许续跑或清理。

启动第四分片后检查：四个根合计约 270 MiB，`/workspace/build-managed` 为 8 KiB，系统可用内存约 10 GiB。对四个活跃根、默认受管临时根及受管构建根执行 `frontend_tmp.py prune --apply`，没有可删除的过期/已释放任务，也没有删除活跃原生程序。旧的 `/workspace/build-debug-final`、`build-debug-dense-infer-pager`、`build-release-final` 仍被现有运行入口引用，继续保留；后续新构建统一通过 `frontend_cmake.py` 的受管任务，成功即删除构建目录。

源码回归：当前源码上固定模型 TP6 2×3 的 `compile_dense_e2e_sequence_runtime_profiles` 编译通过，共 3 个 segment、3 个 linked profile。该编译检查用于确认终端 SRAM 生命周期修复未破坏固定模型映射；它不是原生双 Fresh 证据。

本记录为运行中快照，不声称四分片完成或 M3 整体验收完成。
