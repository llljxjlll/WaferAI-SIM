# P5.1-R29：Dense 1×8 固定模型 DTE V4 原生配置与复验

日期：2026-09-16。对应可复现仿真配置提交 `ef79ea0b`，驱动绑定修复提交 `9df815e8`：`llm/test/simulation_config/dense_dte_v4_spec.json` 相比默认配置只开启 `dte.async`、`dte.use_beha_dte`、`dte.fine_grained_resources`，配置 SHA-256 为 `e15a2296d314e24f2c5953a2c2c46a3faf88d5da06deec786f2b5131420616aa`，不改变模型、物理Die HBM容量或路由。固定源码 `/tmp/p5-dense-resident-v3-frozen-6b9664be`，固定 finalizer/ProgramIO resolver/NpuSim `/tmp/p4-dense-rect-fe454f69-frozen-tools/bin`。

完整两层固定 TP6 Dense 的 1×8 编译与 ProgramIO 后，默认仿真配置在真实 NpuSim 明确失败 `Dte_async primitive requires dte.async=true`；只开启 async 的配置在启动时报 `dte.async requires dte.use_beha_dte=true`；同时开启 behavioral+async 后执行时报 `DTE V4 direction requires fine_grained_resources=true`。三个错误均保留原始诊断目录，全部不计入 M3 成功案例。

在同一批已编译的三段 `.npup`、linked manifest 与 ProgramIO 上仅切换为上述完整 V4 profile，真实 NpuSim exit0：Prefill→Decode→Decode 同一仿真调用，真实三段KV字节 `13824→16128→18432`、`[SIM_RESULT] makespan_cycles=89349`、D2D `37632` 双向包，P2P timing drain/router/D2D residual 均为0，credit 方向平衡。这是**快速配置诊断**，复用了前次失败物化产物，不代替正式发布验收。

正式双 Fresh `/tmp/p5-dense-m3-dte-v4-1x8-6b9664be` 另从两个全新目录独立完整编译三段、finalizer、ProgramIO 和同一 NpuSim invocation，**fresh0/fresh1 均原生 PASS**。主驱动 `matrix_receipt.json` 为 `verified`，SHA-256 `a3eaa4b9c5189327ca8bedea2892bea5aae1b3ab6fd9ebabfc1ee6f5b5899ba1`；case evidence SHA-256 `191bd5c548f31521fae43b6a004a63df8090533ce036154377aa79558e434570`。退出后新的独立 Python 进程使用冻结源码 `audit_cached_case` 对两个 Fresh 的输入、工具、三段 linked/NPUP/ProgramIO、原生 stdout、物理 Die/core、KV 与 D2D 逐字节重开审计，**PASS**。两个 Fresh 均为真实物理 Die `[0,1,2,4,5,7]`、`89349` cycles、KV `13824→16128→18432`B、D2D `37632` 进/出包。

冻结驱动的顶层 `--resume` 有一个独立的严格绑定错误：首次 JSON 写入 `shapes` 元组成为数组，重开后直接拿 JSON 数组与 Python 元组比较，报 `matrix resume binding drifted`；此拒绝不是产物漂移，也不能把该 `--resume` 称为通过。本轮对当前工作树驱动做了最小修复：绑定从一开始就使用 JSON 数组，并补充 round-trip 回归；驱动单测 13/13 PASS。**未修改冻结源码或既有结果**，因此本次只声明新进程 `audit_cached_case` 严格独立重审通过，正式发布收据/双Fresh通过，而不声明冻结版本顶层 `--resume` 通过；后续新矩阵使用已修复驱动重新冻结。该case仍是固定TP6、两层 resident timing 模式，不代表Dense训练或低HBM offload完成。
