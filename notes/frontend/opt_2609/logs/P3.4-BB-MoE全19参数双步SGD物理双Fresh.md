# P3.4-BB：MoE 全 19 参数双步 SGD 物理双 Fresh

日期：2026-09-17。源码提交 `c3a085466ea224fb20be5c6e165ff492c1ac4e74`；独立冻结源码 `/tmp/moe_all_sgd_freeze_c3a08546`，冻结原生工具 `/tmp/moe_all_sgd_tools_bound`。这项验收只覆盖 EP1、单 Die、两层 tiny MoE、静态 top-1 路由、两步 SGD 的 **timing 部分**，不替代 M1 的八矩形主矩阵或 M2 offload。

本阶段把两层 Attention/QKV、MoE、embedding 的反向源接入同一个 IR0→IR1→IR2→linked/finalizer/ProgramIO/NpuSim 链；为 19 个可训练 StateABI 各建立唯一的 FP32 WGRAD→SGD→LSU_STORE，下一步从同一 HBM backing 读取。两个 step 各 160 个物理片段、621 条记录、19 条 SGD 与 19 条 STORE，每步写回 952B。原生 `--moe-all-sgd-partial-sequence` 在同一个 NpuSim 调用中执行两个 one-shot 段，报告 version 0→1→2、38 条 `[TRAIN_SGD]`、累计 `lsu_hbm_read_bytes=5184`/`lsu_hbm_write_bytes=1904`、`makespan_cycles=15481`、`[DENSE_SEQUENCE_DRAIN] segments=2 one_shot=1`、credit 与 D2D 残留为零。

验证：`python3 -B -m unittest llm.test.frontend.unit.test_moe_full_train_all_parameter_sgd_ir0 -v` 实测 13/13 PASS；两份完全独立的 production materialize/finalize/resolve/native Fresh 均 exit 0，产物目录 `/tmp/moe_all_sgd_c3_fresh0` 与 `/tmp/moe_all_sgd_c3_fresh1` 各 18 文件。只读审计命令：

```bash
python3 -B -m llm.test.frontend.integration.audit_moe_full_train_all_sgd_fresh \
  --freeze /tmp/moe_all_sgd_freeze_c3a08546 \
  --tools /tmp/moe_all_sgd_tools_bound \
  --fresh /tmp/moe_all_sgd_c3_fresh0 /tmp/moe_all_sgd_c3_fresh1 \
  --report /tmp/moe_all_sgd_c3_dual_fresh_audit.json
```

审计通过并独立重开每份 manifest/NPUP/ProgramIO/native log，检查 19 个不同 StateABI 全覆盖、实际 WGRAD producer、LOAD/STORE 同态、两步映射一致、版本边、源码/导入模块与二进制 SHA。审计收据 SHA-256 `b4da7b41e545757739c459232346fd8f8bc017bae99135a04f5d0e57f2e7093d`；两份 step0 NPUP SHA 同为 `91510c3858f92da15a746f60245a504f2d76eafffef8919726afe38daf47d497`，step1 同为 `83c972b80e8a29fc0cc14fc77fdb57d7e1f4304f1bf28e78fc2829eabe5a1869`。冻结 NpuSim SHA-256 `1f41a9b4c1cc15b169a89d10b355bc0aefd76758c0b394c57483146645295cc7`。

边界：GEMM/CE/梯度/SGD 是 timing primitive，`numeric_gradient_witness=false`，真实 HBM payload 在三个版本的 digest 相同，这是 `functional=0` 的预期，不能称为数值参数更新或训练收敛。runner 与审计仍输出 `full_training_gate=closed`/`pass_timing_partial_only`；还缺 EP>1 的 shared/expert 梯度同步、多矩形双 Fresh、低 HBM 外存分页、AdamW 与 full-dimension MoE 训练。此前 `/tmp/moe_all_parameter_sgd_diag1` 虽有原生成功但因运行时并发改动导入源码被正确拒发，本记录只引用干净冻结的两份 Fresh。
