# P3.2-R31：Dense DP2 跨副本原生 finalizer 路由来源锚

日期：2026-09-16；源码提交 `9b96e65b`，Python 对应来源锚提交 `fd215dfb`。目标是对官方 Dense TP2×DP2 两层×两步真实梯度，在 N6.5 链接后既保留原有本地 TP `StandaloneCollectivePlan` 的逐片段信任锚，又严格允许唯一的跨副本 `DenseDP2RoutePlan` 锚承载真实 FP32 求和片段，不将 DP 梯度冒充 TP 本地 collective。

C++ finalizer 的 `ManifestInputKindDto` 和精确字符串解析新增 `dense_dp2_route_plan`；仅有 train lineage 时认可 `wafer_frontend.dense_dp2_route_plan/v1alpha1` 及 `dense_dp2_route_plan_` 身份。`dense_dp_gradient_lowering` 片段必须是 standalone 物理片段，真实两个 GlobalActionDAG 各自拥有 60 个源梯度、单个副本各 5 个或 3 个 action，恰共 120 个片段；此时必须恰有一个 route anchor。本地 TP standalone digest↔本地 standalone fragment 的旧一一映射仍执行；无 DP 片段时幽灵 route anchor 立即拒绝。`input_digests` 最终全集闭包仍需完全一致；所有已有片段、来源digest和NPUP的原校验保留。

用重新从源码链接的真实 `/tmp/dense_train_dp2_20260916_d/full_dp2_two_step.linked.json`（16,984,950B，SHA-256 `d4be38dee96d45e2149564f0c9b9676bf37a19c5a26e201a5f2ba64c4edb2808`）运行新编译 finalizer **exit0**，产出 `/tmp/dense_train_dp2_20260916_d/full_dp2_two_step.dp_route.npup`（793,360B，SHA-256 `083d57b6d85d7b3a9f741e2ae304efc45507770cc4945af8ed59b02a8519dc45`），报告 SHA-256 `afc7ee4e4f9d63bbe3f139960a8f2568eb8db3b91a83201e905f05c5e56c3692`；报告有10个真实core、6,132条record和10,568个relocation。用同一新版 finalizer 对既有 Dense TP6 原生三段 linked Prefill `--validate-only` **exit0**。删除唯一 DP route anchor或伪造其schema的两份manifest，原生finalizer均 **exit1**（首先拒绝不一致 artifact ID），不能用于声明后续物理梯度已运行。

同源构建的最终三原生工具冻结于 `/tmp/dense-dp2-route-tools-frozen-9b96e65b/bin`，只读SHA-256：NpuSim `2f4fd5feba391159f8e3ebcf0cd2a1da60d4705b9ed75c8f3757756c38c60699`，finalizer `534dbdce1ec7b4b237cbbd52e948972061f5eccd92c66ac442ed5e459b837cf3`，ProgramIO resolver `d20d692968b543275d69c825ccf4e9179afe08f076188cdb345a1dd2bed73ba0`。NpuSim `--isa-v1-selftest` exit0（V1 collective program85项、phase25项等），DRAMSys HBM2配置SHA `ea23201734e0e72170847a7988fc4a45da17c0680a6bc50be8e6efdedbf35e09`。此阶段**仅NPUP成功**：ProgramIO种子绑定、同一次NpuSim invocation中两个DP副本跨Die通信、两步权重状态版本及双独立 Fresh 尚待后续真实执行证据，不能据此宣布M1 Dense DP2完工。
