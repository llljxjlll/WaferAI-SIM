# P5.3-R2：Dense 全 Die 紧凑请求构图尝试与编译门禁

日期：2026-09-17。状态：受生产语义门禁阻断，未计入 M1/M3 成功 case。尝试源码提交 `5de54a4f`，回滚提交 `4b45c7b9`。

为了降低 10×10 全 100 Die TP 推理的构图开销，曾建立与现有 shape-scaled profile 分离的 compact case：仍为两层 Dense、TP 等于物理 Die 数、Prefill→Decode→Decode，但将每步请求数由 `ranks` 改为 1，独立 case ID 和矩阵绑定。`2x3` 预检合法，case `workload_case_e294a7387dd2019f`，六 Die 全部 compute-active。17 项矩阵/审计单测通过。

从冻结 checkout `/tmp/p5-dense-compact-frozen-5de54a4f` 启动真实 `2x3` Fresh0 后，生产编译在 `_validate_inputs` 明确返回 `dense_compile_sequence_unsupported: request.decode_tokens_must_be_divisible_by_tp`，原始 `compile_failure.json` 与 traceback 保存在 `/tmp/p5-dense-compact-2x3-5de54a4f/2x3/fresh0`。原生 finalizer、ProgramIO、NpuSim 均未运行。编译器当前要求每个 Decode 步的 request_count 可被 TP 整除；TP6、请求数1违反该合同，不能把预检结果当作 runtime 成功。

已回滚未通过的 compact profile，不改变正在独立冻结源码上运行的原 shape-scaled 全 Die 矩阵。若继续降低大 TP 的请求展开成本，需要先明确并实现非整除 Decode 的 shard/padding 与 KV/attention 工作量合同，再补 producer、oracle 和真实 NpuSim 测试，或从生产编译器的其他冗余验证/程序展开环节优化。
