# P4.4-R3 MoE 推理 Die1 原生核几何与外存分页修正

## 真实阻断

已冻结源码 `6b9664be` 的1×2/EP2两层完整 MoE 推理低HBM外存复核在编译生产full-model segment后被分页source gate拒绝：`two-layer EP2 full-model linked source shape changed`。独立复核生产三个原始 linked segment 的结构仍为39/43/43 fragments、195/199/199条Core0记录、每段52条远端记录、27/31/31 StateOperandBinding，但实际远端runtime core是 **4**，旧分页链及原生 C++ pager 写死 **16**，与修复后 NpuSim 每 Die 2×2 core 网格不符。该失败未进入 NpuSim，不计成功。

## 修复与当前检验

将该固定1×2 profile 的前端分页 source、完整推理 runner 的远端算子/残留审核、原生阻塞 DMA pager 的源流、事件序、请求映射和诊断统一约束为Core0/Core4；其它state/bytes/三步Prefill→Decode→Decode数量、物理路由不放宽。三份真实生产 segment 在修复后均已通过 `relink_moe_inference_paged_segment`（原生 cores `[0,4]`、state bindings `27/31/31`）。NpuSim 已重编，`--isa-v1-selftest` exit0；原生 SHA `d5c9ab2d8dc71679add694c0fec23275ccb7d6309634a2713eac6dccb38c36fc`。

该阶段仅承诺 source gate 与 native 构建/ISA 通过。完整 low-HBM 真正双Fresh仍需在冻结源码和相同工具上执行后另记，M2四族矩阵尚未完成。
