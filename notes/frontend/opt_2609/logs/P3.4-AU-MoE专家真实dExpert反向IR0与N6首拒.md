# P3.4-AU — EP1 专家真实 dExpert→反向源及 N6 首拒

新 `MOE_EXPERT_BACKWARD` 复合源从 layer1 同一步 forward expert 激活与 gate/up/down 三个真实权重，以及 native 0x28 的 `dExpert` 构造；输出 `(M,H)` FP16 dActivation 与 `(H,I)`、`(H,I)`、`(I,H)` 三个 FP32 投影梯度。本例 `M=4,H=4,I=8,EP=1`。IR0 校验验证同层 route digest、forward 输出确实进入 0x28、精确输入/输出和 producer；IR1/Action 保持类型与有序角色。两个 step 的正反源测试 2/2 PASS；`test_ir2_schema` 46/46 PASS。

官方 source→IR1→N4→N5 可运行，首个物理拒绝在 `artifact_manifest._compute_record_abi`：`moe_expert_backward_recompute` 不是合法的单 opcode。该拒绝正确，不能用一条伪 GEMM 代替专家真实反向。下一最小物理改动是为复合反向签五块互不别名的 FP16 scratch（重算 gate/up concat、SwiGLU 激活、dActivated、gate/up dY、gate/up dX parts），并从同一 dExpert 发出真正下投影 dX、SwiGLU backward、gate/up dX、三条 FP32 WGRAD、局部 dX SUM；随后对每条记录做精确 BufferABI/relocation/ProgramIO 所有权闭包。当前 full-training gate 仍为 `closed`，此前 router partial 双 Fresh 不计专家训练。
