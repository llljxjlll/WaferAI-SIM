# P5.1-R44：Dense 全 Die矩形转置原生烟测与限额矩阵启动

日期：2026-09-17。以 Git clean 冻结源码 `85359f91` 和相同 frozen NpuSim/finalizer/resolver，分别独立运行形状 2×3、3×2 的两层 Dense Prefill→Decode→Decode。每个形状目前**仅单次 Fresh**，不计入正式 M3 双 Fresh 发布矩阵。

| 物理形状 | 实际活跃 Die | 三段 KV bytes | D2D packets (send,recv) | makespan cycles | 状态 |
|---|---|---|---|---:|---|
| 2×3 | 0–5 全部 | 4,608 / 9,216 / 13,824 | 14,400 / 14,400 | 115,381 | 原生三段 + ProgramIO + 独立重审通过 |
| 3×2 | 0–5 全部 | 4,608 / 9,216 / 13,824 | 14,400 / 14,400 | 115,219 | 原生三段 + ProgramIO + 独立重审通过 |

两条运行分别在 8 GiB 上限的受管 job 内，工具/配置与 309 份实际导入的冻结 Python 源 SHA 均由审计器复核。原生使用真实 2×3 与 3×2 邻接方向 D2D links，核心 ID 为 0、4、8、12、16、20；不同形状的路由与周期不同，不能把转置当作同一路径复制。两条单 Fresh 原件暂留用于审阅，之后正式 shard 双 Fresh 将单独产生新物化，不回收用作发布计数。

同一冻结版本的全 Die 100 形状第 0/4 分片已启动于 `/tmp/waferai-frontend-m3-full/job-dense-all-die-shard0-yq153fl8/matrix`。它在独立受管根 `/tmp/waferai-frontend-m3-full` 内运行，根目录总预算 8 GiB；正常完成的证据待统一审计后显式释放，失败证据最长保留 48 小时供续跑。启动不代表分片完成，更不代表四分片/100 形状通过。此前旧版本的全 Die 分片因源码 SHA 不同，不能混入本轮正式收据。

受管 CMake build 根 `/workspace/build-managed` 已初始化；本轮未需要重编译原生工具，故其中没有大型构建缓存。原生单形状程序和分片输出都在受管 `/tmp` 中，前置失败诊断任务已释放。
