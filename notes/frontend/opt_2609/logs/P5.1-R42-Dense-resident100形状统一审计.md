# P5.1-R42：Dense resident 100 形状统一审计

日期：2026-09-17。对重启后严格续跑的四个 Dense resident mixed 分片执行独立审计；四个分片各 25 个 case，均已产生完整的 `matrix_receipt.json`。正式机器可读收据为 [P5.1-R42 JSON](P5.1-R42-Dense-resident100形状统一审计收据.json)。

审计器 `llm.test.frontend.integration.audit_dense_native_mesh_release` 从冻结源码 `/tmp/p5-dense-optimized-frozen-b6f7a689` 读取 driver、runner 和 DRAMSys 配置，重新计算工具 SHA 与每份 Fresh 的真实导入源码 SHA；核对四分片确为 1..10 × 1..10 恰好 100 个不重不漏形状，逐 case 重开双 Fresh 证据、运行现有 `audit_cached_case`、核对分片收据及原路径。结果 `status=verified`，`case_count=100`、`native_fresh_count=200`，所有四个分片的源码/工具绑定通过。

**范围**：收据的 `profile_scope=resident_mixed_shape_scaled_and_fixed_tp6`，`full_die_active_accepted=false`。大形状下固定 TP6 只使用选定的六个计算 Die，其余 Die 可承担路由；这不等价于 100 个全 Die 都计算活跃的矩阵。独立的 all-Die-scaled 分片仍不完整，1×1 原生 `P0.logits` ProgramIO 末端 SRAM validity 为 0 的阻塞见 [P5.1-R41](P5.1-R41-Dense全Die单Die输出生存期与重启续跑.md)。该冻结 profile 不能改用本次 mixed 收据冒充发布成功。GEMM timing-only，输出零值探针通过不代表数值推理正确。

审计输出只新增上述日志 JSON 和本文，原冻结 case/NPUP/ProgramIO 内容未修改。之后若重新生成工具或源码，应另起 Fresh，不复用本收据。
