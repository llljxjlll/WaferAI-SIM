# P3.4-AW：MoE EP1 router 输入梯度原生双 Fresh

状态：**partial native pass；M1/M2/M3 full-training gate closed**。本阶段把层 1 `MOE_COMBINE_BACKWARD` 的真实 `dScore` 与同层 router 的 H×1 gate 参数 StateDecl 接入 `GEMM_INPUT_DX`，得到 router activation dX。IR0 校验要求 step、layer、冻结路由 digest、前向 router 输出与反向输入、参数 owner/rank/shape 完全一致；缺任一来源即拒绝。没有把静态 route-freeze 当可微分梯度来源。

源码提交：`82d30821`（EP1 router dX 的 IR0 精确校验）、`307d9b10`（builder、runner 模式和正负测）、`9c0c57b4`（严格双 Fresh auditor 的 router-dX profile）。聚焦测试 `test_gemm_input_dx_new_only_contract test_moe_full_train_router_wgrad_ir0` 15/15、`test_moe_full_train_router_dx_ir0` 4/4 PASS。共享工作树执行 `/tmp/moe_router_dx_probe1` 曾因运行时被并发修改的 Python import 哈希漂移而 fail closed，仅保留作诊断，不计 Fresh。

正式源码是 clean detached `/tmp/moe_router_dx_freeze`，commit `9c0c57b4d107c006ee07a73c6516958e4df9988a`；硬链接冻结工具在 `/tmp/moe_router_dx_tools`，SHA256：finalizer `6cc731011230c674e243279a7e7a65490e476c645a261177c71fa21d3fbd09c1`、resolver `f39ca054372cdd1c4618b1db304f3a78294a76880fc89d5f66c36eef1f3e4856`、NpuSim `b9db246e81ea353d931a894e59cb330c22810120bbc5b1f613dbb2e70cbb98d5`。真实 NpuSim cwd 绑定 `/tmp/moe_router_dx_tools`。

Fresh 根为 `/tmp/moe_router_dx_fresh0`、`/tmp/moe_router_dx_fresh1`，各自从零执行 step0 与 step1 的 materialize→native finalizer→ProgramIO resolver→NpuSim；合计四次独立 one-shot 原生执行，**不是**同一 invocation 内的两步 SGD。每 step 63 leaves、300 native records、5 条 dX（含 router dX）、5 条 WGRAD、100 个初始化与 10 个 ProgramIO 输出 probe；0 条 SGD，HBM 读 1248 B、写 0 B。日志显示每步 `PROGRAM_IO phase=verify pass=1`、credit 平衡、D2D drain 归零。双 Fresh 的相同步骤 linked ID 完全一致。

只读审计 `/tmp/moe_router_dx_double_fresh_audit.json` 为 `pass_partial_only`；逐根重开 17 个产物，核对 30 个显式源文件、入口 340/出口 350 个导入 Python 文件、三工具哈希、clean commit、实际 NpuSim cwd、物理 records、NPUP、ProgramIO 与日志。审计报告 `native_executions=4`、`numeric_gradient_witness=false`、`full_training_gate=closed`。

**首个物理数值缺口仍在**：现有 0x25/0x26 timing primitives 跳过输入/输出字节；10 个梯度 probe 的期望为零，不能证明专家/router 数值梯度或写回。还缺专家输入 dX 与 router dX 合并返回 shared backbone、层 0 完整反向、全参数更新，以及单次 NpuSim invocation 内 step0 STORE→step1 LOAD 的同一 HBMRuntime 版本链。M2 AdamW/offload 未由本阶段证明。
