# P3.3-R5：MoE M1 四 Die 三形状双 Fresh 与冻结续跑

日期：2026-09-16

## 冻结环境

本子批从 detached worktree `/tmp/moe-m1-b9af4f2e-freeze` 执行，源码固定在 `b9af4f2e`（已包含单 Rank GATE 修复 `5b8cc6e2`）。matrix driver SHA 为 `287dda2e…`，runner SHA 为 `924d17ba…`。finalizer、ProgramIO resolver 和 NpuSim 以 hardlink 放入 freeze 的独立 `build-debug-final`，inode 与原工具一一相同；SHA 分别为 `76a7e61d…`、`29e8fb38…`、`1090f93f…`，simulation SHA 为 `337f5bc3…`。

最终原始输出根：

```
/tmp/p3-3-moe-m1-freeze-shard-a-1x4-4x1-2x2
```

matrix receipt 为 `status=verified`，binding SHA256 为 `fe136a92822c6aea8e28397943a1272d4046dc296e63985680269a55f3a9d3be`。三个形状均各自由两个新进程完整执行 materialize/compile、三段 finalizer、显式 ProgramIO resolver 和 Prefill → Decode → Decode NpuSim；每个 Fresh reopen 17 个产物。

## 1×4 双 Fresh

- sequence：`4ae3c1cd…`
- 全 Die/core：`0→0, 1→4, 2→8, 3→12`
- PROGRAM_MEMORY issued/completed：`71/71, 18/18, 18/18, 18/18`，全部 LSU/DTE residual 为 0
- D2D：`0→1 E=10`、`1→0 W=10`、`1→2 E=4`、`2→1 W=4`、`2→3 E=2`、`3→2 W=2`
- resolver initializations/probes：`96/1, 100/1, 100/1`
- 两次 makespan 均为 `8713` cycles；source/tool binding SHA 均为 `5f764d49…`

## 4×1 双 Fresh

- sequence：`064ddc6b…`
- 全 Die/core：`0→0, 1→4, 2→8, 3→12`
- PROGRAM_MEMORY 与 1×4 相同且全部闭合
- D2D：`0→1 N=10`、`1→0 S=10`、`1→2 N=4`、`2→1 S=4`、`2→3 N=2`、`3→2 S=2`
- resolver initializations/probes：`96/1, 100/1, 100/1`
- 两次 makespan 均为 `8749` cycles；source/tool binding SHA 均为 `cfc1483e…`

## 2×2 双 Fresh

- sequence：`42e55d8c…`
- 全 Die/core：`0→0, 1→4, 2→8, 3→12`
- PROGRAM_MEMORY 与另两个四 Die shape 相同且全部闭合
- D2D：`0→1 E=8`、`0→2 N=2`、`1→0 W=6`、`1→3 N=2`、`2→0 S=4`、`3→2 W=2`
- resolver initializations/probes：`96/1, 100/1, 100/1`
- 两次 makespan 均为 `8619` cycles；source/tool binding SHA 均为 `0b131c37…`

## 严格续跑

相同 frozen command 加 `--resume` 后从磁盘重新核对当前冻结源码、工具、六个 Fresh 树和全部 receipt：

```
MoE full-model matrix RESUME 1x4 verified
MoE full-model matrix RESUME 4x1 verified
MoE full-model matrix RESUME 2x2 verified
```

## Fail-closed 诊断

首次从共享可变 worktree 启动的 `/tmp/p3-3-moe-m1-shard-a-1x4-4x1-2x2` 不计发布证据。`1x4 fresh0` 虽完成原生模拟，但 runner 检出 `artifact_manifest.py` 在运行中变化并拒绝生成 verified receipt。随后所有 case 均在新 detached freeze 中从头双 Fresh 重跑，没有复用该目录。

本子批只发布 M1 的 `1x4, 4x1, 2x2`；未缩小模型或跳过 Die。其余 `2x3, 3x2, 3x3, 10x10` 由后续冻结 shard 完成。
