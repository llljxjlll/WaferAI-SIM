# P3.4-AQ MoE 输出头反向与共享梯度入口

状态：EP1 两层 MoE 的两个独立 step 都从实际 CE dLogits 继续生成 LM-head 参数 FP32 dWeight（GEMM_WEIGHT_WGRAD 0x25）与 FP16 dHidden（GEMM_DX 0x26）。dWeight 的 source_parameter_state_ref 指向真实 LM-head trainable StateDecl，dHidden 消耗原有权重和 CE dLogits，不制造伪上游。两步 IR0 各 33 节点，经过官方物理 IR1→N4→schedule→GlobalActionDAG→N5，均 55 leaves/224 records，分别恰一条 CE_BACKWARD、0x25、0x26。开发期 NpuSim full physical PASS 原始根 `/tmp/moe_ep1_head_backward_b/`，其两步 finalizer、ProgramIO resolver、one-shot NpuSim 完成；`/tmp/moe_ep1_head_backward_a/` 的首错是 runner 通过 values 末尾位置找 dLoss，在插入 head 输出后错误；已改成语义 value ID 查找。当前单测覆盖两步 dLogits→head 梯度类型、producer、缺 CE 反向拒绝。

这仍是**部分反向**：dHidden 尚未沿 final norm/residual 传播到 layer1 combine；专家/动态 router/backbone 的梯度和 SGD HBM 写回还没有物理记录。NpuSim timing primitive 的输出不能作为数值梯度正确性证明。full-training gate 保持关闭。下一生产缺口是最终 norm dHidden→layer1 shared dCombined，再通过 MoE 专用 combine/score/expert/backbone 反向到 optimizer；现有 OpKind 仅有五种 MoE 前向节点，没有 combine backward 的官方 OpKind/N4 carrier，不能把 schema-only 0x28 算作执行。
