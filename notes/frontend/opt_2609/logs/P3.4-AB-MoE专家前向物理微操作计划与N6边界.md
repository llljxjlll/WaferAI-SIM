# P3.4-AB：MoE 专家前向物理微操作计划与 N6 边界

- 源：当前已提交的 EP1 双层 MoE TRAIN IR0→真实 Die0 IR1→官方 IR2→schedule→GlobalActionDAG。两层各一个 `moe_expert_forward` action；均来自实际 E2E source operation、冻结 route-trace digest、真实 runtime core 和五个完整 FP16 BufferBinding。
- 新 `plan_moe_expert_native_forward` 对每层严格导出四个独立的原生计算：gate MATMUL `(1,4,4,8)`、up MATMUL `(1,4,4,8)`、SWIGLU `(32,)`、down MATMUL `(1,4,8,4)`。gate/up 的 64 B 输出占互不重叠的 128 B concat；SWIGLU 输出另需 64 B activation scratch。down 仅将实际 activation 与 down weight 写至源 expert output，不复用权重或公开输出作 scratch。
- 两个 plan ID 不同，均绑定 source action/schedule/operation/trace、Die 与 runtime core；校验精确 BufferABI 字节数、FP16、全 tensor slice、无别名、五个精确 operand uses 和公开 action/schedule artifact ID。截短 up weight、复用 activation storage、伪造 action kind/Die 均拒绝。
- 验证：`PYTHONPATH=llm/test/frontend/unit:. python3 -m unittest llm.test.frontend.unit.test_moe_full_train_expert_microplan`，14 tests PASS（含实际 fixture 的继承测试）。
- 门禁仍关闭：本阶段只到 source-bound expert microplan，未生成完整 CommandFragment/NPUP，也未完成一次 full-model native 两步 SGD。当前公开 `CommandFragment.validate_against` 对 COMP 只允许 `SRAM_BIND + 单 opcode`，`LinkedProgramManifest.validate_against` 的每个 BufferABI 又必须对应 IR2 源 schedule/action use，不能事后插入无源的两个 scratch。后续须使专家的 gate/up/SwiGLU/down 子值进入可审计源 DAG/schedule，或设计并验证公共多记录 compute 契约；route-freeze/dispatch/combine、共享 dCombined、backbone backward 和两步参数状态链仍需贯通。
