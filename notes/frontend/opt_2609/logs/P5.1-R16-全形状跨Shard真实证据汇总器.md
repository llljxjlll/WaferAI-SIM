# P5.1-R16 全形状跨 shard 原生证据汇总器

## 目标与开发

新增 `aggregate_native_mesh_release.py`。对 Dense 或 MoE 的 1..10 共100个形状，检查每个 shard 的绑定文件原始 SHA、driver 和真实 workload runner 源码 SHA、工具/模拟配置的跨 shard 一致性、确定性的完整分片及重复索引；Dense 还校验其冻结 HBM DRAMSys 配置字节。逐 case 调用原族 `audit_cached_case`，重新检查两份独立 Fresh 的原生记录和已存证据后才给出100形状/200次执行汇总。缺 shard、重复、坏产物和源码漂移均拒绝；不存在用 schema 生成虚拟通过 case 的路径。

## 验证与当前范围

`python3 -m unittest llm.test.frontend.integration.test_aggregate_native_mesh_release`：5项通过，覆盖缺失、重复、工具/源码漂移、缺原生 case 文件；Dense/MoE 原矩阵审核器合计29项通过。汇总器尚未产生真实完整100形状发布收据；只有全部物理运行完成后才能调用汇总器发布 M3 对应族的 resident 子矩阵。

另以冻结源码6b9664be和固定原生工具快照，从全新目录 `/tmp/p5-dense-resident-v3-1x2-2x1-6b9664be` 实际完成 Dense resident 1×2 与2×1各双 Fresh，`matrix_receipt.json=status=verified`。两形状物理 Die 均为0、1，KV bytes各次为512→1024→1536，D2D in/out各192/192；1×2两次makespan均33681 cycles，2×1均33708 cycles。此次只是 Dense 100形状的2个子case，不能计作四族M3矩阵完成；1×5/5×1正在独立运行。
