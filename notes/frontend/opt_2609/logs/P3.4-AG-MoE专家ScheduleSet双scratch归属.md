# P3.4-AG：MoE 专家 ScheduleSet 双 scratch 归属

- 官方 `NaiveIntraDiePolicy` 对每个真实 `MOE_EXPERT_FORWARD` task 在 ScheduleSet 的 MoE 专属 sidecar 签出 `gate_up_concat` 和 `swiglu_activated` 两块独立 `OWNED` FP16 BufferBinding。身份锚定 source DAG/task、实际 runtime core、SRAM region、绝对偏移与大小；生命周期只覆盖该 task。普通非 MoE schedule 的身份保持原样。
- 所需空间从同一物理 core/region 已排程公开缓冲的最大末地址之后按真实 SRAM alignment 分配，检查 region/profile 容量、16-bit 地址和 bank interleave。`IntraDieSchedule.validate_against` 从 IR2 task、IR1 fabric 与公开 schedule 重算，缺一根、篡改偏移或注入 phantom root 都拒绝。专家微计划和物理片段现在使用 sidecar 签出的 binding，而非自行捏造临时地址。
- 验证：IR2 schema、naive intra-die、MoE expert、MoE route-native 共 90/90 PASS，96.612 s；缺根和偏移伪造负测 PASS。**专家 12-record 片段尚未通过 GlobalAction/LinkedManifest 生命周期与地址闭合；ScheduleSet sidecar 只完成物理 scratch 归属。**完整 full-model linked/ProgramIO/NPUP/native 两步仍未完成，M1 MoE full TRAIN 门禁关闭。
