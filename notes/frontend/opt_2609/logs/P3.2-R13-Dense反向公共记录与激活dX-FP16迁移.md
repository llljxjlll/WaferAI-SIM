# P3.2-R13：Dense 反向公共记录与激活 dX FP16 合同迁移

本阶段只交付单 core 公开 timing primitive 链，**不构成完整 Dense 训练完成证据**。`RMSNORM_BACKWARD_TIMING`、`ATTENTION_BACKWARD_TIMING`、`ROPE_BACKWARD_TIMING`、`RESIDUAL_BACKWARD_TIMING` 固定为 0x29–0x2C；同步 C++ Opcode、Prim78/79、manifest、200B 严格 record codec、lowering、config helper、native finalizer 和 Python artifact schema。Norm/Attention 复用具名 Prim；RoPE/Residual 接入原有的真实物理工作量，而不是 MATMUL 替代。四条记录具备精确参数、物理源输入与上游梯度地址；Residual 有独立的左/右 FP16 输出及正向输入 witness。所有 SRAM extent、dtype、对齐、寿命和地址重叠均 fail closed；RoPE position trace 只允许 30-bit tag 且与确定性 trace 的低 30 位匹配。

同时迁移旧 0x26 `GEMM_DX_TIMING`：FP16 权重、FP16 上游梯度、**FP16 激活 dX**，输出 BufferABI 精确为 `2*K*M` 字节。原 FP32 dX 字段及 `4*K*M` extent 不兼容，codec、Prim 物理工作、finalizer、Python artifact/workload schema 和 ProgramIO 均明确拒绝旧 artifact。参数梯度 WGRAD 和 DP reduction 的 FP32 合同不变。并行的 Dense full-training owner 已在同一共享工作树把旧 head 源图及专项测试迁至 FP16；共享 index 的并发提交将其四个精确路径与公共 31 路径一并收入 `22db6424`。已核对 35 个路径无 pager/MoE 活跃文件，并在该提交后重跑四个旧源图专项套件共 10 tests 全绿；完整 Dense 全链时间线仍待独立验收。

验证（`/tmp/moe-public-router-build`，2026-09-16）：

- `npusim --isa-v1-selftest` exit 0：manifest/factory 1333、record codec 1482、Prim wire 608、backward timing 17、GEMM input dX FP16 12、program format 669、record lowering 458、config helper 207 checks 均 PASS；完整输出 `/tmp/p32-dense-backward-isa-selftest-final.log`。
- `npusim_program_finalizer_selftest` exit 0；`npusim_program_io_selftest` exit 0，8/8 checks；日志 `/tmp/p32-dense-backward-finalizer-selftest-final.log`、`/tmp/p32-dense-backward-program-io-selftest-final.log`。负测含缺参、非法 dtype、短/旧 extent、重叠、溢出、非规范 aux、错误 trace、Residual 双输出别名。0x26 特别拒绝 FP32 dtype 和 128B 旧输出。
- `run_public_dense_backward_fragment_canary.py`：emitter→LinkedProgramManifest→native finalizer→ProgramIO resolver→NpuSim 均 exit 0；source/tool SHA 固定，13 次初始化、5 个 owned FP16 根精确 probe，四个 `[DENSE_BACKWARD_PUBLIC] stage=...pass=1`，ProgramIO resolved/applied/verify pass、drain residual 0、makespan 286 cycles。零化签名非零源输入的负测拒绝。manifest `linked_program_manifest_718a05e3996f23b8`、artifact SHA `21ccf2d2d3fabbd17293d2efc91817e920292d333a45fe3302100c55524112d7`；完整证据 `/tmp/p32-dense-backward-public-canary-v8/dense_backward.evidence.json`。
- `run_public_gemm_dx_fragment_canary.py`：0x26 FP16 具名 StateABI HBM LOAD 和非零 dY 经 ProgramIO→NpuSim exit 0，3 次初始化、1 个 64B 精确 probe，零化源输入拒绝，makespan 199 cycles；artifact SHA `2960a499786ee0598b2ba119fb89a6964516cbb5fe664b20e2c05fe9d7e04579`；证据 `/tmp/p32-gemm-dx-fp16-canary-final.log`。

这两条 canary 的 `scope` 限定为公开 primitive 及源约束片段；`full_dense_training_complete=false`。完整 Dense 反向 typed IR、跨 rank 梯度闭环和总训练时间线仍需后续独立验收。
