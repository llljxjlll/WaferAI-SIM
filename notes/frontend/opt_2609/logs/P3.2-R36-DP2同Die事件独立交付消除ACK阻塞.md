# P3.2-R36：DP2 同 Die 状态事件独立交付消除 ACK 阻塞

日期：2026-09-17。源码提交 `a0036ce5`。本记录是 C++ 传输修复与**单个已签名程序的诊断重放**；两次从干净源码重新编译/运行的正式 Fresh 尚待后续记录，不以此宣布 M1 通过。

## 原生首阻断与因果定位

带 22 对跨 core `STATE_IO STORE→EVENT_SET / EVENT_WAIT→LOAD` 的第一套 `/tmp/dense_train_dp2_20260917_fenced_l` 完成 finalizer、ProgramIO resolver，真实 NpuSim 于 cycle 50,705 触发 `[PROTO_WAIT]`：router8 的 core9→8 EVENT 滞留输入，router8/12 的旧 P2P ACK 滞留输出；`event=18`、`dte_tokens=7` 未排空。stdout SHA256 `7d9d869ce86f22f8d9d7f1f5555738832d6f7cdb2f11a1dc327ef4e8515a3005`。单靠源图先执行四组 `DTE_RECV/DTE_WAIT` 的 `/tmp/dense_train_dp2_20260917_drain_diag` 在旧工具上仍于同一 cycle 看门狗失败，stdout SHA256 `752129f652a2aa4e6cd8cda5715edb23ebadc6548db289c6a307ec2ce64ea1b0`；`DTE_WAIT` 的本地完成不等于旧控制通道 ACK 全排空。

根因是旧 `MAX_BUFFER_PACKET_SIZE=3` 控制接收队列：ACK 满时整个 core 的控制可用信号失效，正等待的 EVENT 无法送达。修改在 Monitor 给所有 core 注入一份共享、按物理 Die 限定的事件运行时；同 Die `EVENT_SET` 经一周期本地事件通道送入目标 core 的定界 mailbox，`EVENT_WAIT` 按 source/destination/tag 消耗 credit。跨 Die EVENT 保持原有路由。错误端点和跨 Die误投递被拒，最终 residual 被纳入仿真器排空门禁。该路径保留原有 `EVENT_SET/WAIT` signed record 与先 STORE 完成、后 LOAD 开始的执行顺序。

## 构建、测试和真实重放

最终 NpuSim SHA256 `0fac89f5de54bebba49d94e18f229652615c4636278945035340ab49059e03de`，冻结于 `/tmp/dense-dp2-local-event-tools-final-20260917/bin`；同目录 finalizer / resolver SHA256 为 `d2ff7a75b8a0463ffe2f9b594edb3315dc93d29a3763d32f10b39d2fc7ddd949` / `dd8b95162f43e8a279f61d85562734ae2838f0e38cdb7d17c1e470b67df65a58`。`npusim --sync-runtime-selftest` 59 checks PASS，新增 wait-first、错 Die 拒绝与 credit 排空自测；C++ build 和 diff whitespace check 通过。

直接用此前 drain_diag 的签名 linked / NPUP / ProgramIO 字节，不重新生成程序，仅替换 NpuSim 原生重放：三文件 SHA256 依次 `a66491b918517cdbda21330cdb94a6ec245b9ee3e179281bd5fed0245cf28c3d`、`371ab3b7e176251caddd9ca6085899ff0230777b4effad35c0fa5e8b5eeacebc`、`ab9293ca988ac53a2b17f485d0dde033894ae81f0086256d9be060725c177159`。最终 stdout `/tmp/dense_train_dp2_20260917_drain_diag/npusim_event_final_probe.stdout.txt` SHA256 `ef6bb64c7495a582e00beb72af711c47c89066013123859e8aa96f1f3505dea1`，exit 0、43,416 cycles、`TRAIN_SGD=120`、ProgramIO verify pass、`PROTO_WAIT=0`、`COLL_DRAIN endpoints=0 dte_tokens=0 event=0`，所有 10 个活跃 core 的 LSU 完成数等于 issued 数。

此成功归于本地事件交付修复；源图接收顺序调整本身已被上述旧工具负例证明不足。下一步须从固定源码和固定三工具做两个独立全 Fresh，重新核对 source/NPUP/ProgramIO/物理状态版本门禁。
