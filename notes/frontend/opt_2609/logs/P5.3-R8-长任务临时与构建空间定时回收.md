# P5.3-R8：长任务期间的临时与构建空间定时回收

日期：2026-09-18。继续 M1–M3 开发时，确认 Dense 全 Die 2×6、2×7 的 Embedding 整表暂存仍受 64 KiB SRAM 地址窗口阻断；分段方案必须同时调整 lookup 的索引/地址语义，不能只拆 DMA。本阶段不宣称这两个形状已运行成功。

本轮先加固开发过程的磁盘限额：`scripts/frontend_tmp.py` 对运行中的受管 scratch/evidence 作业每 5 秒重新扫描容量，并在每次检查前清理同一受管根里已到期的失败任务。超过 `--max-gib` 的原生/构建进程组被终止，任务按现有失败 TTL 清理；成功 scratch 与受管构建仍在退出时立即删除，待审阅 evidence 仍需显式 `release`。清理范围保持为有合法标记的 job，活跃作业、未标记目录和已有冻结构建树不会被压力清理。

回归：在 1 GiB 受管 scratch 内运行 `python3 -m unittest llm.test.frontend.unit.test_frontend_tmp llm.test.frontend.unit.test_frontend_cmake`，14 项通过。新增测试证明超额且仍在睡眠的构建任务在 12 秒内停止并回收，以及长任务进行期间过期的失败 sibling 被回收。`git diff --check` 通过。阶段末对 `/tmp/waferai-frontend-managed` 和 `/workspace/build-managed` 执行 `prune --apply`：前者 76,530,351 B，后者 23 B，均无符合删除条件的 job；前者主要是此前待审阅的 held evidence，没有误删。

后续 M3 工作仍需为 Embedding 建立合法 token/row tile 的 HBM→SRAM 搬运及局部索引合同，再以相同模型、相同硬件在 2×6、2×7 双 Fresh 验证，并从新冻结源码重跑全形状审计。本阶段清理机制仅控制开发磁盘占用，不改变负载支持状态。
