# P5.1-R41：Dense 全 Die 单 Die输出生存期与重启续跑

日期：2026-09-17。此记录是失败诊断与严格续跑记录，**不是** M3 全 Die 发布成功证明。

## 真实 1×1 阻塞

在冻结源码 `/tmp/p5-dense-all-dies-frozen-01e33744` 和原生工具 `/tmp/p4-dense-rect-fe454f69-frozen-tools/bin` 下，全 Die scaled 两层 Dense 1×1 的 Prefill 段完成计算后，NpuSim 返回 `Dense sequence ProgramIo segment verification failed`，因此没有完成 Decode 或双 Fresh，也没有把它计入发布矩阵。

为保留失败原件，主机重启后将中断目录移动到 `/tmp/p5-dense-20260917-reboot-interrupted/F0-1x1-fresh0`；原三段 NPUP、linked manifest、ProgramIO 和硬件配置未改。用加入只读探针诊断的本地 NpuSim 重新执行原段，`/tmp/p5-dense-all-dies-1x1-programio-debug.stdout` 显示 `index=0`、`P0.logits`、core 0、地址 4192、256B，expected/actual SHA256 同为 `5341e6b2646979a70e57653007a1f310169421ec9bdd9f1a5648f75ade005af1`，`exact=1 valid=0`，进程 exit 1。该本地二进制是诊断工具，不能替代冻结原生工具的正式收据。链接 manifest 的 logits fragment 同时包含 `SRAM_ALLOC_AT`、GEMM 和 `SRAM_FREE`；终端 ProgramIO probe 的物理 SRAM 生命周期须继续核查与修正。其他形状即使零值探针通过，也不能仅凭零值匹配推断该终端 buffer 在段末仍有效。

诊断源码提交 `00978582`：仅在原有 Verify 失败分支输出 probe ID、核号、地址、字数、预期/实际 SHA 与 validity；`cmake --build build-moe-training-grad --target npusim -j 4` 构建通过。修复需重新物化并用新工具做独立双 Fresh；当前全 Die 1×1 **未通过**。

## 重启后的矩阵边界

宿主约 08:30 UTC 重启；通过 `/proc` 中进程不存在与 `uptime` 确认全部旧任务已停止。对每个已完成 case 的两份 Fresh 严格重开；8 份不完整 Fresh 保留在 `/tmp/p5-dense-20260917-reboot-interrupted`（manifest 逐项记录原路径），仅移出原根目录，绝不作为成功样本。冻结源码、工具和原 `matrix_binding.json` 不变，四个 Dense resident mixed 分片在 `--resume` 下逐项打印 `RESUME ... verified` 并恢复 100 形状运行；全 Die scaled 分片 1、2、3 同法恢复，分片 0 在单 Die 修复之前保持未发布。两个 profile 必须分别统计：mixed 允许六个计算 Die 与其余路由 Die，全 Die scaled 要求每个物理 Die 计算活跃。任何尚在运行的分片均不得记为完成。

## 暂存空间

按用户要求清理 `/tmp`：删除已停用的 ASAN/CMake 构建树、旧 MoE 阶段中间冻结副本、被取代的编译实验快照；`/tmp` 从约 90 GiB 降为 51 GiB。保留所有正在运行矩阵根、当前 frozen 源/原生工具、完整 Dense AdamW 与 MoE 双 Fresh、以及该失败诊断与重启中断原件。正在运行的矩阵进程经清理后仍存活。
