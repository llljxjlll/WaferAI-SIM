# P3.4-AS MoE 真实 0x28 与动态路由 FP32 梯度

状态：EP1 两层模型 step0、step1 的 layer1 反向从同一真实 forward 0x27 weighted combine 的 route/score/expert tape，和上游 final norm→residual2 的 dCombined，生产 0x28 native dScore/dExpert；再由同层 router activation + 0x28 dScore 生成 gate StateDecl 对应的 0x25 FP32 WGRAD。IR0 新节点不能借用静态 gate=1 元数据，严格要求 step/layer/route digest、真实 residual dCombined producer、六个不同 SRAM storage、4 input/2 output 地址及全量 relocation/BufferABI 闭合。action carrier、IR1、N4、schedule、lowerer、linker、finalizer、ProgramIO 与 NpuSim 原生路径均已走通；N4/IR2 policy 未修改。

原始诊断根 /tmp/moe_ep1_combine_backward_{a,b,c,d,e,f}/ 逐步保留 fail-closed 首错；f 两步 0x28 NpuSim PASS，每步 59 leaves/244 records，恰一条 0x28。再接 router 0x25 的原始根 /tmp/moe_ep1_router_wgrad_a/ 两步 NpuSim PASS，每步 60 leaves/248 records，恰一条 0x28、两条 0x25（LM-head 与 router 各一条）。router 的 0x25 tuple (H,E,M)=(4,1,4)；两个 step 的 IR0/linked/npup/ProgramIO 均不同。ProgramIO 用 signed route HBM 与独立非零 dLoss seed；NpuSim timing 的 loss probe 不能证明数值梯度。单测验证 route/score/expert/dCombined→dScore/dExpert 的源关系、坏 route digest/缺 dCombined 拒绝，以及 dScore→真实 gate state 0x25。

完整 TRAIN 门禁仍关闭。当前 dExpert 是 terminal；没有 expert gate/up/down 的保存中间值反向、三投影 WGRAD、layer1/0 shared backbone 其余反向、gradient sync 或任何 SGD HBM write。step1 参数仍是 host timing seed，不是 step0 物理 SGD 写回。下一最小生产工作是保存 expert 前向中间激活并把 0x28 dExpert 接真实 down/SwiGLU/gate/up 反向与三个参数 WGRAD；此后沿 layer1 残差分叉传回 layer0，并接 19 个 trainable 状态的同步/SGD 版本链。本阶段新源码提交后需 fresh 重跑，开发诊断不计冻结 full-train 证据。

提交后冻结：精确提交 1005aeef，在 clean detached /tmp/moe_router_freeze 运行两个独立新根 /tmp/moe_ep1_router_wgrad_fresh0/ 和 /tmp/moe_ep1_router_wgrad_fresh1/。两份 receipt 均为 router_wgrad_physical_partial、full_training_gate=closed；20 项 source SHA、finalizer/resolver/NpuSim binary、simulation/hardware SHA 与两步 IR0/linked/npup/ProgramIO SHA 精确一致。每步 60 leaves/248 records，1 条 0x28、2 条 0x25；NpuSim PROGRAM_IO verify、PROGRAM_MEMORY、CREDIT/DRAIN 通过。日志 SHA 因运行时间项而不同，保留原始 log，未要求字节相同。108 项相关 schema/manifest/MoE 回归中 106 PASS，2 个旧断言失败；在不含本轮 0x28 改动的 clean baseline /tmp/moe_router_baseline（e20b590b）精确复现同两项失败，原始输出 /tmp/moe_router_native_regression.log、/tmp/moe_router_baseline_failures.log。
