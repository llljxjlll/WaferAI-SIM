# P5.1-R6：3×3 全九 Die Dense 推理真实运行

> **后续校正（2026-09-15）**：本记录的Dense sequence真实运行使用native `x=4` 且缺`y`（原生4×4核/Die），而编译Fabric为2×2核/Die；global core ID被解释为错误物理Die。原exit0/KV/周期是该错误硬件下的历史观测，不能计入目标矩形物理Die或跨Die路由验收。详见[P1.1-R1 核几何审计](P1.1-R1-前端原生核几何失配与矩形物理路由修正.md)；正确几何的全新双跑另计。

日期：2026-09-15。延续 [R4 64KiB 计算地址窗口修正](P5.1-R4-TP9计算地址窗口与SRAM生命周期重用.md)。本记录区分已成功的首轮原生仿真与正在执行的第二轮独立 fresh；两轮均已真实执行并通过独立比较，本单案例 **repeatability_verified=true**；四family M1/M2/M3总体仍未完成。

## 冻结运行合同

精确 detached 源码提交 `392a8207`，工作树 `/tmp/m1-tp9-source-freeze`，运行前clean，finalizer、ProgramIO resolver、NpuSim、simulation配置 SHA256 依次 `76a7e61d8b7fd4f49d225d5927d224b6198de9ddf9f1ba397dbe51d314dd25d8`、`29e8fb38cace40beca5efdfba53ae520f04e71d088ffdc4648a868f7bfb03763`、`1090f93f8eb787a117804e33622a9a53dc656b2098ccbb29e1ed612f90bab5cd`、`337f5bc3f195489e8c6ef8830d83ce709a3e79a434401bd545504bf543420afb`。固定主负载 `workload_case_08e8a2359dead3c4` 为两层Dense FP16、TP9、H144/I144/NH9/KVH9/DH16、每Die一个rank、9request、Prefill1→Decode1→Decode1；100个矩形源case仍只是L0/L1，不能外推为真实runtime覆盖。

每Die物理SRAM128KiB、allocation alignment32B，计算record绝对地址必须小于64KiB；驻留HBM由生产preflight绑定。两轮均从不存在的独立空结果目录运行原生产 `compile_dense_e2e_sequence_runtime_profiles→finalizer→ProgramIO resolver→NpuSim --program-sequence`，没有将三段分开计时相加为模型 makespan。

## 首轮真实证据

首轮 `/tmp/m1-dense-scaled-all9-3x3-wire64k-fresh0` **native exit0、runner验收PASS**。生产编译301.295s、前端峰值736,112KiB，三段linked每段14MiB、NPUP约657/660/660KiB，compiled core stream每段均恰好覆盖Die0..8。三段 finalizer/ProgramIO resolver 的产物、报告和stdout保留；最终 `compiled_receipt.json` runtime_status=`verified`、总wall1345.538s、source_tool binding SHA `ca16787c7328375ef2c080966d748be381a2b7543b81e4e26835b94470d81006`。三段 NPUP SHA256 依次为 `16bb377e2f9b0c8d16137801f0bea49ebdef4ef4ef0eee623e05ea1030b8a79e`、`5d2a3a30d1a6e779907e75ebdd46c6fb11376cb4f6c3fa6f672e86819b63bf0a`、`d9c96fd1c037f0cfa51f2d9d898f5d91a2f0221ebe6456081704cc880750487c`，sequence digest `b6c73040852934f767ee6dbd02ba6526aac3cb23cf2a1e9060fd818038f423d8`。

对生产linked的三个段各1377条、合计4131条 `SRAM_ALLOC_AT` 独立解析：有效对齐32/64B全部满足、无地址越64KiB，三段最大结束地址48,704/47,552/47,552B。真实 NpuSim stdout中 `[DENSE_SEQUENCE_SEGMENT]` index0/1/2 final=0/0/1、KV字节边界10,368→20,736→31,104且各pass=1、`[DENSE_SEQUENCE_DRAIN] segments=3 one_shot=1`、唯一 `[SIM_RESULT] makespan_cycles=481454`。`[D2D_DATA] in_pkts=24192 out_pkts=24192` 与非零跨Die link流量证明不是九份独立单Die循环。该仿真仍是 timing_execution；这些证据验证时序/字节/状态边界，不能声明GEMM的数值 logits正确性。

## 第二轮与里程碑状态

第二轮 `/tmp/m1-dense-scaled-all9-3x3-wire64k-fresh1` 从另一个空目录独立完成完整物化和真实 NpuSim，runner exit0、`compiled_receipt.runtime_status=verified`。生产编译322.629s、总wall1419.272s、前端峰值737,408KiB。两轮的case/source request/加载Python和工具SHA、sequence、linked manifest、全部三段NPUP、ProgramIO sidecar、硬件SHA均相等；逐轮真实KV边界及三个digest完全相同，`[D2D_DATA]` 均为in/out各24,192包，唯一`[SIM_RESULT]`均为481,454 cycles。这个 **shape-scaled Dense inference 3×3 单案例** 的双独立运行现已verified。这里的模型按形状放大且只覆盖Dense inference 3×3；计划M1主矩阵四family×8形状×双跑、M2低HBM完整offload和M3全100形状/扩展范围均不因该单案例完成。
