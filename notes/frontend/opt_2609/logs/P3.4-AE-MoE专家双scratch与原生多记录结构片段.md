# P3.4-AE：MoE 专家双 scratch 与原生多记录结构片段

- EP1 双层专家源动作从实际 IR1 Die/core/region 与官方 ScheduleSet 派生两个非重叠 `OWNED` SRAM scratch root，保留五个源输入/输出 BufferABI、准确 MATMUL `(1,4,4,8)` / `(1,4,8,4)` 和 SwiGLU 32 元素。
- 每层实际发出 12 条 N6 `CommandFragment` 记录：两条 `SRAM_ALLOC_AT`、四组 `SRAM_BIND` + `MATMUL/MATMUL/SWIGLU/MATMUL`、两条 `SRAM_FREE`；record 地址、up 输出 64 B 偏移与物理 scratch 区域对准。双片段均通过 `CommandFragment.validate()`。源集合 16/16 tests PASS，52.564 s。
- **尚不可链接**：N5 `GlobalAction` 的 COMP buffer uses 只暴露 4 个输入和 1 个输出，ScheduleSet 未声明两个内部 scratch 用途；`CommandFragment.validate_against` 正确以生命周期 coverage 拒绝该结构片段。必须把 MoE 专属 scratch 引入 N4/N5 schedule/use 与 linker 地址闭合，之后才能称其为真实全链专家执行。现无 full linked ProgramIO/NPUP/native 两步结果，M1 MoE full TRAIN 门禁继续关闭。
