# P5.2-R4：Dense 矩阵真实 Fresh 阶段开销汇总工具

日期：2026-09-17。源码提交 `649f7a88`。新增 `collect_dense_native_mesh_costs.py`，逐案复用矩阵驱动的 `audit_cached_case` 重开两次 Fresh 真实原生产物，然后从每次 `compiled_receipt.json` 读取生产编译、finalizer、ProgramIO、resolver、NpuSim 的实际 wall、前端/子进程 RSS、linked/NPUP/ProgramIO 文件字节与原生 makespan。缺失 case 保留在 `missing_shapes`；即使所提供的一个小矩阵已完结，报告仍固定 `release_completion_claim=false`，不自动升级为 100 形状验收。

真实样本：`/tmp/p5-dense-all-dies-1x4-01e33744` 的 1×4 全 Die 双 Fresh 已重新审计；报告 `/tmp/p5-dense-all-dies-1x4-costs-v1.json`，1 case/2 Fresh、0 missing。Fresh0 实测生产编译 58.920s、finalizer 三段合计 10.392s、ProgramIO 5.197s、resolver 7.205s、NpuSim 24.901s、整体 119.718s，前端峰值 504480 KiB、linked 三段合计 11107519B、NPUP 540912B、ProgramIO 392075B；原生 makespan 69829 cycles。宿主各阶段 wall 不能相加视为模拟 makespan。

验证：模块语法检查、真实双 Fresh 重开及 `git diff --check` 通过。完整 100 形状曲线需待四分片完成后汇总；本记录不宣称 M3 发布完成。
