# P3.4-AD：MoE 静态路由 HBM 读取与两层物理 LSU_LOAD

- full TRAIN IR0 双层各有独立的 `MOE_STATIC_ROUTE` STEP/READ_ONLY 状态与 INT32 `(4,5)` source tensor；它从同一 E2E/P2 frozen assignments 签出的 80 B 五字段 blob 派生。route-freeze 的两个真实输入为 FP16 router score 和该 HBM route source。StateAccess READ 对准每层 freeze，不能掉 state 或改成虚假的输出 producer。
- EP1/EP2 HBM layout 把两份 route state 置于 Die0、64 B 对齐且与所有 shared Dense、router、expert 参数互不重叠的独立地址；EP1/EP2 的物理 state manifest 分别增至 21/29 份声明和绑定，没有删除原先 11 shared + 8/16 expert/router 参数。
- 官方 IR1→IR2 投影将 route source 输入替换为真正的 DMA staging；route-freeze action 依赖对应 DMA。EP1 两层各由原生 `NaiveStateDmaLowering` 生成经 `CommandFragment.validate_against` 的 80 B `LSU_LOAD`，StateABI 为准确 Die0 HBM 地址。Die0 route home 碰撞、缺 route state 在源边界被拒。
- `bind_full_moe_route_state_program_io` 只接受真实 full linked manifest、有效 baseline ProgramIO、与 source/placement 相符的两份 StateABI 和已解析 HBM 绝对地址，并写入签名 route payload 的 `STATE` 初始化。**当前未有完整 full-model MoE N6 linked manifest，因此该入口尚未产生正向 ProgramIO/NPUP/native 两步运行证据。**禁止把孤立两份 LSU_LOAD 叶当作整模训练成功。
- 验证：`PYTHONPATH=llm/test/frontend/unit:. python3 -m unittest llm.test.frontend.unit.test_moe_full_train_forward_ir0 llm.test.frontend.unit.test_moe_full_train_ep_placement llm.test.frontend.unit.test_moe_full_train_ep_ir1_source llm.test.frontend.unit.test_moe_full_train_expert_microplan llm.test.frontend.unit.test_moe_full_train_route_table_source llm.test.frontend.unit.test_moe_full_train_route_state_native`，共享 action/IR2 提交后全套 96 tests PASS，269.977 s；公共状态/ProgramIO/N6 回归 39/39 PASS，10.769 s。
- 专家两个临时 root 现在绑定到实际 IR1 core/region，按真实 schedule 全部缓冲的最大末地址之后、64 B 对齐分配，并对物理 region、profile 容量与 16-bit SRAM ISA 地址界限 fail closed；所有权明确为 OWNED，双层 15 个专家微计划测试通过。此仍是源绑定微计划，不是可链接 N6 fragment。
- 剩余主链：route-freeze 的源内 static table→输出复制仍缺 N6 多记录/专用 lowering；dispatch、expert gate/up/SwiGLU/down 的独立 scratch 已规划但尚未进入正式 ScheduleSet/BufferABI/N6，多记录专家片段未生成；combine 及共享 dCombined/backbone backward/两步 SGD 完整 invocation 未完成。M1 MoE full TRAIN 门禁维持关闭。
