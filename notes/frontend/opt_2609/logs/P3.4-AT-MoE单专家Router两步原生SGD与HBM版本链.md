# P3.4-AT — EP1 router partial SGD 的两步原生 HBM 版本链

冻结源码 `7e6c7b6e434e1a8c8f2d95f02066da27fe27dc76`，clean detached worktree `/tmp/moe_router_sgd_freeze`；三件冻结工具均在 `build-moe-training-grad` 原生工作目录执行：finalizer `3011bac86bd7820a3cd7bb9e85b0251f751ffb61f79f716a7f0327cefbcffb9c`、resolver `d64cd70d84e5e9721dbe3a60949a8ae17f71d7d831ba9e9ec3ac9b0f6c0edfa2`、NpuSim `7e9887f579005ea4c2d18ba50b609f0b2ef3ce512ad91a106a4fd4804f7a6f67`。

独立全新目录 `/tmp/moe_router_sgd_fresh0` 和 `/tmp/moe_router_sgd_fresh1` 各自完成 step0、step1 的 IR0→IR1→N4→N5→linked manifest→finalizer→ProgramIO resolver→one-shot NpuSim，且在**同一个 NpuSim invocation** 中再执行严格两段 partial sequence。每步 63 leaves、254 records，包含真实 0x28 dScore、2 个 native FP32 0x25 WGRAD、1 个 0x20 SGD 和对应 8B gate HBM STORE。两套 fresh 均为 19 个非零 trainable HBM 种子、2 个静态 route state；跨段 952B 状态布局精确连续，step0 完成 STORE 后 step1 ProgramIO 仅补缺失状态并保持原 HBM，step1 入口 digest 精确等于 v1。原生累计 `lsu_hbm_write_bytes=16`、2 次 `[TRAIN_SGD]`、`segments=2 one_shot=1`，ProgramIO、credit、D2D drain 全通过。`/tmp/moe_router_sgd_double_fresh_audit.txt` 逐项重开 21 个 source SHA、工具 SHA、各 step artifact/ProgramIO/NpuSim log 与 sequence log，双 fresh 确定性产物相同，审计 PASS。单测 `test_moe_full_train_router_sgd_ir0.MoeRouterSgdSourceTest` 2/2 PASS；旧 forward-only sequence 真原生回归 PASS，新模式对旧 forward-only 产物和两种模式共开均拒绝。

实际状态 digest `v0=v1=v2=26a9dedf9bb3dfedc565d05c53acd7a1adce3a9f54c504eda8b4dfa6ed9f3c13`。这证明共享 HBM 中真实 native STORE 和随后 LOAD 的版本顺序，不证明参数数值发生变化。EP1 单专家路由分数导数可能为零；当前尚缺 expert gate/up/down 三投影真实 dGradient/WGRAD、expert dX、layer0/backbone 全反向与所有参数 SGD。状态为 `router_sgd_physical_partial`，full-training gate 严格 `closed`，不计 M1 完整训练。
