# P3.4-AA — MoE 单 Die 官方 IR2 调度与 N6 边界

状态：1×1 两层 step0 前向的真实 source-bound IR1 已进入官方 IR2、schedule、GlobalActionDAG；完整两步 TRAIN native 门禁保持关闭。

五类有类型 MoE 前向节点现在拥有严格 `ComputeContract`：router 接收 activation 与每个 EP gate weight；route-freeze 接收 scores；dispatch 接收 activation/route IDs 并输出每专家 activation；expert-forward 接收 activation 与 gate/up/down 三份真实权重；combine 接收每专家输出与 route IDs。workload.kind 与 OpKind 必须精确匹配。1×1 实测两层各五个 MoE compute task，共 49 个官方 IR2 task；伪造 operand role 或把 expert workload 贴成 router 都被拒绝。

调度器只对真实 `(TP1,EP1)` 单 rank group 的 rank0 `(0,0)` 启用完整 rank-local 视图。1×1 实测 `IR1.validate`、fusion、IR2 projection、`NaiveIntraDiePolicy.schedule`（一个 schedule）、`build_global_action_dag`（49 actions）通过；伪造为非 singleton 二轴 group 被拒绝。没有放开 1×2/EP2 的原有 owner-rank 投影约束。

验证：`PYTHONPATH=llm/test/frontend/unit:. python3 -m unittest llm.test.frontend.unit.test_moe_full_train_ep_ir1_source llm.test.frontend.unit.test_naive_project_to_ir2 llm.test.frontend.unit.test_naive_intra_die`，43 项 PASS；输出 `/tmp/moe_ep1_ir2_schedule_tests.log`。

下一真实边界：N6 `_compute_record_abi` 对 `moe_*` impl_ref 没有冻结公开 opcode 映射，`_lower_fragments` fail-closed。`moe_expert_forward` 对应多条 gate/up/down、SwiGLU 原生记录，不能伪装成单条 MATMUL；router freeze/dispatch/combine 亦需准确 route/BufferABI 数据流。完整 dCombined/backbone 反向、单次两步执行与双 Fresh/reopen 尚未实现。
