# P3.4-Z — MoE 单 Die 整模训练源门禁与 IR2 阻断

状态：1×1 两层两步的真实 E2E 参数需求与物理 owner 审核通过；整模 TRAIN native 门禁保持关闭。

整模源需求现在读取实际 mesh/EP/expert_count：EP1 每 step/layer 有一份 8B gate replica 和三份 expert projection，共 16 条跨两层两步的 MoE 参数要求；11 个 Dense shared 参数仍被保留，四个 Dense MLP StateDecl 仍必须排除。EP2 原有两份 gate replica/16B、共 32 条 MoE 参数要求不变。两种形状都检查生产 StateABI 在真实 Die 的唯一归属、E2E v0→v1→v2 版本和真实 P2 梯度/同步/SGD/store 引用。`source_ir0_replacement_materialized` 仍为 False；篡改为 True 或以物理 Dense MLP carrier 冒充 MoE 均被拒绝。

验证：`python3 -m unittest llm.test.frontend.unit.test_full_moe_shared_train_requirements`，19 项 PASS，输出 `/tmp/moe_ep1_requirements_tests.log`；新增 EP1 Dense MLP carrier 负测单独 PASS。

整模原生链实测探针：1×1 MoE 前向 IR0→官方 IR1 验证 PASS，`partition_ir1` PASS（0 fusion）。官方 `NaiveProjectToIR2.run` fail-closed：`moe_expert_forward` 尚无 compute carrier。标准 `place_train_forward_ir0` 也仅接收 DenseIR0Validator 接受的 Dense 源及 TrainPlacedIR1 包装。真实 shared dCombined、全 backbone/expert 反向、两步完整 SGD 及单次 NpuSim invocation 仍未产生；不能将已通过的四个独立 MoE leaf 或 Dense 训练 carrier 拼接冒充整模训练。
