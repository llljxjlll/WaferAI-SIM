# P5.1-R10：修复核几何后 1×2 Dense 全参与双 Fresh 真实运行

## 目标与结论

以核几何修复提交 `e1c3b5e66d60b505952551fcf76b103e122403ad` 的 clean detached worktree，补充 M3 100 形状矩阵中的 1×2 Dense resident inference 实际 case。两次从不同空目录、不同 Python 进程重新 materialize、编译两层 Prefill→Decode→Decode、运行三段 finalizer/ProgramIO resolver，并各自启动一次真实 NpuSim。输出为 `/tmp/m3-dense-geometry-1x2-fresh0` 和 `fresh1`；两个进程均 exit0、`runtime_status=verified`。

P5.1 的物理审核器直接检查 hardware JSON、原生 `[PROGRAM_MEMORY]`、`[D2D_LINK]`、KV marker 及每段 linked/NPUP/ProgramIO 字节 SHA。前端和原生每 Die 核网格均 `[2,2]`，runtime core0/4 实际落在物理 Die0/1；`die0→die1 E` 与 `die1→die0 W` 各有96个 data包，原生汇总 `D2D_DATA in/out=192/192`。两次 KV 字节均 `512→1024→1536` 且三段 `pass=1`，makespan均33,681 cycles。

| 证据 | fresh0 | fresh1 |
|---|---:|---:|
| 实际物理 Die | `(0,1)` | `(0,1)` |
| 编译 wall | 14.914s | 14.521s |
| 原生 NpuSim wall | 3.199s | 3.235s |
| 完整入口总 wall | 39.461s | 39.630s |
| source/tool binding SHA-256 | `2e2f2e1d780e0f58cbee2c9796791573190b76f1a6cb99584067b9b7a97aff18` | 同左 |
| sequence digest | `b0d85da44d1f39e2d1344ca3fbdff7d31c1655e95dfcf0f36ea797b7d2824f36` | 同左 |

模型 request SHA-256 为 `a6f014da2197af04327ca0aac663664a333323ecd8f14c467a61308b60511297`，case ID `workload_case_6015307fdfce6d2f`。双 Fresh 的硬件、三段 linked manifest、NPUP、ProgramIO、KV digest 与观测语义完全相同。此记录只完成 **M3 Dense resident inference 100形状矩阵中的1×2一项**；不能用它推导其余99形状、offload、Dense训练或MoE两族。后续由同一矩阵驱动逐形状运行，并按资源预算分片与断点续跑。
