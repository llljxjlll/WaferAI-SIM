# P3.4-AV：MoE EP1 专家反向物理结构双 Fresh；数值与全训练门禁继续关闭

状态：**partial native pass，M1/M2/M3 full-training gate closed**。本阶段接在 `88f82481` 的同层真实 `dExpert` IR0 源之后，补齐了层 1 专家 gate/up/down 反向的官方 IR1→N4→N5→N6、私有 SRAM scratch 生命周期、21 条精确原生记录、linked BufferABI/relocation、native finalizer、ProgramIO resolver、NpuSim。两步均是各自独立的 one-shot；未把它们描述成同一 invocation 内的两个 SGD step。

源码提交：`2304b0d5`（ProgramIO 五个专家 scratch）、`85e8afee`（只含 MoE 的 artifact manifest 六个 hunk）、`7d6fd5b9`（专家反向 lowerer、linker/lifecycle、IR2 scratch、finalizer、runner 与负测）、`2fd1aabf`（runner 冻结源码/工具/CWD receipt）、`11e1b103`（只读双 Fresh reopen auditor）。共享 `artifact_manifest.py` 的 DP2 STATE_IO hunk由 Dense agent 在此后独立提交，不在本次冻结源码内。

最终冻结：`2fd1aabfd9ff2d0a6b244bd55cce0eb60ad59b41`，clean detached tree `/tmp/moe_expert_backward_freeze_bound`。三个硬链接冻结工具存于 `/tmp/moe_expert_backward_tools`，SHA256 分别为 finalizer `c7a4e59dd36b3bf2d7c03b783724406648f002cfcd64db2618eca71f7cb631e2`、resolver `8f7c2db8342d1d56557d68a40b4ff2a4086e3174bc31cab6865b229b96b89976`、NpuSim `5eb6fbaec4e979edf2f4735ee2067d12f1b41f00ecdbba22c12cfe805eac04e5`。`native_runtime_cwd` 绑定为 `/tmp/moe_expert_backward_tools`。

独立 Fresh 根：

- `/tmp/moe_expert_backward_bound_fresh0`
- `/tmp/moe_expert_backward_bound_fresh1`

两根各从零执行 step0 与 step1 完整 materialize→finalizer→resolver→NpuSim；共 **4 次真实原生 one-shot 执行**，每根 17 个产物。每个 step：61 leaf、293 records、5×`GEMM_WEIGHT_WGRAD_TIMING`、4×`GEMM_DX_TIMING`（含头部一条与专家三条）、1×`SWIGLU_BACKWARD_TIMING`、1×`LOCAL_REDUCE`、1×`MOE_SCORE_WEIGHT_BACKWARD`，0×SGD。ProgramIO 98 个初始化、9 个 exact output probe；其中专家 `dX` 与 gate/up/down 三个 FP32 WGRAD 各有一个物理 probe。NpuSim 每步 `PROGRAM_MEMORY core=0`：22 个 LSU LOAD 全完成，HBM 读 1240 B、写 0 B，`PROGRAM_IO phase=verify pass=1`、credit 平衡和 D2D drain 归零。两 Fresh 的 step0/step1 linked IDs 精确一致。

只读统一审计：`/tmp/moe_expert_backward_bound_double_fresh_audit.json` 为 `pass_partial_only`；reopen 两根全部 17 产物/根，重新比对 29 个显式源码文件、338 个入口及 348 个出口导入 Python 文件的磁盘 SHA、三个工具 SHA、clean commit、实际 NpuSim cwd、hardware/simulation 配置、每个 linked opcode 数量、NPUP、ProgramIO、finalizer/resolver/NpuSim 日志。另用独立拷贝篡改 `ir2.py` 导入源码哈希和 step1 NPUP 字节，审计均 fail closed，日志分别 `/tmp/moe_expert_backward_tamper_source.log` 与 `/tmp/moe_expert_backward_tamper_artifact.log`。focused `test_moe_full_train_expert_backward_native` 13/13（含 fixture 与 relocation/bind 负测）、typed expert/IR2 59/59 通过。

**数值门禁的首个确定性阻断**：当前 0x25/0x26 native timing primitives 的 `skip_input=true`、`skip_output=true`，见 `llm/src/prims/comp_prims/gemm_weight_wgrad_timing_prim.cpp` 与 `gemm_input_dx_npu_prim.cpp`。当前九个 ProgramIO 输出探针的期望 blob 均为全零，包括四个专家反向输出。故此双 Fresh 证明真实源图和物理记录/地址/调度/外存加载能执行，**不能证明实际梯度数值、物理 producer→consumer 梯度字节写入或参数变化**。此外尚缺层 0 反向、全部专家/共享/backbone 状态 SGD、step0 STORE→step1 LOAD 同一 HBMRuntime 的单调用版本链。下一阶段需先给 backward/gradient prim 实际读写字节语义，再接层 0 与全状态更新；M2 AdamW/offload 仍未进入本证据范围。
