# P3.4-BA：MoE layer1 四参数 SGD 共享 HBM 两步原生双 Fresh

状态：**partial only**。这份证据不计 M1 MoE full training；`full_training=0`、`functional=0`、`numeric_gradient_witness=false`。

冻结源码 `2896614fc0a90a1a7d529e3e40dbc94db91f7e31`，工具目录 `/tmp/moe_layer1_sgd_seq_tools_bound`，NpuSim SHA-256 `e8e1870fbd67280b47028491bcffda3d0d8cb2183886c1acf5a8a398adab8971`。两个独立全新输出根为 `/tmp/moe_layer1_sgd_sequence_fresh0` 和 `/tmp/moe_layer1_sgd_sequence_fresh1`，每根 18 个 reopen 产物；严格审计 `/tmp/moe_layer1_sgd_sequence_double_fresh_audit.json` 报 `pass_partial_only`。每次生成 step0/step1 完整物化、finalizer、ProgramIO、物理 NpuSim one-shot，并由单次 NpuSim invocation 在同一 HBMRuntime 中顺序运行两个 program。审计统计共 6 次 native execution、5 次 native invocation（含严格 reopen 检查）。

单次共享 HBM 序列包含 layer1 router gate 与 expert gate/up/down 四个 source-bound SGD 和四个 HBM STORE，每步 340 个物理记录。四个状态的 linked StateABI 均为真实 READ_WRITE 参数状态（8、64、64、64 字节），step0 STORE 先于 step1 LOAD；运行日志记录 `version=0→1→2`，每个版本为 952 字节。两次 Fresh 的前后状态 SHA-256 均为 `26a9dedf9bb3dfedc565d05c53acd7a1adce3a9f54c504eda8b4dfa6ed9f3c13`，`content_changed=0`。这证明版本与物理读写顺序，并**不**证明参数值更新：上游 0x25/0x26 timing primitive 的输入/输出 skip 语义令当前梯度数值为零。所有运行均显式打印 `functional=0 full_training=0`。

仍缺 layer1 attention 与投影反向、layer0 全链反向、共享 backbone 梯度合流、其余所有可训练参数的源绑定 SGD，以及数值有效的梯度与 loss 证据。下一阶段沿这些缺口推进，不把四参数 partial 形状扩展当作完整训练。
