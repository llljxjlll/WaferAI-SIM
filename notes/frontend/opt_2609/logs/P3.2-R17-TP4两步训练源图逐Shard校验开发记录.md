# P3.2-R17 TP4 两步训练源图逐 shard 校验

日期：2026-09-16。范围：`passes/validate_ir0.py` 的完整 Dense TRAIN 源图校验，以及 `test_full_dense_training_tp_shard_validator.py` 正反验收。本记录仅说明 IR0 源图校验，不把尚未执行的 TP4 原生训练记作 M1 通过。

在实际 `build_flexible_dense_train_plan(_spec(1,4), RectMeshSpec(1,4))` 的双层完整训练图中，校验器已接受 382 个节点、60 个独立参数状态、432 条 StateAccess 的两步 TP4 源图。每个 step/parameter/rank 必须有唯一的 FP32 WGRAD、匹配的 FP16 SGD 更新、rank 相同的原生状态读写和从 step0 STORE 到 step1 参数 READ 的控制边；重复、缺失、错误 rank/状态均拒绝。仅允许由真实列分片前向 GEMM、实际每 rank 参数 READ 产生的 partial 输入梯度进入逆向 SUM ReduceScatter，CE 仅允许一致的 TP 行分片，SGD 的每 rank 别名须与节点 shard 一致。

六项新测试覆盖 1×1 旧双步路径、生产 TP4 双步路径、缺失 tp3 梯度读取、tp3 更新错绑 tp0 状态、漏掉 tp3 状态版本边和重复版本边，均通过。原有完整 Dense 反向、SGD、两步源图与 1×1 原生链接/ProgramIO 的回归同时执行。TP1 保留已有实例的状态访问与输出格式，未通过放宽旧 Lite 原语来接纳 TP4。

下一步需实际投影 TP4 所有反向、逆通信、梯度及 120 次 SGD 到 IR2/GlobalActionDAG、链接、单次 NpuSim 连续执行，再按 8 个代表矩形双独立完整运行；现有源图通过不等于这些原生证据已存在。
