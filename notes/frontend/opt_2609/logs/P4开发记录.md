# P4：外部内存与传输通路开发记录

日期：2026-09-14  
对应计划：[矩形 Die Mesh 端到端训练与推理负载支持完善开发计划](../plans/矩形DieMesh端到端训练与推理负载支持完善开发计划.md)

## 1. 本次目标与完成边界

本次交付完成 P4.1 和 P4.2 的独立 Python 基础切片：

- 建立严格的 external backing、HBM endpoint、共享 link、die connection 和 route schema。
- 实现能够复制非零 payload 的 external 与 HBM 双向传输服务。
- 为共享半双工带宽、固定延迟、队列深度和最大 outstanding 建立确定性时序。
- 输出逐请求 completion，以及方向性 bytes、busy、stall、queue、outstanding、makespan 和 pending 统计。

初始 P4.1/P4.2 Python 切片没有修改 C++/SystemC NpuSim、ISA、finalizer、ProgramIO 或现有 workload lowering。External transfer report 固定携带 simulator_runtime_integrated=false，表示该报告只来自生产包中的 Python 组件服务，不能作为完整模型 offload 已在 NpuSim 中运行的证据。后续独立 C++ component runtime 见第 9 节；它仍未接入 workload action DAG。

## 2. P4.1 外部层 schema

新增 schema/external_memory.py。

### 2.1 存储与链路合同

ExternalMemoryFabric 复用 P2 的 MemoryTierCapacity 描述容量和地址范围，并显式区分：

- external capacity：外部 backing 的有限地址空间。
- HBM capacity：传输目标或来源 die 的有限地址空间。
- ExternalMemoryLink：一个 external backing 的传输 controller/link。
- ExternalMemoryConnection：link 到具体 die HBM 的连接和 route。

初版 link 只允许 HALF_DUPLEX_SHARED，两个传输方向使用同一个服务时间线。每条 link 必须声明：

- ingress die。
- bytes_per_cycle。
- latency_cycles。
- queue_depth。
- max_outstanding。

max_outstanding 不得超过 queue_depth 加一个正在服务的请求。容量、带宽、队列和 outstanding 必须为正数；latency 可以为零。

### 2.2 直连与非直连 die

每条 link 必须存在 ingress die 的直接连接。直接连接的 route 必须恰好为 ingress die，额外 route latency 为零且没有 route bandwidth。

访问非直连 die 时必须给出：

- 从 ingress die 开始、在 target die 结束的无重复 route_die_ids。
- 非零的 route_latency_cycles。
- 非零的 route_bytes_per_cycle。

因此非直连 die 不能省略路径，也不能被计为零成本。当前 route 参数表示该路径的总附加服务成本。

Fabric 校验还包括：

- external/HBM tier 类型正确。
- 地址范围按 `(tier, location_ref)` 所属地址域检查；同一 owner 不重叠，不同 die 的 HBM 可以各自从本地地址 0 开始。
- 每个 external backing 恰好对应一条 link。
- connection 的 link、HBM 和 target 引用闭合；HBM owner 必须使用规范 `die:<id>`，且该 id 与 connection target 相等。
- 每个 HBM capacity 至少有一个 external connection。
- 同一 link/target connection 不重复。
- schema 严格 JSON 往返和稳定 artifact ID。

## 3. P4.2 确定性传输服务

新增 passes/external_memory.py。

### 3.1 Payload 存储

SparseMemoryImage 是容量受限的稀疏字节存储：

- 未写入地址读为零。
- 非零和零字节都按真实 payload 语义写入。
- 每次 read/write 都检查配置的半开地址范围。
- 稀疏宿主表示不改变被模拟的容量；越界仍返回 external_memory_address_out_of_range。

execute_external_transfers() 要求 external/HBM image 与 fabric capacities 精确闭合，不接受缺失、额外或绑定错误的 image。

### 3.2 时序公式

每条 link 独立维护可用时间。相同 issue cycle 的请求按稳定 request ID 排序。

直接连接：

    external_service_cycles =
      latency_cycles + ceil(payload_bytes / bytes_per_cycle)

非直连连接：

    route_service_cycles =
      route_latency_cycles + ceil(payload_bytes / route_bytes_per_cycle)

    total_service_cycles =
      external_service_cycles + route_service_cycles

共享 link 上：

    start_cycle = max(issue_cycle, previous_completion_cycle)
    completion_cycle = start_cycle + total_service_cycles
    queue_stall_cycles = start_cycle - issue_cycle

external_to_hbm 与 hbm_to_external 在同一 link 上串行，不能同时获得两份带宽。不同 external links 有各自时间线，可以并行。

请求进入服务前检查 issue cycle 上的 outstanding 和等待队列占用。超过任一上限时返回 external_queue_exhausted，不静默创建无限队列。

### 3.3 完成与统计

每个成功请求产生唯一 ExternalTransferCompletion，包含：

- request/link/direction。
- start/completion cycle。
- external 与 route service cycles。
- queue stall。
- payload bytes。
- 实际搬运 payload 的 SHA-256 digest。

ExternalTransferReport 要求 completion 精确覆盖 request，重新计算时序、地址、queue/outstanding 上限和统计。统计包含：

- submitted/completed/pending requests。
- external read/write bytes。
- HBM read/write bytes。
- shared-link busy cycles。
- queue stall cycles。
- 最大 queue occupancy。
- 最大 outstanding。
- makespan。

## 4. 测试内容与结果

新增 llm/test/frontend/unit/test_external_memory.py，共 9 个测试：

1. 非零 payload external→HBM→external 往返、digest 和四类方向 bytes。
2. 直接访问的延迟/带宽公式。
3. 非直连 die 的 route 延迟/带宽附加成本。
4. 两个方向共享半双工 link，验证串行 completion 和 stall。
5. 跨 die 请求在同一 link 上争用，不同 link 可以并行。
6. queue depth/max outstanding 耗尽时稳定拒绝。
7. 缺 connection 和 external/HBM 地址越界时稳定拒绝。
8. 零容量、非法 queue/outstanding、缺少非直连 route 成本和非法方向。
9. 地址空间重叠、image 集合不闭合和伪造 completion。

最终聚焦与相邻回归命令：

    PYTHONPATH=/workspace python3 -B -m unittest test_external_memory.py test_memory_plan.py test_persistent_state_schema.py test_placement_pass.py

结果：

    ................................
    ----------------------------------------------------------------------
    Ran 32 tests in 1.157s

    OK

另已通过新增三个 Python 文件的 py_compile、公共 package 导入检查及相关文件的 git diff --check。

## 5. 改动文件

- llm/frontend/wafer_frontend/schema/external_memory.py
- llm/frontend/wafer_frontend/passes/external_memory.py
- llm/frontend/wafer_frontend/schema/__init__.py
- llm/frontend/wafer_frontend/passes/__init__.py
- llm/test/frontend/unit/test_external_memory.py
- notes/frontend/opt_2609/logs/P4开发记录.md

## 6. 尚未完成

以下仍属于后续 P4 工作：

- 将 external request 编入 workload action DAG、N6/npup 和 ProgramIO。
- 在 C++/SystemC NpuSim 中实现 controller queue、完成事件和真实资源争用。
- 将 P1 的物理 D2D route/link contention 接入非直连 external transfer；当前 Python 服务只计入 connection 中明确配置的总 route 成本。
- P4.3 已由本文第 7 节的后续切片完成；尚未接入真实 workload/runtime。
- Dense/MoE 推理及训练的 blocking offload。
- AdamW state、激活 offload/recompute、KV 分块、prefetch 和最终状态导出。
- external service 当前以批量 request trace 执行，没有增量 event callback，也没有与 compute/LSU/DTE 共享时间线。

因此当前可以声明“P4.1 schema 与 P4.2 Python external transfer component 已通过单元测试”，不能声明 M2、offload_blocking、四类完整负载或 NpuSim external runtime 已完成。

## 7. P4.3 HBM residency/offload planner

本次后续切片新增 schema/offload.py 与 passes/offload.py，在 P2 allocator 和 P4 external transfer service 上实现确定性的 offload_blocking 组件 API。

### 7.1 输入与输出合同

输入由以下对象组成：

- OffloadChunk：逻辑 state generation 0、对象类型、chunk 大小/对齐、external backing 地址和目标 connection。
- OffloadTraceEvent：连续 ordinal 的 PIN、UNPIN、READ、WRITE 事件及显式依赖。
- OffloadResidencyRequirement：普通事件使用 AUTO_BRING_IN；需要验证预取/依赖的 consumer 可声明 MUST_ALREADY_RESIDENT。
- ExternalMemoryFabric：复用 P4.1 的 external/HBM 容量、连接和链路参数。

输出 BlockingOffloadPlan 包含：

- 完整 state generation 列表。
- P2 MemoryPlan 生成的每段 HBM lifetime、allocation、地址和 peak。
- 显式 external_to_hbm / hbm_to_external transfer request sequence。
- 严格串联的 bring-in、consume、pin/unpin、dirty writeback 和 clean discard operation。
- bring-in/writeback/discard、读写消费、bytes、blocking cycles、HBM peak、pin 及最终 drain 统计。

BlockingOffloadExecution 使用 P4.2 SparseMemoryImage 和 execute_external_transfers 执行计划内的真实字节搬运，并要求 transfer report 与计划的 fabric/request 精确相等。

Plan 和 Execution 均保持 simulator_runtime_integrated=false。本切片是 Python 组件级 blocking offload，不是 C++ NpuSim 完整负载 runtime。

### 7.2 Next-use、eviction 与状态版本

每个 HBM endpoint 独立计算 resident bytes。Bring-in 前容量不足时，只从 pin_count 为零的 resident chunks 中选择 victim：

1. 优先选择下一次使用最远的 chunk。
2. 没有未来使用视为最远。
3. 下一次使用相同则按稳定 chunk ID 选择。

Victim 为 clean 时生成 CLEAN_DISCARD，不产生虚构 transfer；victim 为 dirty 时生成 HBM_TO_EXTERNAL writeback，并保持 HBM allocation 至 writeback completion。新 bring-in 只能在该 completion 之后复用地址。

Bring-in 占用 HBM 地址后先记录 TRANSFERRING residency，完成时切换为 CLEAN。WRITE consumer 为对应逻辑 state 创建 generation+1，predecessor 指向同 state 的前一代，并在 WRITE 完成点切换为该 generation 的 DIRTY residency。Dirty writeback 完成前保留 HBM lifetime，完成后状态变 clean 并退出 resident window；最终 planner 按稳定 chunk ID 排空，保证 final resident、dirty 和 pin 均为零。

为支持这一合同，P2 MemoryPlan 允许同一物理 allocation 上存在多个时间连续的 residency segments。Segments 必须无缝覆盖 allocation lifetime、属于同一逻辑 state，且 version 只能保持不变或沿 predecessor 逐代推进。P4 validator 会在 operation 的实际 start/ready cycle 查找对应 residency，拒绝只有 operation generation、没有物理 residency 的伪造计划。

PIN 会自动 bring-in 后增加引用计数；UNPIN 减少引用计数。Pinned chunk 不参与 eviction。末尾引用计数非零返回 offload_pin_leak，容量全部被 pinned chunks 占用返回 offload_pinned_capacity。

Consumer operation 精确依赖前一 blocking operation。自动 bring-in 后的 READ/WRITE cycle 等于 transfer ready cycle。MUST_ALREADY_RESIDENT 在没有既有 residency 时返回 offload_read_before_resident。

### 7.3 Resident-only 对照与容量

plan_resident_only_memory() 将同一组 chunks 作为整个 lifetime 同时驻留，直接调用 P2 plan_hierarchical_memory。相同模型状态在低容量 HBM 上由 resident-only 返回 memory_capacity_exceeded，而 offload_blocking 可以通过分段 residency 运行。

单个 chunk 对齐后仍大于目标 HBM capacity 时返回 offload_minimum_window_insufficient。所有成功计划再次执行 P2 MemoryPlan.validate，并要求记录的 HBM peak 不超过配置容量。

### 7.4 P4.3 测试

新增 llm/test/frontend/unit/test_offload.py，共 8 个测试：

1. 同一 chunks 在 resident-only 下 OOM、offload_blocking 下成功。
2. clean discard、重复 bring-in、最终 residency drain 和稳定计划/serde。
3. WRITE 产生 generation+1，dirty eviction 生成真实 writeback request 并由 P4.2 service 完成全部 transfers。
4. 三个 chunks/两个 HBM slots 时选择下一次使用最远的 victim。
5. consumer 读前未驻留、单 chunk 最小 HBM window 不足时 fail closed。
6. pinned capacity 阻塞 eviction 和末尾 pin leak 时 fail closed。
7. consumer 等待 bring-in completion，并拒绝 writeback 尚未完成时复用 HBM allocation。
8. request/graph/source-memory digest 与当前 workload 精确绑定，外来 source plan、chunk、state/version fail closed。

最终聚焦与相邻回归：

    PYTHONPATH=/workspace python3 -B -m unittest test_offload.py test_external_memory.py test_memory_plan.py test_persistent_state_schema.py test_placement_pass.py

结果：

    ..........................................
    ----------------------------------------------------------------------
    Ran 42 tests in 1.263s

    OK

另已通过 P4.3 新增文件的 py_compile、公共 package 导入检查和长行检查。

### 7.5 P4.3 尚未覆盖

- Trace consumer 的 WRITE 只推进 timing state generation 和 dirty 状态，不模拟模型计算对 payload 的数值更新。
- P1 D2D route 的逐 link contention 仍未接入；使用 P4.1 connection 声明的总 route 成本。
- 没有 action DAG/N6/npup/ProgramIO/C++ SystemC 集成，也没有与 compute/LSU/DTE 共用时间线。
- 尚未接入 Dense/MoE 的权重、KV、激活、梯度或 optimizer workload adapter。
- 当前是静态 blocking trace；跨 tier 异步迁移、prefetch、双缓冲与取消属于 P4.8。

因此 P4.3 完成状态仅为 residency/offload planner 与 Python component execution；M2 和四类完整负载 offload 仍未完成。

## 8. 集成审阅问题 7/8 修复

### 8.1 Typed address-space owner

集成审阅发现 ExternalMemoryFabric 原先把所有 HBM capacity 放在同一个全局数值地址域内检查 overlap。这会错误拒绝 `die:0` 和 `die:1` 都从本地地址 0 开始的合法配置，同时 connection 没有验证目标 die 是否真正拥有所引用的 HBM。

修复后：

- capacity overlap 按 `(MemoryTier, location_ref)` 分域检查；同一 typed owner 的相交区间仍 fail closed。
- 每个 HBM `location_ref` 必须使用规范 `die:<uint64>` 形式。
- 每个 ExternalMemoryConnection 的 `target_die_id` 必须等于 HBM owner die。
- transfer service 继续通过 connection 的 `hbm_capacity_ref` 选择目标 SparseMemoryImage；两个 die 即使使用相同本地地址，也不会混写。

该语义变更使用 `wafer_frontend.external_memory_fabric/v1alpha2`，不会把旧解释静默伪装为同一 schema。

### 8.2 Workload/source binding

BlockingOffloadPlan 原先只携带独立 chunks/events 和派生 memory plan，无法证明这些状态来自当前 request、logical graph 及其源 memory plan。修复将计划 schema 升为 `wafer_frontend.blocking_offload_plan/v1alpha2`，并新增：

- `request_digest` 与 `logical_graph_digest`。
- 嵌入的 `source_memory_plan` 及 `source_memory_plan_digest`。
- `OffloadStateMapping`，逐 chunk 一一绑定 source state version 与 source allocation。

计划自校验要求 source digest 与嵌入 artifact 的 canonical digest 相等；mapping 必须精确覆盖所有 chunks，且 source version、allocation、object kind、external tier、size、alignment、address 和 capacity 均一致。派生 generation 只能属于已映射 logical state，validator 还会重建每个 chunk 的权威 version chain，并要求 state_versions 精确等于 generation-0 输入加实际 WRITE 结果。

`validate_blocking_offload_plan()` 和 `execute_offload_blocking()` 现在要求调用方传入当前 request、logical graph、source memory plan 三个 expected digest；任一不一致返回 `offload_workload_binding_mismatch`。因此从另一个 case 拼入一份自洽计划也不能在当前 workload context 中执行。

这是显式 schema/API 迁移：v1alpha1 offload plan 不会被 v1alpha2 decoder 当作兼容对象读取。当前仓库内调用点及 focused tests 已迁移；新 schema 的公共 `__init__.py` 导出由主线集成统一处理。

### 8.3 新增回归覆盖

本轮新增或收紧以下测试：

1. 两个 die 的 HBM 都以 local address 0 起始，分别执行非零 payload 搬运并写入各自 image。
2. 同一 die 内 HBM capacity overlap 失败。
3. connection target 与 `die:<id>` owner 不一致失败。
4. 当前 request/logical graph expected digest 与计划绑定不一致失败。
5. source memory plan digest 不一致失败。
6. 另一组独立 chunk/state/version 与原 source mapping 混绑失败。

P2/P4 focused 与相邻 persistent-state/placement 回归共 42 项通过，结果见第 7.4 节。该 schema 修复自身未增加 C++ runtime 或 workload adapter；后续独立 C++ component 见第 9 节，Python plan 的 `simulator_runtime_integrated=false` 能力边界不变。

## 9. P4.2 C++ external-memory component runtime

### 9.1 交付范围

新增以下独立 C++17 组件：

- `llm/include/memory/external_memory_service.h`
- `llm/src/memory/external_memory_service.cpp`
- `llm/unittest/external_memory_service_selftest.cpp`

根 CMake 新增 `npusim_external_memory_service` static library、`npusim_external_memory_service_selftest` 可执行文件及 `external_memory_service_selftest` CTest。

组件配置与 Python ExternalMemoryFabric v1alpha2 的核心语义对齐：

- external capacity 使用显式 owner，HBM capacity 使用显式 owner die；地址 overlap 只在相同 typed owner 内拒绝。
- connection 必须引用已声明 link/HBM，`target_die_id` 必须等于 HBM owner die。
- 直连 route 必须为 ingress die 的零成本单点路径；非直连 route 必须从 ingress 开始、在 target 结束，并声明非零 latency/bandwidth。
- 每个 external capacity 恰有一个 service link；每个 link 有 ingress-die direct connection；每个 HBM capacity 均有 connection。

`SparseMemoryBacking` 是有限地址范围内的稀疏字节 backing：未写入字节读零，写零会删除稀疏项，所有读写均检查正长度、uint64 overflow 和 capacity bounds。每个 HBM capacity 持有独立 backing，因此不同 die 可同时使用 local address 0。

`ExternalMemoryService::Execute()` 接受 external↔HBM 请求，按 `(issue_cycle, id)` 确定排序，并按 connection 引用选择 external/HBM backing。每个 link 使用共享 half-duplex timeline，两个方向不能并行。时序与 Python 服务一致：

    external_cycles = latency + ceil(payload_bytes / bytes_per_cycle)
    route_cycles = remote ? route_latency + ceil(payload_bytes / route_bytes_per_cycle) : 0
    completion_cycle = max(issue_cycle, link_free_cycle) + external_cycles + route_cycles

提交时严格检查 queue depth 和 max outstanding。成功报告包含逐请求 start/completion、external/route service、queue stall、payload bytes，以及方向性 external/HBM bytes、link busy cycles、总 stall、最大 queue/outstanding、makespan 和 pending 数。

### 9.2 聚焦测试

C++ selftest 覆盖：

1. 含零字节和非零字节的 external→HBM→external 往返及方向性 byte/completion 统计。
2. 相反方向请求争用同一 half-duplex link，第二个请求等待第一个 completion，stall/busy/queue/outstanding 精确。
3. 两个 die 都从 HBM local address 0 开始，分别写入独立 backing。
4. 同 die HBM overlap、connection/HBM owner 不一致、请求容量越界、connection 缺失和 queue/max-outstanding 耗尽均 fail closed。

构建与执行：

    env SYSTEMC_HOME=/opt/systemc-2.3.3 cmake -S . -B build
    cmake --build build --target npusim_external_memory_service_selftest -j2
    /opt/cmake/bin/ctest --test-dir build -R ^external_memory_service_selftest$ --output-on-failure

结果：

    1/1 Test #1: external_memory_service_selftest ... Passed
    100% tests passed, 0 tests failed out of 1

新 target 使用 `-Wall -Wextra -Werror` 编译通过；scoped `git diff --check` 和 C++ 长行检查通过。

### 9.3 Component runtime 与 workload runtime 边界

该 C++ 服务已实际执行有限 external/HBM backing 间的非零 payload 搬运，并形成可运行的 component runtime。第 9 节记录的是初始独立 batch service；第 10 节在此基础上增加了直接访问现有 HBMBackend 的 SystemC runtime bridge。完整 workload runtime 仍未绑定：

- N6/npup external-memory opcode、record 或 action DAG。
- finalizer、ProgramIO、workload lowering 和统一 runner。
- compute/LSU/DTE action DAG 和完整 workload 共用时间线。
- P4.3 planner 输出到 C++ request 的序列化桥接。

因此不能把本节的 C++ selftest 记为 Dense/MoE 训练或推理的 offload workload runtime，也不能将 `offload_runtime_verified` 置为 true。完整 workload runtime 仍属于后续 P4.4–P4.9 集成。

## 10. P4.2 C++ HBM runtime bridge

### 10.1 版本化 typed DMA 与真实 HBM backing

新增：

- `llm/include/memory/external_memory_runtime.h`
- `llm/src/memory/external_memory_runtime.cpp`
- `llm/unittest/external_memory_runtime_selftest.cpp`

并在 CMake 中加入 `npusim_external_memory_runtime` static library、integration selftest 和 `external_memory_runtime_bridge_selftest` CTest。

`TransferRequest` 新增显式 schema version `npusim.external_dma_request/v1alpha1`。`ExternalMemoryRuntimeBridge` 在 admission 时校验 schema、唯一 request ID、方向、当前 cycle、connection、external/HBM 地址范围、queue depth 和 max outstanding；所有检查通过后才登记请求、更新 submitted 统计并进入 link worker。失败请求不会修改 external backing、HBM backend 或已提交统计。

桥接器只维护 external sparse backing。HBM 一侧不再建立副本，而是按 `hbm_capacity_ref` 找到调用方提供的 `HBMBackend*` 并提交标准 `HBMBackendTransaction`：

- external→HBM 在 admission 时快照 external payload，完成 external/route service 后向真实 backend 提交 write。
- HBM→external 先从真实 backend read；只有 backend 成功完成后才把 payload 写入 external backing。
- backend 返回的 completion delay、service time、status 和 error 均进入 runtime completion；backend 失败不会对反向 DMA 的 external 目标进行部分写入。

现有 `HBMRuntime` 拥有的 backend 可直接作为 binding 传入；integration selftest 使用 `BehavioralHBMBackend` 验证与现有 backing 的实际互操作。当前 ExternalMemoryFabric 只有 HBM owner die，没有 stack/channel 标识，因此 bridge 要求上层显式提供完整且非空的 `capacity_ref -> HBMBackend*` 映射，不能从 `HBMRuntime` 拓扑自动猜测 endpoint。

### 10.2 SystemC completion 与共享链路

每个 external link 由一个 SystemC thread 串行处理，请求两个方向共享同一 half-duplex 队列和 service timeline。远端 route 成本继续使用 fabric 中显式声明的 route latency/bandwidth。

runtime 提供：

- `Poll(request_ref)`：非阻塞读取单请求 completion。
- `Wait(request_ref)`：SystemC process 内等待对应请求完成。
- `CompletionEvent()`：供调度器轮询任意 completion。
- `Outstanding()` 和 `Stats()`：报告完成数、失败数、方向性 external/HBM bytes、external/HBM service time、queue stall 和最大 outstanding。

completion 包含 request ID、status/error、submit/start/complete time、external/HBM service time 和 payload bytes。写入真实 backend 后才发布 external→HBM completion；读取真实 backend 并提交 external 写入后才发布 HBM→external completion。

### 10.3 Integration selftest

selftest 覆盖：

1. external 中的非零 payload 经 DMA 写入 `BehavioralHBMBackend`，completion 前 Poll 未就绪，Wait 后直接 backend read 得到相同 payload。
2. 先直接写脏数据到同一个 HBM backend，再执行 HBM→external DMA，external probe 得到 dirty payload。
3. capacity 越界、缺 connection 和错误 schema 均在 admission 阶段失败，submitted 计数不变。
4. max outstanding/queue depth 耗尽拒绝第三个请求；已接受请求按 half-duplex 次序完成。
5. backend 读失败时 external 目标保持原值，失败状态进入 completion/stats。

构建与测试：

    env SYSTEMC_HOME=/opt/systemc-2.3.3 cmake -S . -B build
    cmake --build build --target npusim_external_memory_runtime_selftest -j2
    ./build/npusim_external_memory_runtime_selftest
    /opt/cmake/bin/ctest --test-dir build -R "^(external_memory_service_selftest|external_memory_runtime_bridge_selftest)$" --output-on-failure

结果：

    external memory runtime selftest: PASS
    2/2 tests passed, 0 tests failed out of 2

service 与 runtime bridge 均使用 `-Wall -Wextra -Werror` 编译通过。

### 10.4 尚未接入的 workload runtime 接口

本节完成了可执行的 HBM backend bridge 和事件接口，尚未完成：

- 在 N6/npup schema 中定义 DMA submit、poll/wait action，并将它们加入 action DAG 依赖和调度。
- 将 P4.3 planner 的 transfer sequence 序列化/降低为上述 typed DMA request。
- 在 finalizer 中把 logical allocation、capacity 和地址 relocation 解析为 HBMRuntime 的 `(stack_id, channel_id)` endpoint 及 `capacity_ref -> backend` binding。
- 在 ProgramIO/runner 中初始化 external payload、收集最终 external probe 和 transfer completion evidence。
- 将 offload completion 纳入 compute/LSU/DTE 的统一完成条件、错误传播和 capability report。

因此该切片证明 external DMA 可与真实 HBM backend backing 互操作，但仍是 component runtime integration，不能据此宣称 Dense/MoE 训练或推理端到端 offload 已完成。

## 11. P4 planner 到 C++ runtime 的 sidecar 最小闭环

### 11.1 Python finalizer 与版本化 program

新增：

- llm/frontend/wafer_frontend/schema/external_dma_program.py
- llm/frontend/wafer_frontend/passes/external_dma_program.py
- llm/test/frontend/unit/test_external_dma_program.py
- llm/test/frontend/golden/external_dma_program_v1alpha1.json

finalize_external_dma_program() 先使用当前 plan 内的 request、logical graph 和 source memory plan digest 重新执行 validate_blocking_offload_plan()，再只选择 BRING_IN 和 DIRTY_WRITEBACK operation，按原 operation sequence 产生 wafer_frontend.external_dma_program/v1alpha1 sidecar。

Program 顶层绑定：

- case digest。
- request digest。
- logical graph digest。
- source memory plan digest。
- blocking offload plan ID 及 canonical digest。
- 完整 ExternalMemoryFabric v1alpha2。

每个 descriptor 使用 npusim.external_dma_request/v1alpha1 request schema，并保留 source operation ID、source transfer request ID、原 operation dependency、transfer-only blocking dependency、connection、方向、external/HBM 地址、长度及 planner 的 issue/ready cycle。Descriptors 使用连续 sequence，严格依赖前一个 transfer descriptor，不能在 planner dependency 的 ready cycle 前开始。

HBM binding 使用显式 hbm_capacity_ref 到 (owner_die_id, stack_id, channel_id) 映射。Bindings 必须精确覆盖 fabric 内所有 HBM capacity，owner 必须等于 capacity 的 die:<id>，endpoint 不能重复，stack/channel 必须可由现有 HBMRuntime::Find(int, int) 表示。该 binding 由 finalizer 调用方提供；当前没有从硬件配置自动推导。

External ProgramIO 使用同一 sidecar 内的 external_seeds 与 external_probes。Payload 使用非空小写 hex，记录按 capacity/address/ID 稳定排序，检查 uint64 range、capacity bounds 及同类记录 overlap。Program、binding、descriptor、seed 和 probe 均使用 Python stable artifact ID；serializer 使用 canonical JSON 并保留最终换行，严格 parser 拒绝未知字段、重复 key、隐式类型及 schema version 不匹配。

### 11.2 C++ loader 与 executor

新增：

- llm/include/memory/external_dma_program.h
- llm/src/memory/external_dma_program.cpp
- llm/unittest/external_dma_program_selftest.cpp

CMake 新增 npusim_external_dma_program library、npusim_external_dma_program_selftest executable 和 external_dma_program_cross_language_selftest CTest。

C++ loader 检查顶层和嵌套对象的精确字段、duplicate JSON key、版本、digest 格式及调用方提供的 expected case/request/graph/source-memory/offload-plan digest。它重建 FabricConfig 并复用 ValidateFabricConfig()，同时验证 backend binding 精确覆盖、owner/endpoint、descriptor 顺序/依赖/时序/地址以及 external seed/probe 的排序、范围和 overlap。

ExternalDmaProgramExecutor 按 endpoint binding 将 capacity 解析到调用方提供的真实 HBMBackend 指针，构造第 10 节的 ExternalMemoryRuntimeBridge。SystemC thread 先初始化 external backing，再依序等待 transfer dependency，按 planner issue cycle 的 not-before 约束提交 typed DMA，并等待 runtime completion。真实 HBM backend latency 可能使后续请求晚于 planner 的 external-link-only issue cycle；executor 不回拨模拟时间，结果以 runtime completion 的实际时间为准。

全部 descriptor 成功后读取 external probe。Execution 返回逐请求 runtime completion、逐 probe payload/match、runtime stats 和错误。任何 source binding、loader、backend、DMA completion 或 probe 错误均令 execution fail closed。

### 11.3 跨语言 golden 闭环

Golden 由 Python finalizer 从一个真实 BlockingOffloadPlan 生成。该计划包含：

1. 一个 16-byte writable parameter chunk。
2. external 到 HBM bring-in。
3. CONSUME_WRITE 状态 generation 推进。
4. HBM 到 external dirty writeback。
5. external seed 与最终同地址 probe。

C++ CTest 加载该 Python canonical JSON，使用显式 (stack=0, channel=0) binding 和 BehavioralHBMBackend 执行两个 descriptors。测试验证：

- C++ 读取的 program identity 与 Python golden 一致。
- 第二个 transfer 在第一个 runtime completion 之后启动。
- 非零含零字节 payload 完整往返，external probe 逐字节匹配。
- submitted/completed 均为 2，失败为 0。
- external read/write 与 HBM read/write 均为 16 bytes。
- 错误 case digest 和未知 JSON 字段被拒绝。

测试结果：

    PYTHONPATH=/workspace python3 -B -m unittest
        test_external_dma_program.py test_offload.py
        test_external_memory.py test_memory_plan.py

    .............................
    Ran 29 tests in 0.240s
    OK

    /opt/cmake/bin/ctest --test-dir build
        -R '^(external_memory_service_selftest|external_memory_runtime_bridge_selftest|external_dma_program_cross_language_selftest)$'
        --output-on-failure

    3/3 tests passed, 0 tests failed out of 3

新 Python 文件通过 py_compile；新 C++ target 使用 -Wall -Wextra -Werror 构建通过；C++ 长行、trailing whitespace 和 git diff --check 均通过。

### 11.4 能力边界与剩余集成

本节完成的是 BlockingOffloadPlan transfer subset 到真实 HBM backend 的可执行 sidecar 闭环。它仍不是完整 Dense/MoE 训练或推理 workload runtime：

- sidecar 由独立 executor 运行，尚未形成 N6/npup DMA opcode、record 和主 action DAG node。
- source_operation_deps 作为 provenance 保留；主 action scheduler 尚未让 compute/LSU/DTE operation 直接等待 DMA completion。
- endpoint binding 由调用方显式提供；finalizer 尚未结合硬件 HBM address map 自动完成 capacity/address relocation 和跨 channel split。
- BlockingOffloadPlan 的 CONSUME_WRITE 只推进 state generation/dirty 状态，没有模型计算 payload 变换。因此 golden 验证 transfer 和 backing 一致性，不声称验证数值训练更新。
- external seed/probe 已可执行，但尚未并入统一 ProgramIO contract、runner output 和 workload capability evidence。
- 当前 executor 是全局 blocking transfer 顺序；异步 prefetch、双缓冲、跨 link 并行和取消仍未实现。

因此本节可证明 Python planner 生成的 transfer program 能被 C++ loader 绑定到真实 HBM backend 并完成 payload/probe/stats 闭环，但 offload_runtime_verified 仍不能用于完整端到端训练或推理能力声明。

## 12. P4 transfer 与统一 workload runner 的 action sidecar 绑定

### 12.1 实现

新增以下生产模块：

- `llm/frontend/wafer_frontend/schema/external_dma_action_graph.py`
- `llm/frontend/wafer_frontend/passes/external_dma_action_graph.py`
- `llm/frontend/wafer_frontend/workload_external_dma.py`
- `llm/src/memory/external_dma_program_runner.cpp`

`ExternalDmaActionGraph` 把 P4 DMA descriptor 和 P3 logical operation 合入同一版本化动作清单。顶层同时绑定 workload manifest、case、request、P3 graph、P2 memory plan、P4 blocking plan 和 DMA program digest。DMA action 引用精确 state version 与 residency；parameter load 和 parameter reader 显式依赖 bring-in action。

Validator 会按当前 manifest、plan 和 program 重新构造期望动作，逐项比较 source、dependency、state version 和 residency，不能仅凭 source 集合相同通过。稳定 ID 的 dependency/state/residency 重绑负测会失败。

`create_external_dma_workload_adapter()` 实现统一 runner 的 ADAPTER、FINALIZE、RESOLVE 和 RUNTIME 四阶段。每个后续阶段都从工作目录重新读取并严格验证 action graph；RUNTIME 调用前重新计算 C++ binary digest。C++ runner 输出 typed runtime report，Python 要求 submitted=completed=descriptor count、failed=0、pending=0、probe 全匹配后才返回 runtime stage artifact。

### 12.2 同 workload 容量失败/传输成功 canary

测试使用同一 1x1 Dense inference request、logical graph digest 和 source memory plan：

1. HBM window 小于最小 parameter chunk 时，resident-only planner 按容量失败。
2. 使用合法 window 和 external tier 时，blocking offload 生成 bring-in program。
3. 统一 WorkloadRunner 以 exact-case、source、toolchain 和 C++ binary digest 派生的 capability evidence 驱动 adapter。
4. C++ runner 执行 external 到真实 BehavioralHBMBackend 的非零 payload 搬运。
5. runtime report 验证 external_read_bytes 与 hbm_write_bytes 非零，pending_requests=0。

验证命令与结果：

    cmake --build build --target npusim_external_dma_program_runner -j2
    NPUSIM_EXTERNAL_DMA_RUNNER=/workspace/build/npusim_external_dma_program_runner \
      PYTHONPATH=/workspace:/workspace/llm/test/frontend/unit \
      python3 -B -m unittest -v test_external_dma_action_graph

    Ran 2 tests in 1.019s
    OK

原 P4 Python 回归 29/29、external memory CTest 3/3 继续通过；py_compile 和 git diff --check 通过。

### 12.3 当前边界

该 canary 证明 offload transfer 已进入统一 runner 的 artifact、digest、repeatability 和 drain 合同。C++ runner 当前执行 DMA descriptors，P3 logical operations 在 action graph 中承担依赖和 provenance，尚未由同一 C++ executor 执行 compute/LSU/DTE records。因此这仍是主编排 component canary，不构成完整 Dense inference offload runtime，更不构成四类 workload 的 M2 验收。

下一步仍需把 logical action 映射到真实 N6/npup 或主 runtime node，使 compute 消费 DMA completion；随后加入 dirty model-state writeback、ProgramIO 统一 seed/probe 和完整 workload completion evidence。

## 13. Dense inference compute-consumes-offload 同进程闭环

### 13.1 实现

新增 `wafer_frontend.external_dma_runtime_binding/v1alpha1` 的 C++ loader。Loader 使用精确字段集合和 duplicate-key 检查，校验 action graph 及五项 source digest，并只接受固定相对路径 `artifacts/external_dma_program.json`。未知字段、重复 key、错误 schema 和目录穿越路径均 fail closed。

`npusim --program-sequence` 新增 `--external-dma-binding`。运行时将 program 的 `hbm_capacity_ref` 经 `(stack_id, channel_id)` 解析到当前 `Monitor::hbmRuntime` 持有的真实 `HBMBackend`，不创建第二份 HBM backing。`Monitor::GateStartupUntil()` 让配置、参数 LSU load 和 compute 等待 `ExternalDmaProgramExecutor` 完成，且要求 probe 全匹配、`pending_requests=0` 后才释放启动事件。每个 sequence segment 完成后输出实际 Program artifact 的 compute/LSU_LOAD record 计数；最终 workload drain 再复验 DMA execution 和 probe，并仍只输出一个 `SIM_RESULT`。

新增 `run_dense_external_offload_runtime_canary.py`。该 canary 使用 1×1、两层、Prefill→Decode→Decode 的同一个模型、mesh、steps 和 execution 语义：

- resident HBM 计划峰值为 55,168 bytes，硬件 HBM 只有 54,336 bytes，稳定返回 `memory_capacity_exceeded`；
- external policy 将 53,568-byte parameter 放在 host backing，KV、activation 和 communication 留在同一个有限 HBM 预算内；
- typed 链路为 external `WorkloadMaterializationManifest.memory_plan` → `BlockingOffloadPlan` → `ExternalDmaProgram` → 真实 `ExternalDmaActionGraph` → runtime binding；
- 计算 Program 仍由同模型 resident manifest 生成，因为现有 Dense compiler 会明确拒绝 external manifest；canary 对两份 P3 graph 建立去除 request identity、memory policy 和内容寻址 ID 的完整 useful graph projection，保留 operation、state/value edge、dependency、group 和 route trace 后要求 digest 相等。

### 13.2 真实运行结果

运行命令：

    PYTHONPATH=. python3 -m \
      llm.test.frontend.integration.run_dense_external_offload_runtime_canary \
      --finalizer build-debug-final/npusim_program_finalizer \
      --npusim build-debug-final/npusim \
      --output /tmp/p4-dense-external-offload-runtime-canary \
      --timeout 600

结果为 PASS：

- external DMA action graph digest：`55454ddb99af14d1a639a1f0a9bfbe989fd1d0d05cd5e460064442ad1c475ee8`；
- resident/offload 原始 logical graph digest 分别为 `1d0caf93b4f72b2bf16db8af9f31da27e4292be32562993e97b053c2018448c6` 和 `147e46d6248c47c0395828a0d473339821cea4e2eb9adb7124598692a913fe19`。二者因 memory policy 属于 request identity 而按设计不同；useful graph projection digest 均为 `f46af74c11670505c151667ed80160223f0659cfc603e25ea5e4291d78e7fdfd`；
- `EXTERNAL_DMA_READY` 位于 segment 0 compute 完成 marker 之前，external read 与 HBM write 均为 53,568 bytes；
- 三段 compute records 为 25/25/25，LSU_LOAD records 为 15/19/19；
- 最终 probe=1、external write=0、HBM read=0、`pending=0`，三段只产生一个 `SIM_RESULT`，makespan 为 50,357 cycles。

配套验证：

- `npusim` 增量构建通过；
- 原 1×1 Dense inference sequence canary 通过；
- typed external-offload canary 通过；
- P2/P4 Python 31 tests 通过，1 skipped；
- 真实 runner action graph 2/2 通过；
- external memory CTest 3/3 通过。

### 13.3 能力边界

本节首次证明 external DMA、ProgramIO 参数初始化、真实 LSU load 和 Dense compute 使用同一个 simulator process 与同一 HBM backing，并由 startup happens-before 关系连接。它仍是 1×1 Dense inference 的 blocking startup bring-in：

- Dense compiler 还不能直接编译 external manifest；C++ runtime 校验 binding/program，但尚未重新读取 action graph 文件；
- 没有逐层 eviction、异步 prefetch、双缓冲或 dirty writeback；
- 尚未覆盖训练、MoE 和多 Die external-offload runtime。

因此本节可以标记 Dense inference 的 compute-consumes-offload component runtime 已验证，不能据此把四类 workload 的 M2 或任意 mesh external-offload 标记为完成。
