# P3.2 R19：TP4 真实逆通信、IR2 调度与分片 SGD 版本链

2026-09-16。本轮是 P3.2 R18 之后的物理链进展；**N4/N5 schedule 通过并不等于 N6 native/完整仿真通过**。

生产入口 `build_flexible_dense_train_plan(_spec(1,4), RectMeshSpec(1,4)) → build_full_dense_training_two_step_ir0 → place_train_forward_ir0 → partition_train_forward → plan_train_forward → project_train_forward → schedule_train_forward`。两层两步真实训练源有 382 节点、320 次 StateAccess、60 个参数 shard；旧记录 R18 中暂估的 `432` 次访问已纠正：GEMM/NORM WGRAD 只读 saved activation 和 upstream，不为校验额外发明 HBM 参数 DMA。GEMM dX 仍按四个真实 parameter StateDecl READ；embedding WGRAD 本身有真实 table operand 并维持 READ；每个 SGD 真实 READ_WRITE。

反向 TP AllGather 的逆通信使用独立 SUM ReduceScatter：每个输出 shard 的 owner 从原始 partial gradient 做真实 LOCAL_COPY 写局部中间值，其他三个 rank 逐一 SEND，owner RECV→WAIT，四份来源通过 FP32 累加、FP16 输出的 REDUCE 写真实 shard。按真实 route、完整 rank 顺序和 source IR1 值绑定，少 WAIT、改 SEND 输入均 fail-closed。owner LOCAL_COPY 使四个 input 位于同核 comm SRAM 连续缓冲，满足 LOCAL_REDUCE 的严格 native ABI。独立 IR2 validator 和 schedule 对外部梯度生产依赖、临时值来源、跨两步同 shard SGD→forward 版本边精确验证；GEMM/NORM WGRAD 的唯一 owner 由真实 forward 参数 StateAccess/StateDecl 决定，不再使用假的 WGRAD 参数 DMA。SGD alias 改按 StateDecl shard 严格区分，TP1 的原 alias 保留。

生产探针 `/tmp/dense_tp4_native_pipeline_probe.py` 目前依次输出 `plan`, `source 382`, `ir1 382`, `partition`, `plan_collective`, `projection`, `schedule`。随后在 `schema/train_global_action.py::_validate_full_dense_sgd_actions` 命中旧门禁把 collective DGRAD 要求为 COMP，已按真实 N4/IR2 通信动作修正（该点的重跑仍在继续）。聚焦 TP4 源、RS 正反和旧 SRAM scheduler 回归 `19/19 PASS`；此前共享 action/IR2 schema 加 AG、MoE route 回归 `25/25 PASS`；父任务 TP1 native + TP4 validator 合计 `14/14 PASS`。

下一验收必须有同一个真实全梯度两步 GlobalActionDAG→N6 lower/link、所有 TP backward DTE token/WAIT/SUM、每 shard WGRAD→SGD→STORE→下一步 LOAD 物理闭环，之后参数化 1×4/4×1/2×2 和八形状双 Fresh NpuSim。未拿到这些证据前不申报 TP4/M1 完成。
