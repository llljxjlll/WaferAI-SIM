# P3.4-Y — MoE 单 Die 四训练 leaf 原生 finalizer

状态：真实 1×1 MoE 两层×两步的四个独立训练 leaf 均通过当前 native finalizer；完整 full-model 单次 NpuSim 两步 SGD 门禁仍关闭。

原生 finalizer 首先 fail-closed 报告 `compute SRAM_BIND input_count does not match its exact opcode ABI`。定位为 multi lowerer 的 `SWIGLU_BACKWARD_TIMING` 同时引用 forward gate/up concat 和 activated gradient，但其 SRAM_BIND 仅声明前一输入。将两个实际 BufferABI 都绑定后，1×1 四个 leaf 的 0x22 SwiGLU 反向与各 3 个 0x25 FP32 专家 WGRAD 正常闭合。1×2 EP2 同源 leaf 也通过 finalizer。旧 finalizer 二进制另曾在 opcode 0x25 解析处失败，重建当前源码后不再复现；旧失败不计正证。

最终原始产物根：`/tmp/p34-moe-ep1-native-leaf`。`unit0..3.linked.json`、`.npup`、`.finalizer.json` 均保存；逐产物重新读取后，linked manifest ID、canonical digest 与 npup SHA-256 对 finalizer report 全部精确一致。四个 EP1 leaf 各 67 records，EP2 对照 leaf 为 144 records。这里四个 leaf 的 manifest/程序字节碰巧相同，因为冻结路线和参数布局相同；仍未合并成完整模型或一次 native invocation。

验证：`python3 -m unittest llm.test.frontend.unit.test_flexible_moe_production llm.test.frontend.unit.test_moe_compile_sequence llm.test.frontend.unit.test_moe_full_train_named_wgrad_operands`，41 项 PASS；输出 `/tmp/moe_ep1_native_wgrad_tests.log`。finalizer 使用 `build-moe-training-grad/npusim_program_finalizer`，由当前 C++ 源码重建，build log `/tmp/moe_ep1_rebuild_finalizer.log`。

仍需 shared dCombined、两层 backbone/专家端到端反向、参数 v0→v1→v2 状态连续性、ProgramIO、单次物理 NpuSim invocation 及双 Fresh/reopen；本阶段没有完整训练运行通过声明。
