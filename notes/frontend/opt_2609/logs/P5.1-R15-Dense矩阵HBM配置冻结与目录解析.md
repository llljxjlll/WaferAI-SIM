# P5.1-R15 Dense matrix DRAM configuration binding

The frozen Dense 1x2 runtime aborted before simulation because hardware.json references ../DRAMSys/configs/hbm2-example.json while each fresh runs in case/fresh0 or case/fresh1. This failed run does not count as M3 evidence.

The v3 matrix binding now includes the checked-in DRAM configuration SHA256 and links each case to the same frozen source tree; resume rejects redirects. Tests: `python3 -m unittest llm.test.frontend.integration.test_run_dense_native_mesh_matrix`: 13 passed; `git diff --check`: passed. New native 1x2 and 2x1 results require a fresh output root and remain unverified pending execution. M1-M3 are not complete.
