# P5.1-R38：Dense 百形状逐 Fresh 冻结源码与工具审计

日期：2026-09-17。源码提交：`aa7160cc`。范围为 Dense resident 100-shape 全局审计器的证据绑定加固；四分片实际完成之前不生成通过收据，不计入 M3 主矩阵通过数。

`audit_dense_native_mesh_release` 原先已经严格核对四个 canonical 分片、每 case 的两次真实产物、ProgramIO/NPUP/linked 摘要及物理 Die 活跃情况。本次增加逐 Fresh 的 `source_tool_binding.json` 复核：其 finalizer、resolver、NpuSim、simulation 四项 SHA 必须等于发布输入；首次导入和运行期间追加导入的每个 Python 文件，必须是冻结 checkout 下的相对 `.py` 路径，真实文件字节摘要必须等于 Fresh 声明。跨 case 缓存相同源码的摘要，避免 200 次重复扫描整个源码树。此检查防止从其他源码或工具版本拼接旧成功 case 后生成新发布收据。

验证：`python3 -m unittest llm.test.frontend.unit.test_audit_dense_native_mesh_release -v`，3 项通过，覆盖缺失/重复/乱序/额外 case、修改源码、替换工具摘要及目录穿越路径的拒绝。另将真实 1×4 全 Die Fresh0 与冻结 checkout `01e33744`、绑定工具重新匹配，检查通过。`git diff --check` 通过。尚未完成 100 shape 四分片，因此这只是审计器和单 Fresh 校验通过，不宣称 M3 发布通过。
