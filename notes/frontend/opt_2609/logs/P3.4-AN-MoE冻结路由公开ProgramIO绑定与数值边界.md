# P3.4-AN MoE 冻结路由公开 ProgramIO 绑定与数值边界

状态：EP1 两层 step0 前向的 P2 frozen route→HBM StateABI→ProgramIO→native one-shot 全链诊断通过；两步训练门禁仍关闭。

以 `build_moe_full_train_route_table_source(phase, sequence)` 从真实四个 P2 unit 中导出当前 step0 两份 80B 路由 seed。`bind_full_moe_route_state_program_io` 对 `phase`、`sequence`、物理 `placement`、两层 route home、StateABI、唯一 HBM ABSOLUTE_ADDRESS symbol 和每份 payload SHA-256 逐项核验。生成 `/tmp/moe_ep1_forward_stage_a/program_io_route_bound.json`，与原 ProgramIO 的 76 个 initialization 和单个 output probe 完全一致，只有 `producer_pass` 标成 `source_bound_full_moe_route_program_io`。篡改 payload 必须失败。

生产 `npusim_program_io_selftest --resolve` 返回 `ProgramIo resolved ... initializations=76 probes=1`。`npusim --program-one-shot` 对 route-bound sidecar 退出码 0；原始输出 `/tmp/moe_ep1_forward_stage_a/npusim_route_bound.log`：`PROGRAM_IO` resolve/apply/verify 全 pass，两层各有一次 `MOE_SIGNED_ROUTER pass=1`，`PROGRAM_MEMORY` core0 LSU 21/21、HBM read 1112B、residual 0，末尾 router/D2D drain 0、credit 平衡。

数值边界：另一份诊断 sidecar 把 token ID 改成 0,1,2,3、FP16 HBM state 改成逐元素非零模式，NpuSim 仍执行，但 16B loss probe 仍为全零 SHA-256 `374708fff7719dd5979ec875d56cd2286f6d3cf7ec317a3b25632aab28ec37bb`。源码 `exact_stage2_prims.cpp` 的 `Exact_stage2_prim_base` 令 `skip_input/skip_output=true`，`Cross_entropy_forward_prim::taskCore` 只登记 published timing work；因此当前 native timing PASS 不证明数值 loss/梯度，更不能代替 full SGD step。不同输入诊断原始输出 `/tmp/moe_ep1_forward_stage_a/npusim_variant.log`。

后续仍需真实 shared dCombined producer、expert/backbone 反向及两步 SGD 状态链，再按冻结 source/tool 做两个独立 Fresh。
