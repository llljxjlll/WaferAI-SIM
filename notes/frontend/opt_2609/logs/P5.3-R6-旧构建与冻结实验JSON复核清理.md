# P5.3-R6：旧构建与冻结实验 JSON 复核清理

日期：2026-09-17。接用户再次要求，在前次清理之后重新扫描 `/tmp` 和 `/workspace/build*`，先核对当前进程、测试脚本默认工具目录、文件字节及权限，再处理可重建的构建缓存与精确重复的 JSON。

- `ps` 未发现仍在运行的 CMake、NpuSim、Dense 矩阵或受管临时任务。删除 `/workspace/build-moe-train-offload`（约 125 MiB）和 `/workspace/build-moe-training-grad`（约 2.8 GiB），两者没有进程占用，也不是集成脚本默认工具目录。
- 删除 2026-08-11 的 `/tmp/npusim-release-gate-final.ZnAyhr/src/build-debug` 和 `src/build-release`，合计约 1.3 GiB；保留外层原源码快照和 `source-snapshot.tar`。
- 前次因进程仍在运行而保护的六棵冻结源码树现已停止运行。对其中 `exps/exp4` 三个大型 JSON 与 TP16 四个负载 JSON，核对 SHA-256、文件大小和权限后，将 42 个完全相同的路径原子硬链接到另一棵闲置冻结源码树中的对应文件，回收 1,652,318,208 字节。所有路径、内容和源码树保留。冻结目录后续应只读；若要修改，先复制到新目录。
- 复查 `/tmp` 中 20 MiB 以上的 `npusim`：先前处理的重复二进制已共享 inode。仅四份正式 checkpoint Fresh 工具虽然哈希相同，仍保持四个独立文件，避免改变其独立构建证据。
- `scripts/frontend_tmp.py prune` 只读预览显示受管根目录约 2.3 MiB，当前无过期可删任务。没有改动待续跑的 Dense 100 形状分片或 TP6、MoE、AdamW 的原生 Fresh 证据。

保留的 `/workspace/build-debug-final`、`build-debug-dense-infer-pager` 和 `build-release-final` 均被现有集成或发布脚本作为默认工具路径引用，暂不认定为“不再使用”。复核后 `/tmp` 为约 44 GiB，可用空间约 872 GiB；与前次的 46 GiB 之间也包含已完成实验产物随时间的变化。此次清理没有修改生产源码或实验 JSON 的字节，故只作路径、SHA 和进程校验，不重跑仿真。
