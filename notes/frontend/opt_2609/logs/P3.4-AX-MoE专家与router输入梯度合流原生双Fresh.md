# P3.4-AX：MoE EP1 专家与 router 输入梯度合流原生双 Fresh

状态：**partial native pass；M1/M2/M3 full-training gate closed**。本阶段将层 1 专家反向 activation dX 与 router `GEMM_INPUT_DX` 相加，生成共享 norm2 的上游梯度。只有签名 EP1 冻结路由的所有 token 属于 expert0 且 slot 顺序严格为 `0..M-1`，才允许 packed expert dX 直接作为 token 顺序输入；其他 permutation 必须有独立 inverse dispatch，当前阶段拒绝。IR0 同时校验两条梯度的同层/同 step/同 digest/同 norm2 来源、形状、dtype、物理 owner。加法使用官方 FP16 `RESIDUAL` 物理路径，而非创建 detached tape。

源码提交 `5f1c7285`：IR0 来源与篡改负测、官方 canary `--input-gradient`、严格 reopen auditor profile。新 source 测试 5/5，相关 GEMM/router 测试 19/19 PASS；旧 expert 与 router dX Fresh 用新 auditor 复审均 PASS。

正式 clean detached freeze `/tmp/moe_input_gradient_freeze` 为 `5f1c7285e28ce22d12348d319b9249b415ce82b6`；工具 `/tmp/moe_router_dx_tools` 的 SHA256 为 finalizer `6cc731011230c674e243279a7e7a65490e476c645a261177c71fa21d3fbd09c1`、resolver `f39ca054372cdd1c4618b1db304f3a78294a76880fc89d5f66c36eef1f3e4856`、NpuSim `b9db246e81ea353d931a894e59cb330c22810120bbc5b1f613dbb2e70cbb98d5`；真实 NpuSim cwd 绑定该工具目录。

两次全新 materialize→finalizer→resolver→NpuSim 根 `/tmp/moe_input_gradient_fresh0`、`/tmp/moe_input_gradient_fresh1`，各含 step0/step1，一共四次 one-shot 原生执行。每 step 64 leaves、304 native records、5 dX、5 WGRAD、101 初始化、9 output probes。被加法消费的 expert dX/router dX 已非 terminal output；合流后的 `backward::T0.layer1.moe.input_sum.norm2_gradient` 有且仅有一个物理 output probe。每步 `PROGRAM_MEMORY core=0` HBM 读 1248 B/写 0 B，ProgramIO verify、credit 平衡和 D2D drain 均 PASS；0 SGD。

严格只读审计 `/tmp/moe_input_gradient_double_fresh_audit.json` 为 `pass_partial_only`：两根各重开 17 个产物，重算 31 个显式源码哈希、341 个入口/351 个出口 Python module 哈希、三工具 SHA、NpuSim cwd、linked IDs、records、NPUP、ProgramIO 与日志。两个 Fresh 同步骤 linked IDs 精确相同。

当前 merge 的两个 upstream 来自 timing primitives，尚无数值梯度字节生产；probe 期望仍为全零。此证据只证明签名 identity 路由下的真实物理依赖/地址/执行，不证明梯度值或参数变化，也没有同一 invocation 的两步 SGD。下一段需经 norm2/残差/注意力返回 layer0，并接全部状态更新和可检查的 gradient byte producer。
