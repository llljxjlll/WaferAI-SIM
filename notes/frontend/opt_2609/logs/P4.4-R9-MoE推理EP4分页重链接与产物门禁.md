# P4.4-R9：MoE 推理 1x4 EP4 分页重链接与生产产物门禁

日期：2026-09-16。继 R8 证明同模型每 Die 1024B resident 容量拒绝、8192B 外存规划通过之后，新增独立 `moe_inference_paged_compile_sequence_ep4.py`；旧 EP2 relinker/runtime 与训练路径未改。新版针对固定 1x4 两层 Prefill→Decode→Decode 的真实四核源，严格检查每段 39/43 fragment、4 个 runtime core `0,4,8,12`、31 个 StateABI、Die0 616B 共享/路由权重、每 Die 两层 192B 专家页及 32B gate、四页版本化 KV、原始 HBM home/洞锚点，才把物理状态重链接到各 Die 的 1024B home 内。LSU/DTE/compute 记录和 core streams 保持原生产 manifest；relocated ABI 逐项 `validate()`，每页末端不超过相对地址 960B。

隔离可复现命令（使用 R8 同一冻结源码和 finalizer）：

```bash
PYTHONPATH=/tmp/p4-moe-core4-source-21680aa6 python3 \
  llm/test/frontend/integration/run_moe_inference_paged_ep4_compile_canary.py \
  --source-root /tmp/p4-moe-core4-source-21680aa6 \
  --finalizer /tmp/p4-moe-core4-tools-fullfresh-21680aa6/bin/npusim_program_finalizer \
  --output /tmp/moe-m2-ep4-compile-canary-v1
```

三段都从冻结 full-model/block production source 编译，分别生成原 source 和 EP4 paged linked manifest，调用未修改的原生 finalizer 各六次生成 NPUP 并重开其报告 SHA/linked digest；以原模型 ProgramIO 构建器生成真实协议，再按 paged linked manifest 重定位，`validate_against()` 三段全通过。三段输出 `MoE EP4 compile/finalizer/ProgramIO PASS`，进程退出码 0。原 source manifest ID 为 `linked_program_manifest_90e2c6da85b8fb90`、`linked_program_manifest_0551d6add777f07c`、`linked_program_manifest_f2d585a9cfa03b7a`；paged ID 为 `linked_program_manifest_05f35673e86f091e`、`linked_program_manifest_e44ae1a9d6c90bdf`、`linked_program_manifest_3d3338383c189d45`。各段真实 ProgramIO 初始化 69 条、logits probe 一条；对应四核全覆盖。

产物根 `/tmp/moe-m2-ep4-compile-canary-v1` 的 `compile_canary_receipt.json` SHA-256 为 `84f4dd78b55b9b85ec34e9dab65fbbf8d7952ef2d0de47ddcaebd84a636f42f4`，状态字段明确为 `compile_finalizer_program_io_only`。relinker SHA-256 `0a104a7b5cbd5d9f8cbfb0c0183e136c6902076c041cf3200ec1a41668a07253`，canary SHA-256 `e079312b2024516a100acfed62c53c8508e951e99f088a36797539f18c270772`；对应测试从真实 1x4 源编译、检查三段 ABI/四核、越界 step 拒绝，2 项通过。

**尚未 offload 通过**：这是源→manifest→finalizer→ProgramIO 阶段进展，没有版本化 EP4 external sidecar，更没有 EP4 NpuSim DMA、真实页读写/链路或双 Fresh。下一步需要从这 31 个真实 StateABI 与 39/43 linked LSU gate 推导四 Die 物理页/外存 authority，再同步扩展原生 MoE pager 的 typed schema、四 Die 路由和 DMA 计数；固定每 Die 1024B 与同模型 resident 拒绝不变。
